<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://yjkim-stat.github.io/log/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yjkim-stat.github.io/log/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-19T07:52:10+00:00</updated><id>https://yjkim-stat.github.io/log/feed.xml</id><title type="html">Research Log</title><subtitle>Paper reviews, recent research trends, and notes on machine learning research. </subtitle><entry><title type="html">Process Reward Agents — Online Step-Wise Steering for Knowledge-Intensive Reasoning</title><link href="https://yjkim-stat.github.io/log/blog/2026/pra/" rel="alternate" type="text/html" title="Process Reward Agents — Online Step-Wise Steering for Knowledge-Intensive Reasoning"/><published>2026-07-19T04:00:00+00:00</published><updated>2026-07-19T04:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/pra</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/pra/"><![CDATA[<p><strong>Paper.</strong> Jiwoong Sohn, Tomasz Sternal, Kenneth Styppa, Torsten Hoefler, Michael Moor. <em>Process Reward Agents for Steering Knowledge-Intensive Reasoning.</em> ICML 2026. ETH Zürich · Heidelberg University. <a href="https://arxiv.org/abs/2604.09482">[arXiv]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>In math and code, intermediate reasoning steps are locally verifiable — you can check each algebraic step or run the code. In knowledge-intensive domains like medicine, step verification requires cross-referencing vast external knowledge bases, and a subtly wrong clinical inference may propagate through an entire reasoning trace undetected. <strong>Process Reward Agents (PRA)</strong> address this by training a dedicated <em>reward agent</em> — distinct from the reasoning policy — to observe the unfolding CoT trace, selectively query external knowledge via retrieval, and assign scalar reward scores to candidate continuations <em>during</em> beam search. The policy model is never fine-tuned; only the reward agent is trained. The result is a modular, backbone-agnostic plug-in: a frozen Qwen3-4B-Instruct reaches <strong>80.8% on MedQA</strong> (new SOTA at the 4B scale), with up to <strong>+25.7% improvement</strong> across frozen policy models ranging from 0.5B to 8B parameters. Unlike Self-Consistency, which degrades on hard benchmarks as more samples are added, PRA continues improving monotonically as inference compute scales.</p> <hr/> <h2 id="1-the-problem--non-local-verifiability">1. The Problem — Non-Local Verifiability</h2> <p>Process reward models (PRMs) have been applied successfully to mathematics: you train a reward model to score each step of a proof or calculation, then use those scores to guide beam search toward correct solutions. The recipe works because math steps are <em>locally verifiable</em> — you can determine whether an intermediate algebraic transformation is correct without knowing the final answer.</p> <p>Medical reasoning is structurally different:</p> <table> <thead> <tr> <th>Property</th> <th>Math / Code</th> <th>Medicine</th> </tr> </thead> <tbody> <tr> <td>Local verifiability</td> <td>Yes — each step checkable in isolation</td> <td>No — verifying a clinical inference requires synthesizing clues across large knowledge bases</td> </tr> <tr> <td>Error propagation</td> <td>Detectable early</td> <td>Subtle errors compound undetected through the trace</td> </tr> <tr> <td>External knowledge needed</td> <td>Rarely</td> <td>Constantly (drug interactions, symptom prevalence, guidelines)</td> </tr> </tbody> </table> <p>Prior retrieval-augmented PRMs score <em>completed</em> trajectories after the fact. They cannot steer what is generated — by the time a bad reasoning path is scored, the trajectory is already done. PRA’s contribution is making the process reward signal <em>online</em> and <em>integrated into the generation loop</em>.</p> <hr/> <h2 id="2-the-architecture--frozen-policy--reward-agent">2. The Architecture — Frozen Policy + Reward Agent</h2> <p>PRA decouples two roles that prior work combined:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Policy model (frozen)   →   generates candidate CoT continuations
         ↓
Reward agent (trained)  →   reads partial trace
                        →   decides whether to retrieve
                        →   synthesizes retrieved docs with trace
                        →   assigns reward score per candidate
         ↓
Beam search             →   prunes low-reward candidates
                        →   propagates high-reward candidates
</code></pre></div></div> <h3 id="21-frozen-policy-model">2.1 Frozen policy model</h3> <p>The reasoning policy generates text. It is never updated during PRA deployment. This separation means: when a better backbone model becomes available, you swap the frozen policy with no retraining of the reward agent. Domain expertise transfers.</p> <h3 id="22-reward-agent">2.2 Reward agent</h3> <p>A separate LLM trained to:</p> <ol> <li>Read the partial CoT trace up to the current step.</li> <li>Decide whether to issue a retrieval query.</li> <li>If retrieval is useful: select a query, retrieve relevant documents (medical literature, PubMed, clinical guidelines), and synthesize them with the trace.</li> <li>Assign a scalar reward score to each candidate continuation produced by the frozen policy.</li> </ol> <p>The reward agent’s training labels are generated by a teacher model: given the partial trace and retrieved documents at each step, the teacher assesses whether the partial step is on track. This produces “reasoning labels” that train the reward agent to assign reliable intermediate rewards at inference time.</p> <h3 id="23-integration-into-beam-search">2.3 Integration into beam search</h3> <p>At each beam search step:</p> <ol> <li>The frozen policy autoregressively proposes K candidate continuations.</li> <li>The reward agent scores each candidate.</li> <li>Beam search uses scores to rank and prune: low-scoring candidates are discarded, high-scoring ones propagate.</li> <li>Repeat until the final answer is generated.</li> </ol> <p>This is <strong>online</strong> (rewards computed during generation, not after) and <strong>process-level</strong> (rewards at intermediate steps, not just the final answer).</p> <hr/> <h2 id="3-results">3. Results</h2> <h3 id="31-medqa-usmle-style">3.1 MedQA (USMLE-style)</h3> <table> <thead> <tr> <th>Model</th> <th>Accuracy</th> </tr> </thead> <tbody> <tr> <td>PRA + Qwen3-4B-Instruct</td> <td><strong>80.8%</strong> (new SOTA at 4B scale)</td> </tr> <tr> <td>Improvement across 0.5B–8B frozen policies</td> <td>up to <strong>+25.7%</strong></td> </tr> </tbody> </table> <p>All improvements are achieved without modifying the policy model.</p> <h3 id="32-generalization-benchmarks">3.2 Generalization benchmarks</h3> <p>PRA is evaluated out-of-distribution on:</p> <ul> <li><strong>MedBullets Op4 / Op5</strong> (harder USMLE-style)</li> <li><strong>MedMCQA</strong> (Indian medical licensing exam)</li> <li><strong>MMLU-Med</strong> (medical subset of MMLU)</li> <li><strong>GPQA</strong> (graduate-level “Google-proof” expert questions)</li> <li><strong>Lancet clinical cases</strong> (real-world Lancet journal cases)</li> <li><strong>NEJM clinical cases</strong> (New England Journal of Medicine cases)</li> </ul> <p>PRA consistently outperforms baselines (greedy CoT, RAG, Self-Consistency, Best-of-N, post-hoc PRM scoring) across all benchmarks under matched sampling budgets.</p> <h3 id="33-inference-time-scaling-behavior">3.3 Inference-time scaling behavior</h3> <p>This is the most practically important finding:</p> <p><strong>Self-Consistency degrades on hard benchmarks.</strong> As the number of sampled trajectories increases past ~8, majority vote begins amplifying errors — the policy frequently produces incorrect responses across samples, so the plurality answer is wrong. Self-Consistency actively <em>hurts</em> accuracy on GPQA and Lancet at high sample counts.</p> <p><strong>PRA continues improving.</strong> Because step-wise steering recovers from early errors (rather than averaging over them), PRA’s accuracy increases monotonically with inference compute. There is no observed saturation point in the evaluated range.</p> <p>The mechanism is clear: when the policy starts down a wrong path, the reward agent’s low score causes that branch to be pruned before the error compounds. Self-Consistency has no such pruning — it samples independently and votes.</p> <h3 id="34-ablations">3.4 Ablations</h3> <table> <thead> <tr> <th>Ablation</th> <th>Finding</th> </tr> </thead> <tbody> <tr> <td>Process-level vs. outcome-level rewards</td> <td>Process-level substantially better</td> </tr> <tr> <td>Online vs. offline (post-hoc) timing</td> <td>Online substantially better</td> </tr> <tr> <td>Both process + online</td> <td>Necessary; removing either reduces performance substantially</td> </tr> </tbody> </table> <hr/> <h2 id="4-relation-to-prior-work">4. Relation to Prior Work</h2> <h3 id="post-hoc-prms">Post-hoc PRMs</h3> <p>Standard PRMs and retrieval-augmented PRMs score completed trajectories. They can identify good vs. bad completions after the fact, but they cannot intervene during generation to prevent bad paths from developing. PRA makes the reward signal <em>generative</em> — it shapes what is produced, not just evaluates what was produced.</p> <h3 id="rag-retrieval-augmented-generation">RAG (Retrieval-Augmented Generation)</h3> <p>Standard RAG appends retrieved documents to the prompt at the beginning. PRA retrieves <em>selectively at each step</em> based on what has been reasoned so far. The reward agent decides <em>whether</em> to retrieve and <em>what</em> to query based on the partial trace — a more targeted and dynamic use of retrieval.</p> <h3 id="best-of-n">Best-of-N</h3> <p>Best-of-N samples N complete trajectories and picks the highest-reward one. PRA prunes <em>during</em> generation, preventing bad branches from completing. This is more efficient and produces better results because early pruning stops error compounds before they occur.</p> <hr/> <h2 id="5-why-it-matters">5. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>Process-level rewards for knowledge-intensive domains.</strong> The PRM literature has largely focused on math. PRA is a concrete recipe for extending the process reward paradigm to domains where step verification requires external knowledge — a much larger class of real-world tasks.</li> <li><strong>Frozen policy + trained reward agent = modular deployability.</strong> The decoupled design means domain expertise lives in the reward agent, not the policy. You can upgrade the backbone reasoning model without retraining domain knowledge. This is a practically important architectural property for medical AI deployment.</li> <li><strong>Online steering vs. post-hoc scoring changes the scaling curve.</strong> Self-Consistency’s failure mode at high compute (degrading on hard tasks) is well-documented but underappreciated in practice. PRA’s demonstration that online process rewards maintain monotonic improvement where Self-Consistency fails is a direct argument for moving beyond best-of-N and majority vote as inference scaling strategies.</li> </ol> <hr/> <h2 id="6-limitations-worth-knowing">6. Limitations Worth Knowing</h2> <ul> <li><strong>Not a deployment system.</strong> The paper explicitly states this is a method contribution, not a ready-to-deploy medical decision-support system.</li> <li><strong>Hallucination not eliminated.</strong> PRA reduces the propagation of incorrect intermediate steps but does not guarantee their absence.</li> <li><strong>Inference cost.</strong> Every generation step incurs retrieval overhead plus reward computation, making PRA more expensive than greedy decoding or standard RAG.</li> <li><strong>Hard tasks remain hard.</strong> On very challenging benchmarks (GPQA, Lancet), PRA is more stable than Self-Consistency but still operates under the inherent difficulty of the domain.</li> </ul> <hr/> <h2 id="7-the-takeaway-for-a-first-reader">7. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Knowledge-intensive reasoning has a non-local verification problem</strong> — you can’t check a clinical inference without consulting external knowledge. Prior PRMs were post-hoc; PRA trains a <strong>dedicated reward agent</strong> that reads the partial trace at each step, selectively retrieves relevant knowledge, and assigns step-level reward scores <strong>during</strong> beam search — <strong>without touching the policy model</strong>.</li> <li><strong>Self-Consistency degrades on hard benchmarks as samples increase</strong> (majority vote amplifies errors). <strong>PRA continues improving monotonically</strong> because early pruning prevents bad paths from compounding into completed trajectories.</li> <li><strong>PRA + Qwen3-4B-Instruct achieves 80.8% on MedQA</strong> (SOTA at 4B scale), with <strong>up to +25.7%</strong> improvement across frozen 0.5B–8B policy models and consistent gains across 7 medical reasoning benchmarks.</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Sohn, J., Sternal, T., Styppa, K., Hoefler, T., &amp; Moor, M. (2026). <em>Process Reward Agents for Steering Knowledge-Intensive Reasoning.</em> ICML 2026. <a href="https://arxiv.org/abs/2604.09482">arXiv:2604.09482</a>.</li> <li>Related on this site: <a href="/log/blog/2026/rlhf-to-ruler/">RLHF → RULER trend note</a> — the process reward model context this work extends; <a href="/log/explorations/2026-07-06-adversarial-feedback-design/">Ch.8 Adversarial Feedback Design</a> — external patterns in critic/verifier agent design.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="reasoning"/><category term="process-reward"/><category term="retrieval"/><category term="medical-ai"/><category term="beam-search"/><summary type="html"><![CDATA[Paper review of PRA (Sohn, Sternal et al., ICML 2026, ETH Zürich / Heidelberg) — a framework that trains a separate reward agent to provide online, process-level, retrieval-grounded feedback to a frozen policy model during beam search. A 0.5B–8B frozen reasoner gets up to +25.7% on MedQA; Qwen3-4B-Instruct reaches 80.8% SOTA at 4B scale. Self-Consistency degrades on hard benchmarks; PRA continues improving as inference compute scales.]]></summary></entry><entry><title type="html">Tele-Lens — How Far Ahead Do LLMs Actually Plan in Chain-of-Thought?</title><link href="https://yjkim-stat.github.io/log/blog/2026/tele-lens/" rel="alternate" type="text/html" title="Tele-Lens — How Far Ahead Do LLMs Actually Plan in Chain-of-Thought?"/><published>2026-07-19T03:00:00+00:00</published><updated>2026-07-19T03:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/tele-lens</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/tele-lens/"><![CDATA[<p><strong>Paper.</strong> Liyan Xu, Mo Yu, Fandong Meng, Jie Zhou. <em>How Far Ahead Do LLMs Plan? Uncovering the Latent Horizon in Chain-of-Thought Reasoning.</em> ICML 2026. WeChat AI, Tencent. <a href="https://arxiv.org/abs/2602.02103">[arXiv]</a> · <a href="https://github.com/lxucs/tele-lens">[code]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>A persistent question in CoT interpretability: do LLMs actually plan globally in their hidden states before verbalizing the reasoning steps, or are they making purely local token-by-token transitions? Prior evidence suggests both directions — hidden states seem to encode future information, yet removing explicit CoT steps destroys compositional reasoning. <strong>Tele-Lens</strong> settles this question empirically with a probing framework that tracks three signals simultaneously across the full CoT trajectory: predicted next tokens, predicted final answer, and predicted total reasoning length. The answer is <strong>myopic</strong>: for compositional tasks (multi-step algorithmic reasoning), the model’s hidden states carry <strong>near-chance final-answer predictions (0.49–0.51) throughout most of the trajectory</strong>, spiking to 0.94–0.99 only in the final one or two steps before completion. LLMs do not pre-plan; they compute step by step. This insight has two practical payoffs: a <strong>sparse set of “pivot” positions</strong> (identified by Tele-Lens signals near CoT completion) can represent the full trajectory’s uncertainty with <strong>+6% accuracy improvement</strong>, and automatic CoT bypass achieves a <strong>16.2% bypass rate with negligible performance loss</strong> by detecting when hidden states already carry high early confidence.</p> <hr/> <h2 id="1-the-open-question--do-llms-plan-ahead-in-their-hidden-states">1. The Open Question — Do LLMs Plan Ahead in Their Hidden States?</h2> <p>The empirical evidence before this paper pulled in two directions:</p> <p><strong>Evidence suggesting latent planning:</strong> “Future Lens” (Pal et al., ACL 2023) showed that intermediate Transformer layer activations encode information about tokens and even final answers <em>before</em> those outputs are verbalized. This implies some degree of latent forward planning.</p> <p><strong>Evidence against global planning:</strong> For tasks requiring compositional computation — multi-step algorithms, logical deduction chains — removing explicit CoT steps causes catastrophic performance drops. If the answer were truly pre-planned in latent space, removing intermediate verbalizations should matter less.</p> <p>Tele-Lens asks: <strong>at what point in the CoT trajectory do hidden states reliably encode the final answer?</strong> If the model is planning globally, that signal should appear early. If it is computing locally, the signal should appear only near the end.</p> <hr/> <h2 id="2-tele-lens--a-probing-framework-for-teleological-information">2. Tele-Lens — A Probing Framework for Teleological Information</h2> <h3 id="21-conceptual-lineage">2.1 Conceptual lineage</h3> <p>Tele-Lens generalizes two prior probing tools:</p> <ul> <li><strong>Logit Lens</strong> (Nostalgebraist, 2020): bridges each Transformer layer’s hidden state to the LM head, showing how token predictions evolve through depth.</li> <li><strong>Future Lens</strong> (Pal et al., 2023): extends this to predict <em>subsequent</em> tokens from a single hidden state.</li> </ul> <p>Tele-Lens adds a third dimension — <strong>reasoning length prediction</strong> — and applies all three probes simultaneously across the temporal dimension (CoT trajectory position) rather than just depth.</p> <h3 id="22-three-probing-dimensions">2.2 Three probing dimensions</h3> <p>For each hidden state at each step in a CoT trajectory, Tele-Lens trains probes to predict:</p> <table> <thead> <tr> <th>Dimension</th> <th>What it probes</th> </tr> </thead> <tbody> <tr> <td><strong>Subsequent tokens</strong></td> <td>What comes next at a given offset (1-step, 2-steps ahead)</td> </tr> <tr> <td><strong>Final answer</strong></td> <td>What the model’s final answer will be (classification head)</td> </tr> <tr> <td><strong>Reasoning length</strong></td> <td>How long the total CoT trajectory will be (regression head)</td> </tr> </tbody> </table> <h3 id="23-architecture">2.3 Architecture</h3> <p>A bottleneck low-rank adapter with added nonlinearity is trained on top of each hidden state to produce the three probing outputs. The adapter has far fewer parameters than the LLM itself, reducing overfitting risk. One adapter per task family is trained on held-in data.</p> <h3 id="24-two-model-settings">2.4 Two model settings</h3> <ul> <li><strong>In-Domain LLM:</strong> Qwen2.5-7B-Instruct fine-tuned via <strong>GRPO</strong> on the target tasks. Chosen because this model lacks a native “thinking” mode, enabling a clean bootstrap of CoT.</li> <li><strong>Off-the-shelf LLM:</strong> Publicly available models without task-specific training.</li> </ul> <h3 id="25-task-suite">2.5 Task suite</h3> <p>12 datasets across three categories:</p> <table> <thead> <tr> <th>Category</th> <th>What it requires</th> </tr> </thead> <tbody> <tr> <td><strong>Explicit Compositional</strong></td> <td>Multi-step algorithmic reasoning (Parity, Cycle, Subsum)</td> </tr> <tr> <td><strong>Implicit Compositional</strong></td> <td>Mathematical and logical reasoning</td> </tr> <tr> <td><strong>Semantics &amp; Knowledge</strong></td> <td>MMLU-style factual understanding</td> </tr> </tbody> </table> <hr/> <h2 id="3-the-core-empirical-finding--myopic-planning">3. The Core Empirical Finding — Myopic Planning</h2> <h3 id="31-final-answer-probability-trajectory">3.1 Final-answer probability trajectory</h3> <p>For <strong>compositional tasks</strong> (the hardest and most diagnostic category), the final-answer probability extracted from hidden states follows a characteristic profile:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Most of CoT trajectory:   ~0.49–0.51 (near chance)
Final 1–2 steps:          0.94–0.99 (decisive spike)
</code></pre></div></div> <p>The model has essentially no reliable internal prediction of the final answer until the very last steps of the chain, regardless of how long the chain is. This directly contradicts a “latent global planning” hypothesis.</p> <p>Concrete examples:</p> <ul> <li><strong>Parity task</strong> (is the number of 1s in a binary string odd or even?): The model cannot predict parity until it has counted every bit. The final-answer hidden state signal only appears after the last digit is processed.</li> <li><strong>Cycle task</strong> (does a graph contain a cycle?): The answer crystallizes only after the model has verbally traced a complete path or detected a cycle step-by-step.</li> </ul> <h3 id="32-reasoning-length-prediction--no-global-clock">3.2 Reasoning length prediction — no global clock</h3> <p>Early hidden states show no reliable internal estimate of total reasoning length — with an important caveat:</p> <p>For <strong>Parity</strong> and <strong>Subsum</strong>, early states appear to correlate with true reasoning length. But this turns out to be a <strong>confound</strong>: in those tasks, reasoning length is proportional to input length (a surface shortcut). For <strong>Cycle</strong> tasks, reasoning length scales with path depth — which is <em>not</em> proportional to input length — and here early states completely fail to predict reasoning length.</p> <p>This exposes the Parity/Subsum correlation as shallow pattern matching, confirming the absence of genuine global planning.</p> <h3 id="33-simple-tasks--coarse-gist-not-plan">3.3 Simple tasks — coarse gist, not plan</h3> <p>For semantics/knowledge tasks, early hidden states do carry some predictive signal about the final answer. But the paper characterizes this as <strong>pattern-matching gist</strong> — recognizing a coarse category from surface features — not step-by-step planning. The signal is imprecise and degrades on unfamiliar inputs.</p> <h3 id="34-what-this-means-for-cot">3.4 What this means for CoT</h3> <p>The findings land squarely on one side of a foundational debate:</p> <blockquote> <p><strong>CoT = scratchpad computation</strong>, not “decoding a latent plan.”</p> </blockquote> <p>For compositional reasoning tasks, the intermediate steps are not verbalizations of a pre-existing internal plan — they <em>are</em> the computation. Removing them removes the reasoning itself.</p> <hr/> <h2 id="4-practical-applications">4. Practical Applications</h2> <h3 id="41-pivot-based-uncertainty-estimation">4.1 Pivot-based uncertainty estimation</h3> <p>The finding that useful hidden-state signal concentrates in the final 1–2 steps of CoT suggests that a <strong>sparse set of “pivot positions”</strong> (positions where Tele-Lens signals are most informative) can stand in for full-trajectory uncertainty estimation.</p> <p>Result: <strong>+6% accuracy improvement</strong> compared to naive full-trajectory uncertainty estimation.</p> <p>Comparison: positions selected by standard LM entropy spread across the whole trajectory; positions selected by Tele-Lens cluster near CoT completion — confirming that Tele-Lens adds signal beyond vanilla entropy.</p> <h3 id="42-automatic-cot-bypass">4.2 Automatic CoT bypass</h3> <p>When Tele-Lens signals show high early confidence — the model’s hidden states already encode the final answer clearly from the start — CoT may be unnecessary overhead.</p> <p>Result: <strong>16.2% bypass rate</strong> (proportion of inputs where full CoT is skipped entirely) <strong>with negligible performance loss</strong>.</p> <p>This provides an inference-time efficiency win without requiring a separately labeled router.</p> <hr/> <h2 id="5-why-it-matters">5. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>It resolves the latent-planning debate empirically.</strong> The question “are LLMs pre-computing in latent space?” has significant implications for understanding what CoT is doing and how to improve it. Tele-Lens gives a definitive answer for compositional tasks: no. The scratchpad is the computation.</li> <li><strong>It reframes the value of CoT.</strong> If CoT were just verbalizing a latent plan, you could in principle extract the plan directly and skip the verbalizations. Tele-Lens shows this is not possible: each CoT step genuinely updates the model’s internal state in a way that previous steps did not accomplish. This validates the investment in long CoT for hard tasks.</li> <li><strong>The uncertainty estimation application has practical value.</strong> A method that can reliably identify the handful of positions in a CoT trajectory that carry the most uncertainty information — using learned probes, not heuristics — enables cheaper and better calibration of model confidence, with direct deployment relevance.</li> </ol> <hr/> <h2 id="6-limitations-worth-knowing">6. Limitations Worth Knowing</h2> <ul> <li><strong>Training requirement.</strong> Tele-Lens requires a trained probing adapter, which needs task-specific data. It is heavier than zero-shot interpretability approaches.</li> <li><strong>In-domain focus.</strong> Key quantitative results are on the GRPO- trained Qwen2.5-7B-Instruct model on the curated 12-task suite. Direct generalization to frontier closed-source models (GPT-4, Claude, Gemini) is not confirmed.</li> <li><strong>Task scope.</strong> The 12 tasks skew toward controlled algorithmic and knowledge-QA settings. Whether the myopic pattern holds for open-ended long-horizon tasks (multi-document synthesis, large codebase reasoning) remains open.</li> <li><strong>Correlational, not causal.</strong> Probing establishes that hidden states do not encode future planning signals early — but does not establish <em>why</em>, or whether interventions could force global planning.</li> <li><strong>Per-domain adapter calibration.</strong> The adapter needs domain- specific calibration, limiting plug-and-play deployability across arbitrary tasks.</li> </ul> <hr/> <h2 id="7-the-takeaway-for-a-first-reader">7. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>LLMs are myopic planners.</strong> For compositional tasks, hidden states carry <strong>near-chance final-answer predictions (0.49–0.51) throughout CoT</strong>, spiking to <strong>0.94–0.99 only in the final 1–2 steps</strong>. There is no evidence of global latent planning — CoT is scratchpad computation, and each step genuinely advances the reasoning in a way hidden states alone could not.</li> <li><strong>Tele-Lens probes three signals simultaneously</strong> across the CoT trajectory: subsequent tokens, final answer, and reasoning length — using a bottleneck low-rank adapter trained per task family. The reasoning-length result exposes a surface confound in simpler tasks, strengthening the myopia conclusion.</li> <li><strong>Two practical payoffs:</strong> a sparse set of <strong>pivot positions</strong> near CoT completion can represent full-trajectory uncertainty with <strong>+6% accuracy improvement</strong>, and automatic <strong>CoT bypass achieves 16.2% bypass rate</strong> with negligible performance loss by detecting early high-confidence hidden states.</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Xu, L., Yu, M., Meng, F., &amp; Zhou, J. (2026). <em>How Far Ahead Do LLMs Plan? Uncovering the Latent Horizon in Chain-of-Thought Reasoning.</em> ICML 2026. <a href="https://arxiv.org/abs/2602.02103">arXiv:2602.02103</a>.</li> <li>Code &amp; data: <a href="https://github.com/lxucs/tele-lens">https://github.com/lxucs/tele-lens</a></li> <li>Pal, A., <em>et al.</em> (2023). <em>Future Lens: Anticipating Subsequent Tokens from a Single Hidden State.</em> ACL 2023.</li> <li>Related on this site: <a href="/log/blog/2026/mctd/">MCTD review</a> — inference-time search as an alternative to relying on a single CoT trace.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="interpretability"/><category term="chain-of-thought"/><category term="reasoning"/><category term="probing"/><category term="llm"/><summary type="html"><![CDATA[Paper review of Tele-Lens (Xu, Yu et al., ICML 2026, WeChat AI / Tencent) — a probing framework that measures how far ahead LLMs plan in hidden states during chain-of-thought reasoning. The answer: myopic. Final-answer probability in hidden states stays near chance (0.49–0.51) throughout CoT for compositional tasks, spiking to 0.94–0.99 only in the last 1–2 steps. Leveraging this, pivot-based uncertainty estimation improves accuracy +6%, and automatic CoT bypass achieves 16.2% bypass rate with negligible performance loss.]]></summary></entry><entry><title type="html">Reasoning Cache — Continual Improvement Over Long Horizons via Short-Horizon RL</title><link href="https://yjkim-stat.github.io/log/blog/2026/reasoning-cache/" rel="alternate" type="text/html" title="Reasoning Cache — Continual Improvement Over Long Horizons via Short-Horizon RL"/><published>2026-07-19T02:00:00+00:00</published><updated>2026-07-19T02:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/reasoning-cache</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/reasoning-cache/"><![CDATA[<p><strong>Paper.</strong> Ian Y.H. Wu, Yuxiao Qu, Amrith Setlur, Aviral Kumar. <em>Reasoning Cache: Continual Improvement Over Long Horizons via Short-Horizon RL.</em> ICLR 2026. Carnegie Mellon University. <a href="https://arxiv.org/abs/2602.03773">[arXiv]</a> · <a href="https://openreview.net/forum?id=DROMQyqM52">[OpenReview]</a> · <a href="https://github.com/IanYHWu/rc">[code]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Hard math and science olympiad problems require far more inference-time computation than models are trained on. Simply giving a model a larger token budget at test time does not help — the model was never trained to exploit that extra budget, and its learned distribution does not cover ultra-long traces. <strong>Reasoning Cache (RC)</strong> proposes an iterative decoding algorithm that sidesteps this distributional shift: instead of generating one long trace, the model runs <em>multiple short turns</em>, summarizing each turn’s work into a compact “reasoning cache” before beginning the next. The full trace from the previous turn is discarded; only the summary carries forward. A model trained at a 16K-token budget can thus reason across 8 × 16K = 128K tokens of effective compute without ever seeing a context longer than 16K during either training or inference. A <strong>summary replay buffer</strong> in Stage II of training teaches the model to leverage mid-trajectory summaries, enabling monotonic performance improvement as the test-time budget grows. <strong>RCT-4B</strong> reaches ~70% on HMMT-Nov-2025 at a 512K-token budget (up from ~40% at 16K), and surpasses Qwen3-30B-A3B-Instruct on IMO-AnswerBench at a 256K-token test budget.</p> <hr/> <h2 id="1-the-problem--extrapolation-beyond-the-training-budget">1. The Problem — Extrapolation Beyond the Training Budget</h2> <p>RL-trained reasoning models (DeepSeek-R1, QwQ, and their derivatives) are trained at a fixed token budget — typically 16K–32K tokens per problem. At test time, the hardest problems need much more compute. The naive fix — just give the model a longer context — fails:</p> <table> <thead> <tr> <th>Approach</th> <th>Why it fails</th> </tr> </thead> <tbody> <tr> <td>Longer context window</td> <td>Model’s learned policy does not exploit the extra tokens; it was never trained to self-correct across a long trace</td> </tr> <tr> <td>More sampling (best-of-N)</td> <td>Averages over independent draws; individual quality ceiling unchanged</td> </tr> <tr> <td>Chain-of-thought length scaling</td> <td>Distributional shift: very long traces are OOD; model degrades</td> </tr> </tbody> </table> <p>What is needed is a way to train at a short budget yet <em>extrapolate</em> to arbitrarily long budgets — the model should be able to keep improving as more compute is provided, with no plateau.</p> <p>The paper calls this <strong>extrapolation</strong>: the ability to improve over reasoning horizons more than one order of magnitude longer than those seen during training.</p> <hr/> <h2 id="2-the-key-insight--llms-can-summarize-better-than-they-can-continue">2. The Key Insight — LLMs Can Summarize Better Than They Can Continue</h2> <p>The paper identifies an <strong>asymmetry</strong> in LLM capabilities:</p> <blockquote> <p>A model given a <em>summary</em> of a failed attempt at a problem can produce a better next attempt more easily than it can generate a full solution from scratch.</p> </blockquote> <p>Summarization is an inherently easier task than continuation: the model condenses what happened into a high-level account and then starts fresh with that context. This asymmetry is what makes the RC loop work — each turn is short and in-distribution (≤16K tokens), but each turn is better than the last because it conditions on the compacted outcome of all prior turns.</p> <hr/> <h2 id="3-the-rc-decoding-algorithm">3. The RC-Decoding Algorithm</h2> <p>RC replaces standard autoregressive decoding with an iterative loop:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Turn 1:  problem → [generate trace, ≤16K tokens] → summarize → cache₁
Turn 2:  problem + cache₁ → [generate trace, ≤16K tokens] → summarize → cache₂
Turn 3:  problem + cache₂ → [generate trace, ≤16K tokens] → summarize → cache₃
...
Turn K:  problem + cache_{K-1} → [generate trace, ≤16K tokens] → answer
</code></pre></div></div> <p>At each turn:</p> <ol> <li><strong>Generation step.</strong> The model generates a reasoning trace conditioned on the original problem plus the current summary (empty on turn 1). The trace is capped at the training token budget (16K).</li> <li><strong>Summarization step.</strong> The model summarizes the full trace into a compact textual reasoning cache.</li> <li><strong>Discard.</strong> The full trace is thrown away; only the summary carries forward.</li> </ol> <p>Repeat for K iterations. The effective token budget is K × 16K, but the model’s context at each step is bounded to problem + summary — always in-distribution relative to training.</p> <p>Main hyperparameters in the experiments: K = 8 turns, N_summ = 2 summarization steps, per-turn budget = 16K tokens.</p> <hr/> <h2 id="4-training--two-stages">4. Training — Two Stages</h2> <h3 id="stage-i-base-rc-training">Stage I: Base RC Training</h3> <ul> <li>Train the model (from Qwen3-4B-Instruct or Qwen3-30B-A3B-Instruct) to generate and summarize on Turn 1 (no prior summary in the input).</li> <li>Reward: outcome-based RL via <strong>GRPO</strong> (Group Relative Policy Optimization) — correct/incorrect final answer.</li> <li>Dataset: ~5,700 problems subsampled from <strong>AceReason-Math</strong>.</li> <li>Result: substantial improvement over the base model at moderate test budgets, but limited multi-turn extrapolation without Stage II.</li> </ul> <h3 id="stage-ii-replay-buffer-training">Stage II: Replay Buffer Training</h3> <p>Stage I teaches Turn 1 well but does not teach the model to leverage intermediate summaries (Turns 2–K). Stage II activates a <strong>summary replay buffer</strong> to cover these mid-trajectory states:</p> <ol> <li>During Stage I rollouts, store <code class="language-plaintext highlighter-rouge">(problem, summary)</code> pairs encountered at each RC turn in a replay buffer.</li> <li>At each Stage II training step: sample a <code class="language-plaintext highlighter-rouge">(problem, summary)</code> pair from the buffer — a mid-trajectory starting point.</li> <li>Run RC-decoding for T_train = 3 steps from that starting point.</li> <li>Write the new summaries generated back into the buffer, replacing older summaries for the same problem.</li> <li>Hard problem augmentation: inject difficult problems from the <strong>DAPO</strong> dataset to maintain curriculum difficulty.</li> </ol> <p>The replay buffer is what enables extrapolation: it exposes the model to the full range of mid-trajectory states it will encounter during long-horizon test-time RC-decoding, not just Turn 1.</p> <p><strong>Ablation:</strong> Stage II without the replay buffer yields only ~4% improvement at 192K tokens. With the buffer, the improvement grows to ~9.4% — and continues growing monotonically with budget.</p> <hr/> <h2 id="5-results">5. Results</h2> <h3 id="51-hmmt-nov-2025-math-olympiad">5.1 HMMT-Nov-2025 (Math Olympiad)</h3> <table> <thead> <tr> <th>Model / Setting</th> <th>Token budget</th> <th>Accuracy</th> </tr> </thead> <tbody> <tr> <td>Base Qwen3-4B</td> <td>16K</td> <td>~40%</td> </tr> <tr> <td>RCT-4B (Stage I + II)</td> <td>512K (via RC)</td> <td><strong>~70%</strong></td> </tr> </tbody> </table> <p>+30 percentage points by scaling test-time compute 32× — all from a 4B model trained at 16K tokens.</p> <h3 id="52-imo-answerbench">5.2 IMO-AnswerBench</h3> <table> <thead> <tr> <th>Model</th> <th>Token budget</th> <th>Accuracy</th> </tr> </thead> <tbody> <tr> <td>Base Qwen3-4B</td> <td>16K</td> <td>~34%</td> </tr> <tr> <td>RCT-4B</td> <td>256K (via RC)</td> <td><strong>~50%</strong></td> </tr> <tr> <td>Qwen3-30B-A3B-Instruct</td> <td>(native)</td> <td>lower than RCT-4B at 256K</td> </tr> </tbody> </table> <p><strong>RCT-4B surpasses Qwen3-30B-A3B-Instruct</strong> (a 7.5× larger model) at the 256K-token test budget.</p> <h3 id="53-monotonic-scaling">5.3 Monotonic scaling</h3> <table> <thead> <tr> <th>Model</th> <th>Budget 16K</th> <th>Budget 192K</th> <th>Gain</th> </tr> </thead> <tbody> <tr> <td>RCT-4B</td> <td>baseline</td> <td>+17%</td> <td>monotonic</td> </tr> <tr> <td>RCT-30B</td> <td>baseline</td> <td>+12%</td> <td>monotonic</td> </tr> </tbody> </table> <p>No plateau visible in the measured range. Both models improve continuously as test-time budget grows — the defining property of extrapolation.</p> <h3 id="54-stage-ii-replay-buffer-ablation">5.4 Stage II replay buffer ablation</h3> <table> <thead> <tr> <th>Method</th> <th>Accuracy at 16K</th> <th>Accuracy at 192K</th> <th>Δ</th> </tr> </thead> <tbody> <tr> <td>Stage I only</td> <td>—</td> <td>—</td> <td>baseline</td> </tr> <tr> <td>Stage II without buffer</td> <td>+2.7%</td> <td>+4%</td> <td>small</td> </tr> <tr> <td>Stage II with buffer</td> <td>+2.7%</td> <td>+9.4%</td> <td>grows with budget</td> </tr> </tbody> </table> <p>The replay buffer matters more as the budget grows — exactly the pattern expected if it is enabling multi-turn coordination.</p> <hr/> <h2 id="6-relationship-to-test-time-scaling">6. Relationship to Test-Time Scaling</h2> <p>RC fits into the broader test-time compute literature as a specific instantiation of <strong>iterative self-improvement</strong>:</p> <table> <thead> <tr> <th>Method</th> <th>How it uses extra compute</th> </tr> </thead> <tbody> <tr> <td>Best-of-N</td> <td>Independent samples; no learning across attempts</td> </tr> <tr> <td>Self-Consistency</td> <td>Majority vote over samples; same ceiling</td> </tr> <tr> <td>MCTS-based (e.g., MCTD)</td> <td>Tree search over continuations; structured branching</td> </tr> <tr> <td><strong>RC</strong></td> <td>Iterative summarize-and-restart; bounded context per turn</td> </tr> </tbody> </table> <p>RC’s distinctive property: it trains the model <em>to use the loop</em>, not just to be used in a loop. The replay buffer ensures the model has learned to leverage mid-trajectory summaries, so the quality of each turn increases with the number of prior turns.</p> <hr/> <h2 id="7-why-it-matters">7. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>Training at short horizons, deploying at long horizons.</strong> The ability to extrapolate test-time compute by more than 10× beyond the training horizon — without any architectural change, just an iterative decoding loop — is a practically important capability. Most deployed reasoning models are trained at fixed budgets; RC is a recipe for exploiting more inference compute without retraining.</li> <li><strong>Replay buffer as the key engineering insight.</strong> The Stage I → Stage II split, and specifically the need for a replay buffer to cover mid-trajectory states, is not obvious. Stage II without the buffer provides only marginal gains. This is a concrete implementation lesson for anyone trying to train multi-turn iterative reasoners.</li> <li><strong>Summarization as a computational primitive.</strong> RC’s approach identifies summarization as an <em>enabling primitive</em> for long-horizon reasoning — not an approximation or a context-saving trick, but the core mechanism that lets short training generalize to long deployment.</li> </ol> <hr/> <h2 id="8-limitations-worth-knowing">8. Limitations Worth Knowing</h2> <ul> <li><strong>Training horizon ceiling.</strong> Reasoning traces during training are capped at 16K tokens per turn. The model learns to use summaries to reason across turns but is never trained to generate intra-turn traces longer than 16K.</li> <li><strong>Summarization quality dependency.</strong> A summary that omits a critical insight or introduces an error propagates that error into all future turns with no mechanism for recovery.</li> <li><strong>Error accumulation.</strong> Unlike a single long trace (where later tokens can in principle revise earlier reasoning), an error baked into a summary is a persistent bias in all subsequent turns.</li> <li><strong>Domain scope.</strong> Experiments focus on mathematics and science olympiad problems. Generalization to code, multi-step agent tasks, or open-ended reasoning is not evaluated.</li> <li><strong>Inference latency.</strong> K = 8 sequential model calls per problem increases wall-clock latency. Production deployment would need optimizations not addressed in the paper.</li> </ul> <hr/> <h2 id="9-the-takeaway-for-a-first-reader">9. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>RC trains at 16K tokens but deploys at any multiple of 16K</strong> via an iterative loop: generate a trace → summarize it into a compact cache → discard the trace → start the next turn conditioned on the cache. The model’s context per turn is always ≤16K and always in-distribution.</li> <li><strong>A summary replay buffer (Stage II)</strong> stores <code class="language-plaintext highlighter-rouge">(problem, summary)</code> mid-trajectory pairs and resamples them during RL training, teaching the model to leverage intermediate states — not just Turn 1. Without the buffer, multi-turn extrapolation fails to emerge.</li> <li><strong>RCT-4B</strong> (trained at 16K tokens) reaches <strong>~70% on HMMT-Nov-2025</strong> at a 512K test budget (+30 pp over the base model), and <strong>surpasses Qwen3-30B-A3B-Instruct on IMO-AnswerBench</strong> at 256K tokens — showing monotonic improvement across the full measured budget range.</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Wu, I.Y.H., Qu, Y., Setlur, A., &amp; Kumar, A. (2026). <em>Reasoning Cache: Continual Improvement Over Long Horizons via Short-Horizon RL.</em> ICLR 2026. <a href="https://arxiv.org/abs/2602.03773">arXiv:2602.03773</a>.</li> <li>Code: <a href="https://github.com/IanYHWu/rc">https://github.com/IanYHWu/rc</a></li> <li>Related on this site: <a href="/log/blog/2026/mctd/">MCTD review</a> — inference-time scaling via MCTS inside the diffusion process, a complementary approach to test-time compute scaling; <a href="/log/blog/2026/rlhf-to-ruler/">RLHF → RULER trend note</a> — the RL post-training context this work sits within.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="reasoning"/><category term="rl"/><category term="test-time-scaling"/><category term="long-horizon"/><category term="math"/><summary type="html"><![CDATA[Paper review of Reasoning Cache / RC (Wu, Qu et al., ICLR 2026, CMU) — an iterative decode-and-summarize algorithm that lets LLMs trained on short token budgets extrapolate to reasoning horizons more than 10× longer at test time. A summary replay buffer trains the model to condition effectively on intermediate states. RCT-4B (trained at 16K tokens) reaches ~70% on HMMT-Nov-2025 and surpasses Qwen3-30B-A3B on IMO-AnswerBench at 256K-token test budget.]]></summary></entry><entry><title type="html">Context-Folding — Scaling Long-Horizon LLM Agents via Branch-and-Fold</title><link href="https://yjkim-stat.github.io/log/blog/2026/context-folding/" rel="alternate" type="text/html" title="Context-Folding — Scaling Long-Horizon LLM Agents via Branch-and-Fold"/><published>2026-07-19T01:00:00+00:00</published><updated>2026-07-19T01:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/context-folding</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/context-folding/"><![CDATA[<p><strong>Paper.</strong> Weiwei Sun, Miao Lu, Zhan Ling, Kang Liu, Xuesong Yao, Yiming Yang, Jiecao Chen. <em>Scaling Long-Horizon LLM Agent via Context-Folding.</em> ICML 2026. ByteDance Seed · Carnegie Mellon University (LTI). <a href="https://arxiv.org/abs/2510.11967">[arXiv]</a> · <a href="https://context-folding.github.io/">[project page]</a> · <a href="https://github.com/sunnweiwei/FoldAgent">[code]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>LLM agents solving long-horizon tasks — deep web research, large codebase editing — accumulate interaction history linearly with task length. The dominant ReAct paradigm concatenates everything into a single ever-growing context: observations, tool outputs, reasoning traces, all of it. This produces two compounding failures: the token budget runs out before the task completes, and performance degrades as the model loses focus in a long context (“context rot”). Context-Folding proposes a different primitive: the agent can <strong>branch</strong> into a sub-trajectory to handle a localized subtask, and <strong>fold</strong> it upon completion — collapsing all intermediate steps into a concise <code class="language-plaintext highlighter-rouge">return(message)</code> summary that rejoins the main context thread. To make this learnable, <strong>FoldGRPO</strong> augments standard GRPO with two dense token-level process rewards: an Unfolded Token Penalty that fires whenever the main context grows too large, and an Out-of-Scope Penalty for branches that drift off-task. With a <strong>32K active context budget</strong>, the Folding Agent achieves <strong>62.0% on BrowseComp-Plus</strong> and <strong>58.0% on SWE-Bench Verified</strong> — outperforming a 327K-token ReAct baseline by +20.0% and +8.8% respectively, at <strong>10× smaller active context</strong>.</p> <hr/> <h2 id="1-the-problem--context-as-the-ceiling-on-long-horizon-agents">1. The Problem — Context as the Ceiling on Long-Horizon Agents</h2> <p>The ReAct loop (Reason + Act + Observe) works well for short tasks. For long-horizon tasks the problem is structural:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Step 1: [reasoning] [action] [observation]
Step 2: [reasoning] [action] [observation]
...
Step N: [reasoning] [action] [observation]
        ← all of the above still in context
</code></pre></div></div> <p>At every step the model must fit the entire task history into the context window. Two consequences:</p> <ul> <li><strong>Context exhaustion.</strong> Token budgets cap around 32K–128K tokens in practice. Hard research or engineering tasks easily exceed this before resolution.</li> <li><strong>Context rot.</strong> Even within the window, signal-to-noise degrades: an observation from step 3 that was crucial is competing with sixty later observations for the model’s attention.</li> </ul> <p>Standard remedies are passive and lossy: truncation drops the oldest content (losing critical early state), and summarization compresses at fixed intervals but cannot be end-to-end trained and introduces systematic information loss.</p> <p>The question Context-Folding asks: <strong>what if the agent could actively decide what to forget and when?</strong></p> <hr/> <h2 id="2-the-move--branch-and-fold">2. The Move — Branch and Fold</h2> <p>Context-Folding introduces two new primitive actions into the agent’s action space:</p> <p><strong><code class="language-plaintext highlighter-rouge">branch(description, prompt)</code></strong> — The agent declares a subtask description and enters a fresh sub-trajectory to handle it. From the main context’s perspective, <code class="language-plaintext highlighter-rouge">branch(...)</code> is a single action. The sub-trajectory has its own growing context, but it is isolated from the main thread.</p> <p><strong><code class="language-plaintext highlighter-rouge">return(message)</code></strong> — Called within a sub-trajectory once the subtask is resolved. <code class="language-plaintext highlighter-rouge">message</code> is a concise self-written summary of what was accomplished. Calling <code class="language-plaintext highlighter-rouge">return(...)</code> <strong>folds</strong> the entire sub-trajectory: all intermediate steps disappear, and only the summary is appended back to the main context thread.</p> <p>The analogy to programming is direct: <code class="language-plaintext highlighter-rouge">branch</code> is like calling a function, <code class="language-plaintext highlighter-rouge">return</code> is like returning a value. The internals of the function call are hidden from the caller; only the return value enters the main context.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Main context:
  [problem]
  [action]
  [branch("search for X", "find three sources on X")]
    Sub-trajectory:
      [search query 1] [results 1]
      [search query 2] [results 2]
      [search query 3] [results 3]
      [return("Found: source A (URL), source B (URL), source C (URL)")]
    ← Sub-trajectory discarded
  [Summary: "Found: source A, B, C"]
  [next action based on summary]
  ...
</code></pre></div></div> <p>The main context remains compact because all multi-step operations (iterative web searches, long file reads, tool-heavy exploration) are offloaded to branches. The agent is allowed up to 10 branches per task, and the main thread is capped at 32K active tokens. Total tokens <em>processed</em> across branches can exceed 100K; the agent cycles through them without the main thread ever growing past 32K.</p> <hr/> <h2 id="3-foldgrpo--teaching-the-agent-when-and-how-to-fold">3. FoldGRPO — Teaching the Agent When and How to Fold</h2> <p>The branching primitive is easy to define but hard to learn. Vanilla RL with standard GRPO is insufficient: the agent fails to develop reliable policies for <em>when</em> to branch and <em>what</em> to include in the <code class="language-plaintext highlighter-rouge">return(message)</code> summary.</p> <p>FoldGRPO makes three modifications to standard GRPO:</p> <h3 id="31-dynamic-folded-context-during-training">3.1 Dynamic Folded Context During Training</h3> <p>During RL rollouts, the training environment applies context folding dynamically in real time. The model sees exactly the same folded context structure it will see at inference — not the full unrolled trajectory. This closes the train/inference distribution gap that would otherwise exist if training on full-context trajectories but deploying with folding.</p> <h3 id="32-unfolded-token-penalty-utp">3.2 Unfolded Token Penalty (UTP)</h3> <p>When the total context length of the main thread exceeds 50% of the working context limit, every token in the main thread (except tokens in branch-issuing turns) receives a process reward of $Q_{i,t} = -1$.</p> <p>This creates a direct gradient signal against performing token-intensive operations (long searches, large file reads) directly in the main context. The agent learns to route these into branches instead.</p> <h3 id="33-out-of-scope-penalty-osp">3.3 Out-of-Scope Penalty (OSP)</h3> <p>For each branch, an LLM judge (GPT-5-nano) evaluates whether the agent conducted actions outside the declared subtask scope. Out-of-scope behavior incurs $Q_{i,t} = -0.2$ on all tokens in that branch. This teaches the agent to maintain disciplined focus within a branch.</p> <p>The final reward signal combines the standard sparse task-completion reward (correct/incorrect) with UTP and OSP as dense process rewards operating at the token level. Even on long trajectories where the sparse reward provides no gradient for hundreds of steps, UTP and OSP keep the learning signal flowing.</p> <hr/> <h2 id="4-results">4. Results</h2> <h3 id="41-browsecomp-plus-deep-research">4.1 BrowseComp-Plus (deep research)</h3> <table> <thead> <tr> <th>Method</th> <th>Active context</th> <th>pass@1</th> </tr> </thead> <tbody> <tr> <td>ReAct (long context)</td> <td>327K tokens</td> <td>~42%</td> </tr> <tr> <td>ReAct (short, 32K)</td> <td>32K tokens</td> <td>much lower</td> </tr> <tr> <td>Summarization-based</td> <td>32K effective</td> <td>lower</td> </tr> <tr> <td>Folding Agent + GRPO</td> <td>32K active</td> <td>56.7%</td> </tr> <tr> <td><strong>Folding Agent + FoldGRPO</strong></td> <td><strong>32K active</strong></td> <td><strong>62.0%</strong></td> </tr> </tbody> </table> <p>FoldGRPO over vanilla GRPO: <strong>+5.3 pp</strong>. FoldGRPO over 327K ReAct baseline: <strong>+20.0 pp</strong> with <strong>10× smaller active context</strong>.</p> <h3 id="42-swe-bench-verified-software-engineering">4.2 SWE-Bench Verified (software engineering)</h3> <table> <thead> <tr> <th>Method</th> <th>Active context</th> <th>pass@1</th> </tr> </thead> <tbody> <tr> <td>ReAct (327K long context)</td> <td>327K tokens</td> <td>~49.2%</td> </tr> <tr> <td><strong>Folding Agent + FoldGRPO</strong></td> <td><strong>32K active</strong></td> <td><strong>58.0%</strong></td> </tr> </tbody> </table> <p>FoldGRPO over 327K ReAct baseline: <strong>+8.8 pp</strong>.</p> <h3 id="43-context-efficiency">4.3 Context efficiency</h3> <ul> <li>Folding Agent main trajectory: <strong>~8K tokens average</strong></li> <li>Total tokens processed across all branches: <strong>&gt;100K</strong></li> <li>Compression rate on main context: <strong>&gt;90%</strong></li> </ul> <p>The agent processes substantially more information than a 32K ReAct baseline while keeping the active context a fraction of the size.</p> <h3 id="44-foldgrpo-vs-grpo-ablation">4.4 FoldGRPO vs. GRPO ablation</h3> <p>Removing UTP: main thread fails to stay compressed; context overflows more often; performance drops. Removing OSP: branches drift off-task; scope accuracy degrades. Together UTP + OSP are each independently necessary.</p> <hr/> <h2 id="5-why-it-matters">5. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>Active context management as a learnable skill.</strong> Prior work treated context management as infrastructure (truncation, compression, longer windows). Context-Folding treats it as agent behavior — something the model must learn to do and that can be improved by RL. The branch-and-fold primitive is minimal but expressive.</li> <li><strong>Dense process rewards unlock long-horizon RL.</strong> The sparse task-completion signal in long-horizon tasks is too weak to drive learning. UTP and OSP provide token-level gradient signal at every step — not just at task completion. This is the same lesson MCTD (<a href="/log/blog/2026/mctd/">review</a>) applies in the planning domain: structured intermediate feedback is what enables scaling with compute.</li> <li><strong>Architecture-separable.</strong> FoldGRPO trains the policy to use folding; the backbone model is otherwise unchanged. This means the approach transfers across backbone updates: as better base models appear, FoldGRPO can retrain folding behavior on top without redesigning the system.</li> </ol> <hr/> <h2 id="6-limitations-worth-knowing">6. Limitations Worth Knowing</h2> <ul> <li><strong>Cold-start dependency.</strong> FoldGRPO needs the base model to spontaneously generate at least some valid folding trajectories to bootstrap contrastive gradients. Very specialized domains with near-zero base policy success are problematic.</li> <li><strong>LLM judge dependency.</strong> OSP uses GPT-5-nano as an external judge during training — a proprietary API dependency that could propagate judge errors.</li> <li><strong>Hard tasks remain hard.</strong> On the hardest BrowseComp-Plus subset, accuracy is only 4%. Folding solves the context management problem, not the hard reasoning problem.</li> <li><strong>Summary fidelity unverified.</strong> A poorly written <code class="language-plaintext highlighter-rouge">return(message)</code> silently loses information. The agent has no mechanism to detect or recover from bad summaries.</li> <li><strong>Single-level branching.</strong> Recursive folding (branches within branches) is not implemented, limiting expressiveness on tasks requiring multi-level decomposition.</li> </ul> <hr/> <h2 id="7-the-takeaway-for-a-first-reader">7. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Long-horizon agents fail because their context grows without bound.</strong> Context-Folding gives agents two new actions: <strong><code class="language-plaintext highlighter-rouge">branch()</code></strong> to offload a subtask into an isolated sub-trajectory, and <strong><code class="language-plaintext highlighter-rouge">return(message)</code></strong> to fold it back as a summary. The main context stays ≤32K active tokens even as the agent processes &gt;100K tokens total.</li> <li><strong>FoldGRPO trains this behavior end-to-end</strong> via GRPO with two dense process rewards: an <strong>Unfolded Token Penalty</strong> (gradient against bloating the main context) and an <strong>Out-of-Scope Penalty</strong> (gradient against drifting off-task in branches). These provide token-level signal at every step, not just task completion.</li> <li><strong>62.0% on BrowseComp-Plus and 58.0% on SWE-Bench Verified</strong> with 32K active context — <strong>+20.0% and +8.8% over a 327K-token ReAct baseline at 10× smaller context</strong>.</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Sun, W., Lu, M., Ling, Z., <em>et al.</em> (2025/2026). <em>Scaling Long-Horizon LLM Agent via Context-Folding.</em> ICML 2026. <a href="https://arxiv.org/abs/2510.11967">arXiv:2510.11967</a>.</li> <li>Project page: <a href="https://context-folding.github.io/">https://context-folding.github.io/</a></li> <li>Code: <a href="https://github.com/sunnweiwei/FoldAgent">https://github.com/sunnweiwei/FoldAgent</a></li> <li>Follow-ups: AgentFold (<a href="https://arxiv.org/abs/2510.24699">arXiv:2510.24699</a>), FoldAct (<a href="https://arxiv.org/abs/2512.22733">arXiv:2512.22733</a>).</li> <li>Related on this site: <a href="/log/blog/2026/memento-context-management/">MEMENTO</a> — context compaction via KV-cache compression, a complementary approach.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="agents"/><category term="context-management"/><category term="rl"/><category term="long-horizon"/><category term="llm"/><summary type="html"><![CDATA[Paper review of Context-Folding (Sun, Lu et al., ICML 2026, ByteDance Seed / CMU) — a framework that lets LLM agents actively manage their own working context by branching into sub-trajectories and folding them away once complete. FoldGRPO adds token-level process rewards (Unfolded Token Penalty + Out-of-Scope Penalty) to teach agents when and how to fold. Result: 10× smaller active context, +20% on BrowseComp-Plus, +8.8% on SWE-Bench Verified vs. 327K-token ReAct baselines.]]></summary></entry><entry><title type="html">T3S — Training-Trajectory-Aware Token Selection for Continual Reasoning Distillation</title><link href="https://yjkim-stat.github.io/log/blog/2026/t3s/" rel="alternate" type="text/html" title="T3S — Training-Trajectory-Aware Token Selection for Continual Reasoning Distillation"/><published>2026-07-19T00:00:00+00:00</published><updated>2026-07-19T00:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/t3s</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/t3s/"><![CDATA[<p><strong>Paper.</strong> Zhanming Shen, Jiaqi Hu, Zeyu Qin, Hao Chen, Wentao Ye, Zenan Huang, Yihong Zhuang, Guoshan Lu, Junlin Zhou, Junbo Zhao. <em>Training-Trajectory-Aware Token Selection.</em> ICML 2026. Zhejiang University · Ant Group (Inclusion AI). <a href="https://arxiv.org/abs/2601.10348">[arXiv]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>When you fine-tune a strong student model (say Qwen3-8B) on chain-of-thought traces from a frontier teacher (DeepSeek-R1, QwQ), something counterintuitive happens: training loss falls monotonically, yet <em>all</em> evaluation metrics simultaneously collapse to a shared nadir before slowly recovering. The paper calls this the <strong>Imitation Bottleneck</strong>, caused by <strong>Imitation Shock</strong>. The root mechanism is a token-level bifurcation: surface pattern tokens (formatting, common phrases, boilerplate) anchor the gradient early and starve the harder reasoning tokens of learning signal. T3S proposes a fix that is simple to state but nontrivial to discover: <strong>mask the already-learned anchor tokens from the autoregressive loss, using a trajectory-aware confidence-shift signal to identify them.</strong> With fewer than 1,000 training examples, the resulting Qwen3-8B exceeds its DeepSeek-R1 teacher on both AIME24 (80.63% vs. 79.8%) and AIME25 (73.96% vs. 70.0%), and the same principle extends to diffusion language models (LLaDA-2.0-Mini, state-of-the-art among 16B no-think models).</p> <hr/> <h2 id="1-the-problem--imitation-shock">1. The Problem — Imitation Shock</h2> <p>The standard recipe for continual reasoning distillation: sample chains-of-thought from a frontier teacher, supervised-fine-tune the student on those chains, monitor the benchmark.</p> <p>The problem: on almost every setting the authors tested — across teacher models (DeepSeek-R1, QwQ), student models, dataset scales, and domains — there is a reproducible failure mode. Training loss decreases without exception. But all downstream metrics (AIME24, AIME25, MMLU-Pro, training-set accuracy) drop sharply at the same training step, hit a shared low point (the <em>Imitation Bottleneck</em>), and only slowly recover.</p> <p>This pattern is not a random glitch. It is the same across runs. The question is why a <em>decreasing</em> loss produces <em>degrading</em> benchmarks, and why the degradation and recovery follow such a consistent shape.</p> <hr/> <h2 id="2-the-discovery--token-confidence-bifurcation">2. The Discovery — Token Confidence Bifurcation</h2> <p>T3S isolates the failure at the token level. Define per-token confidence as the log-probability the current model assigns to the ground-truth token:</p> \[c_t(\theta; x, y) = \log p_\theta(y_t \mid y_{&lt;t}, x)\] <p>The <strong>confidence shift</strong> between a training checkpoint $\theta_b$ and the initial model $\theta_0$ is:</p> \[\Delta c_t(x, y) = c_t(\theta_b;\, x, y) - c_t(\theta_0;\, x, y)\] <p>Tracking $\Delta c_t$ across training reveals a clean bifurcation:</p> <table> <thead> <tr> <th>Token class</th> <th>$\Delta c$ trajectory</th> <th>What they encode</th> </tr> </thead> <tbody> <tr> <td><strong>Imitation-Anchor Tokens</strong></td> <td>Rises steeply, early</td> <td>Formatting, common phrases, surface patterns</td> </tr> <tr> <td><strong>Yet-to-learn Tokens</strong></td> <td>Stays low or drops</td> <td>Deep reasoning steps, problem-specific logic</td> </tr> </tbody> </table> <p>Anchor tokens dominate the gradient early in training. Because the autoregressive loss treats all tokens equally, the optimizer devotes most of its capacity to tokens that are already easy — <strong>suppressing the gradient signal for the harder reasoning tokens until after the bottleneck is passed</strong>.</p> <p>The critical empirical finding: <strong>static signals cannot separate these two populations.</strong> Using initial confidence alone (one time point) or local gradient geometry (one step) fails to identify which tokens are anchors. Only the <em>change</em> in confidence over a training window — the trajectory — reveals the bifurcation. This is what makes T3S methodologically distinct from all prior token-selection methods.</p> <hr/> <h2 id="3-the-method--t3s">3. The Method — T3S</h2> <p>T3S reconstructs the training objective at the token level:</p> <ol> <li><strong>Compute $\Delta c_t$</strong> for every token in the batch by comparing the current checkpoint to the initial model.</li> <li><strong>Identify Imitation-Anchor Tokens</strong>: those whose $\Delta c_t$ exceeds a threshold, meaning the model has already learned them and they dominate the gradient.</li> <li><strong>Mask anchor tokens</strong> from the autoregressive loss — they contribute zero gradient for this step.</li> <li><strong>Compute the loss only over yet-to-learn tokens</strong>, giving them the full gradient signal without anchor competition.</li> </ol> <p>The intervention is at the loss layer only. No change to the model architecture, the optimizer, or the sampling procedure for teacher traces.</p> <h3 id="31-why-trajectory-not-static">3.1 Why trajectory, not static?</h3> <p>The comparison to prior methods is direct:</p> <table> <thead> <tr> <th>Method</th> <th>Signal used</th> <th>Signal type</th> </tr> </thead> <tbody> <tr> <td>Rho-1</td> <td>Excess loss vs. reference model</td> <td>Static (single time point)</td> </tr> <tr> <td>TokenCleaning / ssToken</td> <td>Token-level difficulty heuristics</td> <td>Static</td> </tr> <tr> <td><strong>T3S</strong></td> <td>Confidence <em>shift</em> $\Delta c_t$ over training</td> <td>Dynamic trajectory</td> </tr> </tbody> </table> <p>Ablation confirms that substituting the trajectory signal with either static alternative loses most of the gain. The two token classes only become distinguishable <em>because of</em> how their confidence evolves differently over time.</p> <h3 id="32-extension-to-diffusion-language-models">3.2 Extension to Diffusion Language Models</h3> <p>The same principle transfers to masked diffusion training. For LLaDA-2.0-Mini (a diffusion LLM), anchor tokens are those whose mask-prediction confidence has shifted most across training. T3S masks those from the diffusion loss. The paper treats this as a direct validation that the trajectory-awareness principle is architecture-agnostic.</p> <hr/> <h2 id="4-results">4. Results</h2> <h3 id="41-super-teacher-performance-on-aime">4.1 Super-teacher performance on AIME</h3> <p>With <strong>200–1,000 training examples</strong> (far below typical fine-tuning regimes):</p> <table> <thead> <tr> <th>Model</th> <th>Benchmark</th> <th>Standard SFT</th> <th>T3S</th> <th>Teacher (DeepSeek-R1)</th> </tr> </thead> <tbody> <tr> <td>Qwen3-8B</td> <td>AIME24</td> <td>degrades</td> <td><strong>80.63%</strong></td> <td>79.8%</td> </tr> <tr> <td>Qwen3-8B</td> <td>AIME25</td> <td>degrades</td> <td><strong>73.96%</strong></td> <td>70.0%</td> </tr> </tbody> </table> <p>Both T3S numbers are averages over 16 runs. The student <em>exceeds</em> the teacher — an outcome that standard SFT cannot reach even with more data because Imitation Shock degrades the benchmark score before recovery.</p> <p>Qwen3-32B with T3S approaches the performance of Qwen3-235B, the model with roughly 7× more parameters.</p> <h3 id="42-consistency-across-settings">4.2 Consistency across settings</h3> <p>T3S achieves the best performance across all four combinations of training split (BOBA-200, S1K-200) and teacher model (DeepSeek-R1, QwQ), outperforming standard SFT and RRT (the parameter-level counterpart that avoids Imitation Shock by rolling back the optimizer to a pre-bottleneck state).</p> <h3 id="43-ood-and-forgetting">4.3 OOD and forgetting</h3> <p>On out-of-distribution reasoning benchmarks (MATH500, TheoremQA, AMC) and knowledge benchmarks (MMLU-Pro):</p> <ul> <li>T3S <strong>consistently improves</strong> OOD reasoning.</li> <li>T3S <strong>mitigates catastrophic forgetting</strong> of non-reasoning capabilities, relative to standard SFT.</li> </ul> <p>The interpretation: by reducing anchor-token dominance, T3S prevents the model from overfitting to the teacher’s surface style, leaving non-reasoning parameters less disrupted.</p> <h3 id="44-dllm-results">4.4 dLLM results</h3> <p>On LLaDA-2.0-Mini:</p> <ul> <li>T3S dramatically improves reasoning accuracy.</li> <li>Outperforms autoregressive fine-tuning on the same model class.</li> <li>Achieves <strong>state-of-the-art among 16B-scale no-think models</strong>.</li> <li>Generates <strong>15–25% fewer tokens</strong> at inference — because the model no longer overfits to surface-pattern verbosity from the teacher’s long chains.</li> </ul> <hr/> <h2 id="5-comparison-to-related-work">5. Comparison to Related Work</h2> <h3 id="rho-1-and-static-token-selectors">Rho-1 and static token selectors</h3> <p>Rho-1 (arXiv:2404.07965) uses a reference model trained on clean data to score excess per-token loss, selecting only high-loss tokens for training. It works well on LLaMA-family models but has been shown to perform poorly — sometimes worse than full SFT — on Qwen-family models. T3S outperforms Rho-1 by using the <em>shift</em> in confidence rather than its absolute level, which is a more reliable signal when the student already has strong priors.</p> <h3 id="rrt-parameter-level-intervention">RRT (parameter-level intervention)</h3> <p>RRT addresses Imitation Shock at the optimizer level: it detects the onset of the bottleneck and rolls back to the pre-bottleneck checkpoint, then continues. T3S addresses the same phenomenon at the token level without any rollback or checkpoint management. The two methods are complementary in principle; in practice T3S outperforms RRT consistently.</p> <h3 id="alphatoken--tip">AlphaToken / TIP</h3> <p>These assign token-level credit in RL-based post-training from reward model feedback. T3S operates in imitation distillation (no reward model), using the training trajectory itself as the credit signal.</p> <hr/> <h2 id="6-why-it-matters">6. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>It names and explains a pervasive failure mode.</strong> Imitation Shock has surely been encountered by anyone doing continual distillation — most practitioners attribute it to bad hyperparameters or insufficient data. T3S identifies the mechanistic cause at the token level and shows it is structural, not accidental.</li> <li><strong>Super-teacher performance from tiny data.</strong> The fact that a student can exceed its teacher on AIME24/25 with under 1,000 training examples — by <em>masking</em> some of the teacher’s output during training — reframes how we should think about data efficiency in distillation. The bottleneck is not how much teacher data you have; it is whether you are learning from the right tokens within that data.</li> <li><strong>Architecture-agnostic principle.</strong> Applying the same confidence-shift idea to diffusion LMs (LLaDA-2.0-Mini) with SOTA results suggests the token bifurcation phenomenon is not specific to autoregressive models. The trajectory signal may be a broadly applicable tool for any iterative training process over token-level objectives.</li> </ol> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>Token categories drift.</strong> The partition into anchor vs. yet-to-learn is not stable across training. A token that is “yet-to-learn” at step 100 may become an anchor by step 500. The current T3S formulation uses a fixed-threshold partition based on a trajectory snapshot, which does not adapt to this ongoing migration.</li> <li><strong>Static mask mismatch.</strong> Holding the mask fixed against a dynamic learning process introduces a mismatch that can create instability later in training.</li> <li><strong>No dynamic scheduling.</strong> The authors call out the natural future direction: treating token selection as an <strong>optimal control problem</strong> — designing time-dependent priority schedules that continuously re-rank tokens as training evolves. This would close the gap between the static T3S filter and the dynamic reality of learning.</li> <li><strong>Threshold sensitivity.</strong> The confidence-shift threshold that separates anchor from yet-to-learn tokens is a hyperparameter. The paper validates it across settings but does not provide a principled procedure for setting it without a validation set.</li> </ul> <hr/> <h2 id="8-the-takeaway-for-a-first-reader">8. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Continual reasoning distillation suffers from Imitation Shock</strong>: all metrics collapse to a shared bottleneck despite decreasing loss, because surface-pattern <em>Imitation-Anchor Tokens</em> dominate the gradient and starve harder reasoning tokens. This is structural, not accidental.</li> <li><strong>T3S fixes it by masking anchor tokens</strong> from the AR loss, identified via a <strong>trajectory-aware confidence-shift signal</strong> $\Delta c_t$ — the change in per-token log-probability from the initial to the current checkpoint. Static signals fail; only the trajectory distinguishes the two token classes.</li> <li><strong>With under 1,000 training examples, T3S-distilled Qwen3-8B exceeds its DeepSeek-R1 teacher on AIME24 (80.63% vs. 79.8%) and AIME25 (73.96% vs. 70.0%)</strong>. The same principle extends to diffusion LMs, achieving SOTA among 16B no-think models with 15–25% inference token reduction.</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Shen, Z., Hu, J., Qin, Z., <em>et al.</em> (2026). <em>Training-Trajectory-Aware Token Selection.</em> ICML 2026. <a href="https://arxiv.org/abs/2601.10348">arXiv:2601.10348</a>.</li> <li>Liu, Z., <em>et al.</em> (2024). <em>Rho-1: Not All Tokens Are What You Need.</em> <a href="https://arxiv.org/abs/2404.07965">arXiv:2404.07965</a>.</li> <li>Related on this site: <a href="/log/blog/2026/rlhf-to-ruler/">RLHF → RULER trend note</a> — the RL post-training context this distillation work sits within.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="distillation"/><category term="reasoning"/><category term="llm"/><category term="token-selection"/><category term="training"/><summary type="html"><![CDATA[Paper review of T3S (Shen, Hu et al., ICML 2026) — a token-level fix for "Imitation Shock," the failure mode where continual reasoning distillation causes all metrics to collapse simultaneously despite decreasing loss. T3S masks already-learned Imitation-Anchor Tokens from the AR loss using a trajectory-aware confidence-shift signal, enabling Qwen3-8B to exceed its DeepSeek-R1 teacher on AIME24/25 with under 1,000 training examples.]]></summary></entry><entry><title type="html">BG-MCTS — Budget-Guided Tree Search for Fixed Token Budgets in LLM Reasoning</title><link href="https://yjkim-stat.github.io/log/blog/2026/bg-mcts/" rel="alternate" type="text/html" title="BG-MCTS — Budget-Guided Tree Search for Fixed Token Budgets in LLM Reasoning"/><published>2026-07-18T23:00:00+00:00</published><updated>2026-07-18T23:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/bg-mcts</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/bg-mcts/"><![CDATA[<p><strong>Paper.</strong> Sora Miyamoto, Daisuke Oba, Naoaki Okazaki. <em>Aligning Tree-Search Policies with Fixed Token Budgets in Test-Time Scaling of LLMs.</em> ICML 2026. Institute of Science Tokyo. <a href="https://arxiv.org/abs/2602.09574">[arXiv]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Test-time scaling via tree search requires an exploration strategy, but most MCTS formulations for LLM reasoning ignore a critical practical constraint: <strong>the total token budget is fixed</strong>. Standard UCT-based MCTS explores aggressively throughout, even when it is about to run out of budget — generating partial expansions that never reach a complete answer and wasting tokens. <strong>Budget-Guided MCTS (BG-MCTS)</strong> solves this with a single scalar: the <strong>budget ratio</strong> ρ = 1 − C_used / B (remaining fraction of the budget). As ρ decreases, three behaviors automatically adjust: exploration annealing (less UCT bonus → more exploitation), completion bias (longer completions → higher probability of reaching a final answer before budget runs out), and widen annealing (harder to open new branches → favor depth over breadth). The net effect is a <strong>wide-to-deep schedule</strong>: explore broadly early, exploit and complete deeply late. BG-MCTS is <strong>training-free</strong> — it sits atop any frozen LLM + process reward model. On MATH500 Level-5 problems at a 10K-token budget, BG-MCTS achieves <strong>0.662</strong> vs. beam search’s <strong>0.328</strong> — more than double — with consistent gains on AIME24/25 and UGPhysics across Llama and Qwen backbone models.</p> <hr/> <h2 id="1-the-problem--budget-blind-tree-search">1. The Problem — Budget-Blind Tree Search</h2> <p>Tree search scales inference compute by exploring multiple reasoning paths and selecting the best via a process reward model (PRM). MCTS with UCT is the standard approach:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>UCT score = Q(v) + c · √(ln N(parent) / N(v))
</code></pre></div></div> <p>where Q(v) is the estimated value, N(v) is the visit count, and c is the exploration constant. UCT does not know or care about the remaining token budget.</p> <p>This causes a systematic failure under fixed budget constraints:</p> <table> <thead> <tr> <th>MCTS phase</th> <th>Budget-blind behavior</th> <th>Effect</th> </tr> </thead> <tbody> <tr> <td>Early</td> <td>Explores broadly (many nodes, small N(v))</td> <td>Good — covers search space</td> </tr> <tr> <td>Late</td> <td>Continues exploring broadly</td> <td>Bad — opens new branches that cannot be completed before budget runs out</td> </tr> <tr> <td>End</td> <td>Runs out of budget mid-trajectory</td> <td>Wasted tokens, no final answer from unfinished paths</td> </tr> </tbody> </table> <p>The cost is high: unfinished branches produce no answer, meaning budget spent on them yields zero reward. A good tree search policy under a fixed budget should <strong>front-load exploration and back-load exploitation and completion</strong>.</p> <p>No prior LLM reasoning tree search method addresses this directly. BG-MCTS is the first training-free formulation that makes budget awareness a first-class component of the search policy.</p> <hr/> <h2 id="2-the-budget-ratio--one-number-three-behaviors">2. The Budget Ratio — One Number, Three Behaviors</h2> <p>BG-MCTS introduces a single quantity:</p> \[\rho = 1 - \frac{C_{\text{used}}}{B}\] <p>where $B$ is the total output token budget and $C_{\text{used}}$ is the number of output tokens spent so far. ρ ∈ [1, 0]: starts at 1 (full budget remaining), hits 0 when the budget is exhausted.</p> <p>This ratio drives three coupled modifications to the standard BG-PUCT (budget-guided PUCT, the practical UCT variant):</p> <h3 id="21-exploration-annealing">2.1 Exploration Annealing</h3> <p>The UCT exploration bonus is scaled by ρ:</p> \[\text{UCT term} = c \cdot \rho \cdot \sqrt{\frac{\ln N(\text{parent})}{N(v)}}\] <p>When ρ is high (budget plentiful), the exploration bonus is near its full value — the search explores widely. As ρ decreases, the exploration bonus shrinks, and the search increasingly exploits high-value nodes. At ρ ≈ 0, the search is nearly greedy.</p> <p>This is automatic curriculum annealing: no hyperparameter schedule is needed. The budget ratio provides the signal.</p> <h3 id="22-completion-bias">2.2 Completion Bias</h3> <p>When sampling a continuation from a node, BG-MCTS biases the length of that continuation toward completing the reasoning trace. Specifically, a completion bias term proportional to (1 − ρ) is applied — as the budget depletes, the sampled continuations become longer and more likely to reach a final answer.</p> <p>The mechanism: at high ρ, short partial continuations are acceptable (there is budget to expand later). At low ρ, partially expanded traces are likely to be abandoned, so the policy should try to complete them now.</p> <h3 id="23-widen-annealing">2.3 Widen Annealing</h3> <p>MCTS “widening” refers to opening a new child node (a new reasoning branch). In standard MCTS, widening is relatively unconstrained. BG-MCTS gates widening by ρ: when ρ is high, new branches are easy to open; when ρ is low, widening is suppressed.</p> <p>The effect is the defining behavior of BG-MCTS: a <strong>wide-to-deep schedule</strong> emerges automatically without any manual parameter tuning.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Early in budget (ρ ≈ 1):   Wide exploration — many branches opened
Late in budget (ρ ≈ 0):    Deep exploitation — existing branches deepened to completion
</code></pre></div></div> <hr/> <h2 id="3-why-the-three-components-work-together">3. Why the Three Components Work Together</h2> <p>The three modifications address three distinct failure modes of standard MCTS under a fixed budget:</p> <table> <thead> <tr> <th>Failure mode</th> <th>Addressed by</th> </tr> </thead> <tbody> <tr> <td>Over-explores late, misses completions</td> <td>Exploration annealing</td> </tr> <tr> <td>Opens branches that never finish</td> <td>Widen annealing</td> </tr> <tr> <td>Leaves promising branches incomplete</td> <td>Completion bias</td> </tr> </tbody> </table> <p>Each component is independently necessary: the ablation results confirm that removing any one of the three degrades performance. Together they implement a coherent policy: <em>explore cheap paths early, lock in value late</em>.</p> <hr/> <h2 id="4-results">4. Results</h2> <h3 id="41-math500-level-5-hardest-math-problems">4.1 MATH500 Level-5 (Hardest Math Problems)</h3> <p>At a 10K-token budget:</p> <table> <thead> <tr> <th>Method</th> <th>Accuracy</th> </tr> </thead> <tbody> <tr> <td>Beam search</td> <td>0.328</td> </tr> <tr> <td>Standard MCTS</td> <td>lower or similar to beam</td> </tr> <tr> <td><strong>BG-MCTS</strong></td> <td><strong>0.662</strong></td> </tr> </tbody> </table> <p>More than <strong>2× improvement</strong> over beam search on hard math problems — entirely training-free, using the same frozen LLM and PRM.</p> <h3 id="42-competition-math-aime2425-and-science-ugphysics">4.2 Competition Math (AIME24/25) and Science (UGPhysics)</h3> <p>BG-MCTS consistently outperforms beam search across:</p> <ul> <li>AIME 2024 and AIME 2025 competition problems</li> <li>UGPhysics (undergraduate-level physics)</li> </ul> <p>Gains are consistent across backbone models:</p> <ul> <li>Llama-3.1-8B-Instruct</li> <li>Qwen2.5-7B-Instruct</li> <li>Qwen3-32B</li> </ul> <p>This suggests BG-MCTS’s benefit is not specific to a particular model family or reasoning style.</p> <h3 id="43-ablation-results">4.3 Ablation Results</h3> <table> <thead> <tr> <th>Configuration</th> <th>Performance</th> </tr> </thead> <tbody> <tr> <td>Full BG-MCTS (all three components)</td> <td>Best</td> </tr> <tr> <td>Remove exploration annealing</td> <td>Drops</td> </tr> <tr> <td>Remove completion bias</td> <td>Drops</td> </tr> <tr> <td>Remove widen annealing</td> <td>Drops</td> </tr> </tbody> </table> <p>All three components are individually necessary; removing any one degrades performance on at least one benchmark. No component is redundant.</p> <hr/> <h2 id="5-relationship-to-prior-test-time-scaling-methods">5. Relationship to Prior Test-Time Scaling Methods</h2> <p>BG-MCTS sits within the inference-time compute scaling literature but occupies a distinct niche:</p> <table> <thead> <tr> <th>Method</th> <th>Core mechanism</th> <th>Budget awareness</th> </tr> </thead> <tbody> <tr> <td>Best-of-N / SC</td> <td>Parallel sampling + aggregation</td> <td>None</td> </tr> <tr> <td>Beam search</td> <td>Greedy tree expansion</td> <td>None</td> </tr> <tr> <td>Standard MCTS</td> <td>UCT exploration + PRM scoring</td> <td>None</td> </tr> <tr> <td>MCTS + PUCT</td> <td>Improved exploration bonus</td> <td>None</td> </tr> <tr> <td><strong>BG-MCTS</strong></td> <td>UCT + budget ratio ρ (3 components)</td> <td>Yes — training-free</td> </tr> </tbody> </table> <p>PRA (<a href="/log/blog/2026/pra/">review</a>) and Reasoning Cache (<a href="/log/blog/2026/reasoning-cache/">review</a>) also improve reasoning under fixed compute budgets, but via trained components (reward agents, replay buffers). BG-MCTS adds value through inference-time policy adjustment alone — no training required.</p> <hr/> <h2 id="6-why-it-matters">6. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>Budget-awareness closes the gap between MCTS theory and practical deployment.</strong> In practice, LLM inference has a hard cost ceiling — you cannot run MCTS until convergence. BG-MCTS is the first tree search formulation that treats the budget constraint as a first-class design element, not an afterthought.</li> <li><strong>Training-free means drop-in compatibility.</strong> Any existing LLM + PRM stack can adopt BG-MCTS without any model modification or additional training data. The budget ratio is computable from token counters already present in any inference system.</li> <li><strong>The wide-to-deep scheduling principle is general.</strong> The insight that exploration should decrease and completion should increase as budget depletes is not specific to LLM reasoning — it likely applies to any tree search operating under a token or compute budget.</li> </ol> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>Output-only budget tracking.</strong> ρ counts output tokens only. Input tokens from retrieved documents, long system prompts, or tool outputs are not included. For tasks with variable-length context (RAG, tool use), the true budget consumption is underestimated.</li> <li><strong>PRM dependency.</strong> BG-MCTS requires a process reward model to score intermediate steps. PRM quality is a ceiling on BG-MCTS performance — a poorly calibrated PRM can cause the search to exploit bad paths.</li> <li><strong>Fixed-budget assumption.</strong> The budget ratio ρ is only meaningful when the total budget B is fixed in advance. Adaptive budgeting (allocate more budget to harder problems) is not addressed.</li> <li><strong>No wall-clock latency analysis.</strong> The paper reports token counts, not wall-clock time. Sequential MCTS expansion may have higher latency than parallel best-of-N at the same token budget.</li> </ul> <hr/> <h2 id="8-the-takeaway-for-a-first-reader">8. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>BG-MCTS adds a budget ratio ρ = 1 − C_used/B to standard MCTS</strong>, using it to drive three automatic adaptations: <strong>exploration annealing</strong> (less UCT bonus as budget depletes), <strong>completion bias</strong> (longer completions late in the budget), and <strong>widen annealing</strong> (fewer new branches late). Together these produce a <strong>wide-to-deep schedule</strong> — no training, no manual schedule required.</li> <li><strong>All three components are necessary.</strong> Ablations confirm that removing any single component reduces performance — the three modifications address distinct failure modes of budget-blind MCTS.</li> <li><strong>On MATH500 Level-5 at 10K tokens, BG-MCTS scores 0.662 vs. beam search’s 0.328</strong> — more than 2× improvement. Gains hold across AIME, UGPhysics, and three backbone model families (Llama-3.1-8B, Qwen2.5-7B, Qwen3-32B).</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Miyamoto, S., Oba, D., &amp; Okazaki, N. (2026). <em>Aligning Tree-Search Policies with Fixed Token Budgets in Test-Time Scaling of LLMs.</em> ICML 2026. <a href="https://arxiv.org/abs/2602.09574">arXiv:2602.09574</a>.</li> <li>Related on this site: <a href="/log/blog/2026/mctd/">MCTD review</a> — MCTS embedded in the diffusion denoising process, a complementary use of tree search for sequential generation; <a href="/log/blog/2026/reasoning-cache/">Reasoning Cache</a> — iterative summarize-and-restart as an alternative extrapolation strategy under fixed budgets; <a href="/log/blog/2026/pra/">PRA</a> — online process reward signals within beam search for knowledge-intensive reasoning.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="reasoning"/><category term="mcts"/><category term="test-time-scaling"/><category term="math"/><category term="llm"/><summary type="html"><![CDATA[Paper review of BG-MCTS (Miyamoto, Oba, Okazaki, ICML 2026, Institute of Science Tokyo) — a training-free MCTS variant that adapts exploration strategy in real time based on how much of the token budget remains. A budget ratio ρ governs exploration annealing, completion bias, and widen annealing, producing a wide-to-deep schedule that outperforms beam search by 2× on MATH500 Level-5 hard problems at a 10K-token budget.]]></summary></entry><entry><title type="html">Blend-ASC — Optimal Self-Consistency via Power-Law Sample Efficiency</title><link href="https://yjkim-stat.github.io/log/blog/2026/blend-asc/" rel="alternate" type="text/html" title="Blend-ASC — Optimal Self-Consistency via Power-Law Sample Efficiency"/><published>2026-07-18T22:00:00+00:00</published><updated>2026-07-18T22:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/blend-asc</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/blend-asc/"><![CDATA[<p><strong>Paper.</strong> Austin Feng, Marius Alonso, Ambroise Odonnat, Vasilii Feofanov, Ievgen Redko. <em>Optimal Self-Consistency for Efficient Reasoning with Large Language Models.</em> ICML 2026. Yale University · Huawei Noah’s Ark Lab. <a href="https://arxiv.org/abs/2511.12309">[arXiv]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Self-consistency (SC) — sample K answers, take the majority vote — is the dominant inference-time reasoning enhancement for LLMs. But SC is theoretically underspecified: <strong>how many samples do you actually need?</strong> Prior work answers this with heuristics. Blend-ASC grounds it in theory. The key result is a <strong>power-law characterization of SC error decay</strong>: Error(n) ∝ n^{−β}, where β depends on the <em>margin</em> Δ = p₁ − p₂ (the gap between the most and second-most probable answers). From this, the paper proves that two existing adaptive methods occupy complementary regions: <strong>ASC</strong> (adaptive self-consistency) is sample-efficient early but suboptimal at scale; <strong>PPR-1v1</strong> (pairwise polling race) is asymptotically optimal but pessimistic and slow to activate. <strong>Blend-ASC</strong> interpolates between them using the sampling ratio t/T as the mixing weight — pure ASC early, pure PPR-1v1 late — requiring <strong>no hyperparameters</strong> and achieving <strong>6.8× fewer samples</strong> than vanilla SC while remaining theoretically near-optimal throughout.</p> <hr/> <h2 id="1-self-consistency--the-setup">1. Self-Consistency — The Setup</h2> <p><strong>Self-consistency</strong> (Wang et al., 2022) generates K independent chain-of-thought samples from a prompt and returns the empirical mode:</p> \[\hat{y} = \arg\max_{y} \; |\{i : y_i = y\}|\] <p>This works because the majority of independently sampled correct reasoning paths converge on the same final answer, even when individual paths are wrong.</p> <p>The sample budget K is typically a fixed constant (e.g., 40). This is wasteful: some questions are easy and confident (5 samples suffice), while others are hard (even 40 samples may not be enough). An <em>adaptive</em> strategy that stops when confidence is high enough would reduce expected sample count substantially.</p> <p>The open question: <strong>what is the optimal stopping criterion, and how does the error decay with n?</strong></p> <hr/> <h2 id="2-the-power-law-error-decay--the-theoretical-core">2. The Power-Law Error Decay — The Theoretical Core</h2> <h3 id="21-the-margin-δ">2.1 The margin Δ</h3> <p>For a question with answer distribution p₁ ≥ p₂ ≥ …, define the <strong>margin</strong>:</p> \[\Delta = p_1 - p_2\] <p>Δ captures the “difficulty” of the majority-vote problem: large Δ means the correct answer dominates (easy), small Δ means the top two answers are nearly tied (hard).</p> <h3 id="22-power-law-decay">2.2 Power-law decay</h3> <p>Blend-ASC establishes (for the first time comprehensively) that SC error decays as:</p> \[\text{Error}(n) \propto n^{-\beta(\Delta)}\] <p>where β(Δ) is a function of the margin. This is the <strong>first rigorous characterization</strong> of the SC sample complexity curve — prior work bounded error with exponential Chernoff-style arguments, but those bounds are loose for the n values relevant in practice (n = 10–100).</p> <p>The power law has two implications:</p> <ol> <li>Error decreases predictably with n — you can estimate when you are “done enough.”</li> <li>β depends on Δ — the same sample budget yields very different error rates for easy vs. hard questions.</li> </ol> <hr/> <h2 id="3-two-prior-adaptive-methods--complementary-weaknesses">3. Two Prior Adaptive Methods — Complementary Weaknesses</h2> <p>The paper analyzes two existing adaptive SC methods against the power-law theory:</p> <h3 id="31-asc-adaptive-self-consistency">3.1 ASC (Adaptive Self-Consistency)</h3> <p>ASC monitors the current empirical distribution and stops when confidence exceeds a threshold. It is <strong>sample-efficient early</strong>: for easy questions (large Δ), it stops quickly and is correct.</p> <p>Weakness: ASC’s stopping criterion is suboptimal asymptotically. For hard questions with small Δ, ASC misidentifies early noise as a confident signal and may terminate too early.</p> <h3 id="32-ppr-1v1-pairwise-polling-race">3.2 PPR-1v1 (Pairwise Polling Race)</h3> <p>PPR-1v1 runs a statistical test on the top two answers, stopping when the leading answer has significantly more votes than the second. This is <strong>asymptotically optimal</strong>: given enough samples, it consistently identifies the correct plurality answer.</p> <p>Weakness: PPR-1v1 is pessimistic early. For easy questions, it keeps collecting samples long after the right answer is obvious, because the statistical test is conservative.</p> <table> <thead> <tr> <th>Method</th> <th>Early regime (easy Qs)</th> <th>Late regime (hard Qs)</th> </tr> </thead> <tbody> <tr> <td>ASC</td> <td>Efficient — stops early</td> <td>Suboptimal — may stop too early</td> </tr> <tr> <td>PPR-1v1</td> <td>Pessimistic — collects too many</td> <td>Asymptotically optimal</td> </tr> <tr> <td><strong>Blend-ASC</strong></td> <td>Inherits ASC efficiency</td> <td>Inherits PPR-1v1 optimality</td> </tr> </tbody> </table> <hr/> <h2 id="4-blend-asc--the-method">4. Blend-ASC — The Method</h2> <p>Blend-ASC interpolates between ASC and PPR-1v1 based on the sampling progress t/T, where t is the current sample count and T is the maximum budget:</p> \[\text{Blend-ASC stopping criterion} = (1 - t/T) \cdot R_1(\text{ASC}) + (t/T) \cdot R_2(\text{PPR-1v1})\] <p>Early in sampling (t/T ≈ 0): the criterion is dominated by ASC — efficient early stopping for easy questions.</p> <p>Late in sampling (t/T ≈ 1): the criterion converges to PPR-1v1 — asymptotically optimal for hard questions.</p> <h3 id="key-properties">Key properties</h3> <ul> <li><strong>Hyperparameter-free.</strong> The interpolation weight is determined entirely by t/T — no tuning required.</li> <li><strong>Monotone interpolation.</strong> As sampling progresses, the stopping criterion smoothly transitions from aggressive (ASC) to conservative (PPR-1v1).</li> <li><strong>No architecture change.</strong> Blend-ASC operates on the output distribution of any LLM, without access to internal states or logits.</li> </ul> <hr/> <h2 id="5-results">5. Results</h2> <h3 id="51-sample-efficiency-main-result">5.1 Sample efficiency (main result)</h3> <table> <thead> <tr> <th>Method</th> <th>Average samples vs. vanilla SC</th> </tr> </thead> <tbody> <tr> <td>Vanilla SC (K=40)</td> <td>40 (baseline)</td> </tr> <tr> <td>ASC</td> <td>~8–12</td> </tr> <tr> <td>PPR-1v1</td> <td>~12–20</td> </tr> <tr> <td><strong>Blend-ASC</strong></td> <td><strong>~5.9</strong> (6.8× fewer than SC)</td> </tr> </tbody> </table> <p>Blend-ASC requires the fewest samples on average, while maintaining accuracy at parity with SC.</p> <h3 id="52-accuracy-preservation">5.2 Accuracy preservation</h3> <p>Across all evaluated benchmarks and models, Blend-ASC accuracy is within statistical noise of vanilla SC (K=40): no accuracy loss from the 6.8× sample reduction.</p> <h3 id="53-benchmarks-and-models">5.3 Benchmarks and models</h3> <p>Evaluated on:</p> <ul> <li><strong>MMLU</strong> (general knowledge)</li> <li><strong>MATH</strong> (competition mathematics)</li> <li><strong>GPQA-Diamond</strong> (graduate-level “Google-proof” expert questions)</li> </ul> <p>Across five LLM families:</p> <ul> <li>Llama-3.1-8B</li> <li>Llama-3.2-3B</li> <li>Mistral-7B</li> <li>Qwen-2.5-7B</li> <li>Qwen-2.5-32B</li> </ul> <p>Gains are consistent across all combinations, suggesting the power-law analysis is model-agnostic.</p> <hr/> <h2 id="6-relationship-to-the-inference-time-scaling-literature">6. Relationship to the Inference-Time Scaling Literature</h2> <p>Blend-ASC sits at the efficient end of the inference-time compute spectrum:</p> <table> <thead> <tr> <th>Method</th> <th>What it scales</th> <th>Primary cost</th> </tr> </thead> <tbody> <tr> <td>Self-Consistency</td> <td>More samples per question</td> <td>Linear in K</td> </tr> <tr> <td>Best-of-N + PRM</td> <td>More samples + scoring</td> <td>PRM compute</td> </tr> <tr> <td>MCTS-based (BG-MCTS)</td> <td>Tree search</td> <td>Sequential</td> </tr> <tr> <td><strong>Blend-ASC</strong></td> <td>Minimal samples per question</td> <td>Sub-linear in K</td> </tr> </tbody> </table> <p>Blend-ASC is not a replacement for tree search or process reward models on hard tasks — it is an efficiency tool for the majority of questions where SC is already effective. For hard questions (small Δ), Blend-ASC still requires many samples, and at that point more powerful methods (MCTS, PRM-guided search) become appropriate.</p> <hr/> <h2 id="7-why-it-matters">7. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>The power-law analysis provides theoretical grounding where SC was purely empirical.</strong> Most practitioners tune K by eyeballing accuracy curves. Blend-ASC provides a first-principles framework: error decay rate is a function of margin, and the optimal stopping policy follows from that.</li> <li><strong>6.8× sample reduction with no accuracy loss is a directly deployable gain.</strong> Inference budget for SC scales linearly with K. A 6.8× reduction means the same accuracy at 1/6.8 the cost, or 6.8× more questions served at the same budget.</li> <li><strong>The interpolation insight is broadly applicable.</strong> The idea of interpolating between an early-efficient and a late-optimal criterion based on progress t/T is general — it applies to any sequential decision problem where different stopping rules are optimal at different stages.</li> </ol> <hr/> <h2 id="8-limitations-worth-knowing">8. Limitations Worth Knowing</h2> <ul> <li><strong>SC as the base assumption.</strong> Blend-ASC optimizes the number of SC samples but does not change what SC can achieve at its ceiling. If the LLM’s answer mode is incorrect (the most common answer is wrong), Blend-ASC inherits this failure.</li> <li><strong>Margin Δ is unobserved.</strong> The power-law theory depends on the true Δ, but in practice Δ is estimated from n samples. For hard questions (small true Δ), the empirical estimate may be noisy.</li> <li><strong>Discrete answer assumption.</strong> The pairwise polling analysis assumes a discrete answer space (multiple choice or extractable final answers). Open-ended generation with continuous answer spaces requires an answer-canonicalization step that introduces additional error.</li> <li><strong>Benchmark scope.</strong> Evaluations focus on QA-style benchmarks with extractable final answers. Extension to generation tasks (code, text) is not evaluated.</li> </ul> <hr/> <h2 id="9-the-takeaway-for-a-first-reader">9. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>SC error decays as a power law n^{-β(Δ)}</strong>, where Δ = p₁ − p₂ is the margin between the top two answers. This is the first comprehensive derivation — prior bounds were exponential and too loose for practical n values.</li> <li><strong>ASC and PPR-1v1 have complementary weaknesses:</strong> ASC is efficient early but suboptimal on hard Qs; PPR-1v1 is asymptotically optimal but pessimistic early. <strong>Blend-ASC interpolates</strong> between them with mixing weight t/T — no hyperparameters, inheriting the best of both.</li> <li><strong>6.8× fewer samples than vanilla SC</strong> (K=40 → ~6) with no accuracy loss, consistently across MMLU, MATH, GPQA-Diamond, and five LLM families.</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Feng, A., Alonso, M., Odonnat, A., Feofanov, V., &amp; Redko, I. (2026). <em>Optimal Self-Consistency for Efficient Reasoning with Large Language Models.</em> ICML 2026. Also NeurIPS 2025 Workshop on Efficient Reasoning. <a href="https://arxiv.org/abs/2511.12309">arXiv:2511.12309</a>.</li> <li>Wang, X., <em>et al.</em> (2022). Self-consistency improves chain-of-thought reasoning in language models. NeurIPS 2022.</li> <li>Related on this site: <a href="/log/blog/2026/bg-mcts/">BG-MCTS</a> — budget-guided tree search, a complementary approach when SC’s ceiling is insufficient; <a href="/log/blog/2026/tele-lens/">Tele-Lens</a> — probing when hidden states encode confidence, which could inform margin estimation for adaptive SC.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="reasoning"/><category term="self-consistency"/><category term="inference"/><category term="sample-efficiency"/><category term="theory"/><summary type="html"><![CDATA[Paper review of Blend-ASC (Feng, Alonso et al., ICML 2026, Yale / Huawei Noah's Ark Lab) — a theoretically grounded adaptive self-consistency method that achieves 6.8× sample efficiency over vanilla SC by deriving the first comprehensive power-law error decay analysis and interpolating between ASC and PPR-1v1 based on sampling progress. Evaluated on MMLU, MATH, and GPQA-Diamond across five LLM families.]]></summary></entry><entry><title type="html">SOL — Self-Optimizing Language Models via Token-Level Efficiency Policies</title><link href="https://yjkim-stat.github.io/log/blog/2026/sol/" rel="alternate" type="text/html" title="SOL — Self-Optimizing Language Models via Token-Level Efficiency Policies"/><published>2026-07-18T21:00:00+00:00</published><updated>2026-07-18T21:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/sol</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/sol/"><![CDATA[<p><strong>Paper.</strong> Yash Akhauri, Mohamed S. Abdelfattah. <em>Compute Where it Counts: Self Optimizing Language Models.</em> ICML 2026. Cornell University. <a href="https://arxiv.org/abs/2605.10875">[arXiv]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>LLMs allocate compute uniformly: every token gets the same attention computation, MLP depth, and numerical precision. This is efficient in hardware but not in reasoning — some tokens encode high-value information (“however”, entity names, key numerical quantities) while others are low-value filler. <strong>Self-Optimizing Language Models (SOL)</strong> train a lightweight <strong>policy network</strong> to read the frozen LLM’s hidden state at each token and assign a <strong>discrete efficiency action</strong> (which attention heads to sparsify, which MLP activations to prune, what quantization bit-width to apply). The policy is trained via <strong>GRPO</strong> on counterfactual trajectories: the teacher LLM generates high-quality outputs, and the policy is rewarded for matching that quality under a reduced compute budget. Three axes of optimization — attention sparsity C, MLP activation pruning P, and quantization Q — can be combined into a joint policy SOL-J. The result: <strong>+7.3% MMLU accuracy</strong> over uniform-budget baselines, with a consistently better accuracy-compute Pareto front, across Llama-3.2-3B, Llama-3.1-8B-Instruct, and DeepSeek-R1-Distill-Llama-8B.</p> <hr/> <h2 id="1-the-problem--uniform-compute-is-wasteful">1. The Problem — Uniform Compute Is Wasteful</h2> <p>Every token in a transformer forward pass receives the same computational resources:</p> <ul> <li><strong>Full attention</strong>: O(L²) computation over all context</li> <li><strong>Full MLP depth</strong>: all hidden dimensions activated</li> <li><strong>Full precision</strong>: fp16 or bf16 for all operations</li> </ul> <p>This uniformity is a deliberate hardware efficiency choice, not a semantic one. From the model’s information-processing perspective, however, tokens are not created equal:</p> <table> <thead> <tr> <th>Token type</th> <th>Informational role</th> <th>Optimal compute</th> </tr> </thead> <tbody> <tr> <td>High-value (rare words, key quantities, logical pivots)</td> <td>Updates reasoning state significantly</td> <td>High</td> </tr> <tr> <td>Low-value (common function words, repetition, filler)</td> <td>Contributes little to next prediction</td> <td>Low</td> </tr> </tbody> </table> <p>Standard adaptive compute research addresses this at the model level (layer-skipping, early exit). SOL addresses it at the <strong>token level within a layer</strong> — a finer-grained decomposition — using a trained policy rather than a hand-designed rule.</p> <hr/> <h2 id="2-the-sol-framework--three-axes-one-policy">2. The SOL Framework — Three Axes, One Policy</h2> <h3 id="21-three-efficiency-dimensions">2.1 Three efficiency dimensions</h3> <p>SOL defines three independent axes of per-token efficiency:</p> <p><strong>C — Attention sparsity (κ)</strong> At each token, only κ ∈ {0%, 25%, 50%, 75%} of attention heads are computed. High-value tokens use full attention (κ = 0%); low-value tokens drop up to 75% of heads.</p> <p><strong>P — MLP activation pruning (ρ)</strong> At each token, ρ ∈ {0%, 25%, 50%, 75%} of MLP activations are zeroed before the nonlinearity. Equivalent to a form of dynamic width reduction.</p> <p><strong>Q — Quantization bit-width (η)</strong> At each token, weights and activations are quantized to η ∈ {16, 8, 4, 2} bits. Critical tokens keep full precision; less important tokens are aggressively quantized.</p> <h3 id="22-action-granularity--fl-3l-2l">2.2 Action granularity — FL, 3L, 2L</h3> <p>Actions can be applied at different layer granularities:</p> <table> <thead> <tr> <th>Granularity</th> <th>Resolution</th> <th>Action space size</th> </tr> </thead> <tbody> <tr> <td>2L (2-layer groups)</td> <td>Coarse</td> <td>Lower</td> </tr> <tr> <td>3L (3-layer groups)</td> <td>Medium</td> <td>Medium</td> </tr> <tr> <td>FL (full per-layer)</td> <td>Fine</td> <td>Up to 1560 actions</td> </tr> </tbody> </table> <p>FL is the most expressive but has the largest policy output space. In practice, 3L provides a good accuracy-complexity trade-off.</p> <h3 id="23-policy-network">2.3 Policy network</h3> <p>A lightweight network (much smaller than the LLM) reads the hidden state at each token position and outputs a discrete action over the chosen granularity × dimension combination. The policy is architecture-agnostic: it can be applied to any transformer-based LLM.</p> <h3 id="24-variants">2.4 Variants</h3> <table> <thead> <tr> <th>Variant</th> <th>Axes used</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td>SOL-C</td> <td>C only</td> <td>Attention-sparsity policy</td> </tr> <tr> <td>SOL-P</td> <td>P only</td> <td>MLP-pruning policy</td> </tr> <tr> <td>SOL-Q</td> <td>Q only</td> <td>Quantization policy</td> </tr> <tr> <td>SOL-J</td> <td>C + P + Q</td> <td>Joint policy over all three axes</td> </tr> </tbody> </table> <p>SOL-J achieves the highest gains but also the largest action space.</p> <hr/> <h2 id="3-training--grpo-on-counterfactual-trajectories">3. Training — GRPO on Counterfactual Trajectories</h2> <h3 id="31-the-training-setup">3.1 The training setup</h3> <p>Training the policy requires a reward signal: given a token, which efficiency action should be taken? This is not directly labeled in any dataset.</p> <p>SOL uses <strong>GRPO</strong> (Group Relative Policy Optimization) on <strong>teacher-forced counterfactual trajectories</strong>:</p> <ol> <li><strong>Generate reference trajectories</strong>: the frozen LLM produces high-quality outputs on training tasks.</li> <li><strong>Counterfactual rollouts</strong>: the policy proposes efficiency actions for each token; the model forward pass is run with those actions applied.</li> <li><strong>Reward</strong>: the reward for a trajectory is a combination of (a) log-likelihood match to the reference output and (b) a penalty for total compute used (measured by net keep-rate).</li> <li><strong>GRPO update</strong>: the policy is updated to prefer actions that achieve high log-likelihood match with low compute.</li> </ol> <p>The net keep-rate (proportion of activations/attention heads retained on average) serves as the compute proxy. A policy that maintains high match quality with a low net keep-rate is rewarded.</p> <h3 id="32-why-grpo-over-standard-rl">3.2 Why GRPO over standard RL?</h3> <p>GRPO uses group-relative rewards: within a batch of trajectories for the same input, each trajectory’s reward is normalized by the group’s mean and variance. This stabilizes training when absolute reward magnitudes vary across tasks.</p> <hr/> <h2 id="4-results">4. Results</h2> <h3 id="41-mmlu-accuracy-vs-uniform-baseline">4.1 MMLU accuracy vs. uniform baseline</h3> <table> <thead> <tr> <th>Method</th> <th>MMLU accuracy gain vs. uniform budget</th> </tr> </thead> <tbody> <tr> <td>SOL-C (FL)</td> <td>+5.1%</td> </tr> <tr> <td>SOL-P (FL)</td> <td>+4.8%</td> </tr> <tr> <td>SOL-Q (FL)</td> <td>+4.2%</td> </tr> <tr> <td><strong>SOL-J (3L)</strong></td> <td><strong>+7.3%</strong></td> </tr> </tbody> </table> <p>The joint policy dominates all single-axis variants. The gain is measured at the same net keep-rate (equal compute).</p> <h3 id="42-pareto-frontier-improvement">4.2 Pareto frontier improvement</h3> <p>Across all tested compute levels (net keep-rates from 40% to 100%), SOL-J consistently sits above the uniform baseline on the accuracy vs. compute curve. There is no tested compute regime where uniform allocation beats SOL.</p> <h3 id="43-models-evaluated">4.3 Models evaluated</h3> <ul> <li><strong>Llama-3.2-3B</strong> (instruction tuning)</li> <li><strong>Llama-3.1-8B-Instruct</strong></li> <li><strong>DeepSeek-R1-Distill-Llama-8B</strong> (reasoning model)</li> </ul> <p>Gains are consistent across all three models, including the reasoning-specialized DeepSeek-R1 distillation. This is notable: reasoning models with long CoT chains also benefit from per-token efficiency allocation.</p> <h3 id="44-granularity-ablation">4.4 Granularity ablation</h3> <table> <thead> <tr> <th>Granularity</th> <th>Accuracy</th> <th>Policy complexity</th> </tr> </thead> <tbody> <tr> <td>2L</td> <td>Lower</td> <td>Smallest</td> </tr> <tr> <td>3L</td> <td><strong>Best trade-off</strong></td> <td>Medium</td> </tr> <tr> <td>FL</td> <td>Marginal gains over 3L</td> <td>Highest</td> </tr> </tbody> </table> <p>3L provides nearly all of the accuracy benefit of FL at substantially lower policy complexity.</p> <hr/> <h2 id="5-relationship-to-adaptive-compute-literature">5. Relationship to Adaptive Compute Literature</h2> <p>SOL sits at the intersection of two research directions:</p> <table> <thead> <tr> <th>Direction</th> <th>Examples</th> <th>SOL’s relationship</th> </tr> </thead> <tbody> <tr> <td>Layer-level adaptive depth</td> <td>Early exit, layer skipping</td> <td>SOL is token-level within layers, complementary</td> </tr> <tr> <td>Token-level importance</td> <td>Attention pruning, token merging</td> <td>SOL learns to prune via RL, not hand-designed rules</td> </tr> <tr> <td>Quantization</td> <td>GPTQ, AWQ</td> <td>SOL does per-token quantization, not uniform</td> </tr> <tr> <td>RL for efficiency</td> <td>AdapLeR, DynaBert</td> <td>SOL uses GRPO with counterfactual trajectories</td> </tr> </tbody> </table> <p>The key novelty: prior methods pick one axis and optimize statically (uniform pruning, uniform quantization). SOL jointly optimizes three axes dynamically per token using a learned policy.</p> <hr/> <h2 id="6-why-it-matters">6. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>Token-level compute allocation is the right granularity.</strong> Layer-level early exit discards all computation for a token; token-level SOL keeps useful computation while reducing wasteful computation. The policy learns which aspects of which tokens matter most — a fundamentally richer representation than any static rule.</li> <li><strong>+7.3% on MMLU at equal compute is substantial.</strong> MMLU is a broad, stable benchmark. A gain of this magnitude from a compute reallocation alone — without changing the frozen LLM’s weights — demonstrates that significant latent efficiency is being left on the table by uniform allocation.</li> <li><strong>The policy is backbone-agnostic and lightweight.</strong> Because the policy reads hidden states but does not modify the LLM, it can be trained once and applied to any compatible backbone. As better base models become available, the policy can be retrained on the new backbone without architectural changes.</li> </ol> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>Net keep-rate ≠ wall-clock speedup.</strong> The paper reports compute savings in terms of net keep-rate (proportion of activations retained). Actual wall-clock speedup depends on hardware implementation: sparse attention and quantization gains require specific hardware support (sparse tensor cores, mixed-precision units). No hardware benchmarks are reported.</li> <li><strong>Training cost.</strong> GRPO training on counterfactual trajectories requires running many forward passes with modified efficiency actions. The training overhead relative to the model size is not reported.</li> <li><strong>Reasoning token heterogeneity.</strong> For long CoT chains, the policy must distinguish reasoning tokens (high value) from filler tokens across a long trajectory. Whether the learned policy generalizes well across reasoning styles is not evaluated beyond DeepSeek-R1.</li> <li><strong>Discrete action space.</strong> The current action space (4 levels per axis) is coarse. Finer-grained or continuous efficiency actions are not evaluated.</li> </ul> <hr/> <h2 id="8-the-takeaway-for-a-first-reader">8. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>SOL trains a lightweight policy to assign per-token efficiency actions</strong> (attention sparsity κ, MLP pruning ρ, quantization bit-width η) to a frozen LLM via GRPO on counterfactual trajectories. High-value tokens get more compute; low-value tokens get less — learned, not hand-designed.</li> <li><strong>Three axes can be combined into SOL-J</strong>, a joint policy. Granularity 3L (3-layer groups) provides the best accuracy-complexity trade-off, with an action space of up to 1560 discrete actions at FL granularity.</li> <li><strong>+7.3% MMLU accuracy over uniform-budget baselines</strong> at the same compute, with a consistently better Pareto front across Llama-3.2-3B, Llama-3.1-8B-Instruct, and DeepSeek-R1-Distill-Llama-8B.</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Akhauri, Y., &amp; Abdelfattah, M.S. (2026). <em>Compute Where it Counts: Self Optimizing Language Models.</em> ICML 2026. <a href="https://arxiv.org/abs/2605.10875">arXiv:2605.10875</a>.</li> <li>Related on this site: <a href="/log/blog/2026/reasoning-cache/">Reasoning Cache</a> — iterative summarize-and-restart for budget extrapolation, a complementary approach to efficient long-horizon reasoning; <a href="/log/blog/2026/context-folding/">Context-Folding</a> — adaptive context management as a learnable agent skill, paralleling SOL’s theme of learned efficiency allocation.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="efficiency"/><category term="llm"/><category term="rl"/><category term="inference"/><category term="adaptive-compute"/><summary type="html"><![CDATA[Paper review of SOL (Akhauri, Abdelfattah, ICML 2026, Cornell) — a framework that trains a lightweight policy network (via GRPO) to assign per-token efficiency actions (attention sparsity, MLP pruning, quantization) to a frozen LLM, allocating more compute to important tokens. A joint SOL-J policy achieves +7.3% MMLU accuracy over uniform budget baselines, consistently improving the Pareto front across Llama and DeepSeek-R1 models.]]></summary></entry><entry><title type="html">WFM-TTS — Test-Time Scaling for World Foundation Models</title><link href="https://yjkim-stat.github.io/log/blog/2026/wfm-tts/" rel="alternate" type="text/html" title="WFM-TTS — Test-Time Scaling for World Foundation Models"/><published>2026-07-02T03:00:00+00:00</published><updated>2026-07-02T03:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/wfm-tts</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/wfm-tts/"><![CDATA[<p><strong>Paper.</strong> Wenyan Cong, Hanqing Zhu, Peihao Wang, Bangya Liu, Dejia Xu, Kevin Wang, David Z. Pan, Yan Wang, Zhiwen Fan, Zhangyang Wang. <em>Can Test-Time Scaling Improve World Foundation Model?</em> COLM 2025. UT Austin · UW-Madison · NVIDIA. <a href="https://arxiv.org/abs/2503.24320">[arXiv]</a> · <a href="https://scalingwfm.github.io/">[project page]</a> · <a href="https://github.com/VITA-Group/WFM-TTS">[code]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>World Foundation Models (WFMs) — video-generative models that predict future visual states given past observations and control inputs — have become load-bearing components in Physical AI stacks for autonomous driving and robotics. But they’re expensive to make better: pretraining costs are enormous, and post-training improvements are bottlenecked by curated paired data. This paper asks whether <strong>test-time compute scaling</strong> is a third path — one that has been transformative for LLM reasoning but has never been systematically applied to WFMs. The answer is yes: the authors introduce <strong>WFM-TTS</strong>, the first test-time scaling framework for WFMs. The central engineering challenge is that naive multi-candidate search is impractical over continuous video tokens with a diffusion-based decoder. WFM-TTS solves this with a replacement <strong>efficient tokenizer decoder that is ~9,000× faster</strong> than diffusion decoding, then applies <strong>beam search + top-K pruning</strong> guided by <strong>rule-based physical-quality rewards</strong>. A new <strong>five-dimension evaluation toolkit</strong> (3D consistency, temporal consistency, spatial awareness, perceptual quality, text-video alignment) provides the measurement infrastructure the field was missing. The headline result: a <strong>4B-parameter COSMOS model with WFM-TTS matches or outperforms a 12B-parameter baseline</strong>, and test-time scaling laws hold — more compute monotonically improves output quality.</p> <hr/> <h2 id="1-the-problem--wfms-are-hard-to-scale-easy-to-evaluate-poorly">1. The Problem — WFMs Are Hard to Scale, Easy to Evaluate Poorly</h2> <h3 id="11-why-wfms-matter">1.1 Why WFMs matter</h3> <p>A World Foundation Model takes a sequence of past observations (e.g., camera frames from an autonomous vehicle) and ego-action commands and predicts the next video frames — what the world will look like if the agent does those things. For physical AI:</p> <ul> <li><strong>Autonomous driving:</strong> the vehicle can “imagine” its trajectory before executing it.</li> <li><strong>Robotics:</strong> the robot can simulate proposed motions to check for collisions or physical plausibility before committing.</li> </ul> <p>This is the “planning by imagination” paradigm, and it scales directly with how good the WFM is.</p> <h3 id="12-the-scaling-wall">1.2 The scaling wall</h3> <p>WFMs are trained on massive video datasets with large compute budgets. Making them better has two levers:</p> <table> <thead> <tr> <th>Lever</th> <th>Cost</th> <th>Constraint</th> </tr> </thead> <tbody> <tr> <td>More pretraining data / compute</td> <td>Enormous</td> <td>Diminishing returns; data quality ceiling</td> </tr> <tr> <td>Post-training fine-tuning</td> <td>Moderate</td> <td>Bounded by curated paired (video, action) data</td> </tr> <tr> <td><strong>Test-time compute scaling</strong></td> <td>Per-query</td> <td><strong>Unexplored for WFMs</strong></td> </tr> </tbody> </table> <p>Test-time scaling — the idea behind LLM chain-of-thought and OpenAI o1 — trades inference compute for quality without retraining. It works for LLMs because you can generate multiple candidates and use reward models to pick the best. For WFMs the obvious extension is blocked by two problems: (1) diffusion-based decoders are far too slow for multi-candidate generation, and (2) no standardized reward or evaluation toolkit exists to score candidate videos systematically.</p> <p>WFM-TTS addresses both.</p> <h3 id="13-the-missing-evaluation-infrastructure">1.3 The missing evaluation infrastructure</h3> <p>A secondary problem: there’s no agreed metric for WFM output quality. FID and FVD measure frame quality and video quality respectively, but they don’t capture physical plausibility — whether the generated world is physically consistent in 3D, whether objects maintain their spatial relationships, whether the generated scene is temporally coherent across frames. Without those measures, you can’t run principled reward-guided search, and you can’t compare WFMs systematically.</p> <hr/> <h2 id="2-wfm-tts--the-framework">2. WFM-TTS — The Framework</h2> <p>WFM-TTS has three components: an efficient decoder, a search procedure, and a reward signal. The backbone model is <strong>NVIDIA COSMOS</strong> — an autoregressive WFM that operates over discrete video tokens.</p> <h3 id="21-the-efficient-tokenizer-decoder">2.1 The efficient tokenizer decoder</h3> <p>The standard COSMOS decoding pipeline reconstructs video frames from latent tokens using a diffusion-based decoder. This is accurate but extremely slow — slow enough that generating even a handful of candidate videos for comparison is computationally infeasible in a test-time search loop.</p> <p>WFM-TTS replaces the diffusion decoder with an <strong>efficient tokenizer decoder</strong> that:</p> <ul> <li>Maintains consistent quality trends relative to the diffusion decoder (same ordering of candidates by quality).</li> <li>Runs approximately <strong>9,000× faster</strong>.</li> </ul> <p>This speedup is what unlocks beam search. Without it, test-time scaling for WFMs is a dead end.</p> <h3 id="22-probability-based-top-k-pruning">2.2 Probability-based top-K pruning</h3> <p>At each generation step, the autoregressive WFM produces a distribution over possible next tokens. WFM-TTS generates K candidate continuations and prunes the lowest-scoring ones by token probability. Only the top-K beams survive to the next step.</p> <p>This is the standard beam-search pruning strategy, now made feasible by the fast decoder.</p> <h3 id="23-beam-search">2.3 Beam search</h3> <p>The search runs over the full discrete token space of the WFM:</p> <ol> <li>Expand each surviving beam to K children.</li> <li>Score each child using the efficient decoder + reward function.</li> <li>Keep the top-K children; discard the rest.</li> <li>Repeat until the full video sequence is generated.</li> </ol> <p>The output is the highest-scoring complete video sequence found during search.</p> <h3 id="24-rule-based-rewards">2.4 Rule-based rewards</h3> <p>The reward signal that guides beam selection is deliberately simple: <strong>rule-based physical quality metrics</strong> derived from measurable properties of the generated video. Examples include:</p> <ul> <li>Aesthetic quality score.</li> <li>Object permanence / temporal consistency score.</li> <li>3D geometric consistency (via CUT3R, a 3D reconstruction model).</li> </ul> <p>The paper compares rule-based rewards against learned preference models and finds that <strong>rule-based rewards consistently win</strong> on both stability and alignment with qualitative human preference. Learned rewards introduce noise; rule-based rewards are deterministic and directly tied to measurable physical properties.</p> <hr/> <h2 id="3-the-wfm-evaluation-toolkit">3. The WFM Evaluation Toolkit</h2> <p>Alongside the search framework, WFM-TTS introduces a <strong>five-dimension evaluation toolkit</strong> that serves as both the reward signal during search and the benchmark for comparing WFMs broadly.</p> <table> <thead> <tr> <th>Dimension</th> <th>What it measures</th> <th>How</th> </tr> </thead> <tbody> <tr> <td><strong>3D consistency</strong></td> <td>Geometric coherence of the generated scene</td> <td>CUT3R 3D reconstruction from the video</td> </tr> <tr> <td><strong>Temporal consistency</strong></td> <td>Smooth transitions; no object teleportation</td> <td>Frame-to-frame coherence metrics</td> </tr> <tr> <td><strong>Spatial relationship awareness</strong></td> <td>Correct relative positions of objects</td> <td>Object detection + spatial reasoning</td> </tr> <tr> <td><strong>Perceptual quality</strong></td> <td>Visual fidelity and aesthetics</td> <td>VBench / VideoScore</td> </tr> <tr> <td><strong>Text-video alignment</strong></td> <td>Generated video matches the action conditioning</td> <td>Cross-modal similarity</td> </tr> </tbody> </table> <p>Standard metrics (FID, FVD) are evaluated separately on the <strong>nuScenes</strong> and <strong>Waymo</strong> distributions, covering 900 test input videos (150 nuScenes scenes + 750 Waymo clips from 150 scenes × 5 samples).</p> <p>The toolkit is modular and is released as open-source infrastructure for WFM research — filling the measurement gap that had been blocking principled comparison of WFMs.</p> <hr/> <h2 id="4-results">4. Results</h2> <h3 id="41-test-time-scaling-law-holds-for-wfms">4.1 Test-time scaling law holds for WFMs</h3> <p>The paper’s central empirical finding:</p> <blockquote> <p>Performance on every evaluation metric increases monotonically as test-time compute (number of beam-search iterations, candidate count) increases — even in a compute-optimal (FLOPs-matched) setting.</p> </blockquote> <p>This is the WFM analog of the LLM test-time scaling law. It’s not obvious that it would hold, because WFMs operate over continuous-valued visual predictions rather than discrete language tokens. It does hold.</p> <h3 id="42-4b-matches-12b">4.2 4B matches 12B</h3> <p>The most striking headline:</p> <blockquote> <p><strong>COSMOS-4B + WFM-TTS ≥ COSMOS-12B (baseline, no TTS)</strong></p> </blockquote> <p>A model with 3× fewer parameters, given more inference-time compute via WFM-TTS, matches or outperforms the larger model on overall quality. Human evaluators prefer the WFM-TTS-enhanced outputs.</p> <p>This is the same compute-optimal crossover point seen in LLM scaling — you can substitute inference compute for model capacity, and at some point the substitution is favorable.</p> <h3 id="43-quantitative-gains">4.3 Quantitative gains</h3> <p>Overall improvements of <strong>6–12% across evaluation dimensions</strong> relative to greedy/baseline decoding (no test-time search).</p> <h3 id="44-rule-based--preference-based-rewards">4.4 Rule-based &gt; preference-based rewards</h3> <p>Across all settings, rule-based reward functions outperform learned preference models for beam search guidance. The preference models introduce calibration noise; the rule-based rewards are stable and physically grounded.</p> <hr/> <h2 id="5-the-broader-context">5. The Broader Context</h2> <h3 id="51-connection-to-llm-test-time-scaling">5.1 Connection to LLM test-time scaling</h3> <p>The LLM test-time scaling research established that “more compute at inference = better outputs” when search is <em>structured</em> — not random sampling but principled search with evaluation signals. WFM-TTS is the first paper to extend this to physical world modeling, establishing that the same principle applies across modalities.</p> <p>The key enabling difference: language tokens are cheap to decode; video frames from diffusion are not. The 9,000× speedup in the efficient decoder is therefore the structural prerequisite for the entire framework, the way softmax temperature is the prerequisite for language model sampling.</p> <h3 id="52-relation-to-dreamgen-and-dreamzero">5.2 Relation to DreamGen and DreamZero</h3> <p>In the robotics world-model lineage reviewed on this site (<a href="/log/blog/2026/dreamgen/">DreamGen</a>, <a href="/log/blog/2026/dreamzero/">DreamZero</a>), the WFM is used to generate synthetic trajectories or as a policy backbone. WFM-TTS is upstream of both those use cases: it makes the WFM better at the video-generation task itself, without changing the architecture or training. Better WFM → better synthetic trajectories for DreamGen → better downstream policies. Better WFM → better predictive features for VPP-style backbones.</p> <p>This is the “test-time compute as a lever for WFM quality” argument; the downstream benefits to the robotics pipeline are a corollary.</p> <hr/> <h2 id="6-why-it-matters">6. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>It establishes test-time scaling as a first-class lever for WFMs.</strong> Until this paper, test-time compute scaling was LLM territory. WFM-TTS shows that physical world modeling obeys the same scaling law, provided you solve the decoder-speed bottleneck. The implication: the “more inference compute → better quality” recipe now spans language reasoning and physical simulation.</li> <li><strong>The efficient decoder is the enabling contribution.</strong> A 9,000× speedup that maintains quality ordering is nontrivial engineering. Without it, beam search over WFM token spaces is impractical and the rest of the framework doesn’t exist. This is a reusable component for anyone working with autoregressive tokenized WFMs.</li> <li><strong>The evaluation toolkit fills a genuine gap.</strong> FID and FVD were borrowed from image generation and video generation respectively — they don’t measure physical plausibility. A toolkit that captures 3D consistency, temporal coherence, and spatial relationship awareness is necessary infrastructure for the WFM research community.</li> </ol> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>COSMOS-specific.</strong> All experiments use NVIDIA COSMOS at 4B and 12B scale. Whether the approach extends to diffusion-based WFMs (rather than autoregressive tokenized WFMs) requires different search designs.</li> <li><strong>Autonomous driving focus.</strong> Evaluation datasets are nuScenes and Waymo — driving-centric. Robotics scenarios are mentioned but not quantitatively evaluated. The rewards and toolkit metrics may need domain adaptation for manipulation tasks.</li> <li><strong>Rule-based rewards have a quality ceiling.</strong> They are stable and physically grounded, but they measure specific observable properties. Tasks requiring richer goal-conditioned evaluation (did the agent accomplish a specific behavior?) may need learned rewards anyway.</li> <li><strong>Beam search still multiplies inference cost.</strong> Even with the 9,000× speedup, keeping K beams active multiplies per-query compute by K. At large K, the practical deployment cost is non-trivial. The paper doesn’t fully analyze latency-quality Pareto curves under production constraints.</li> <li><strong>Offline evaluation.</strong> Quality is assessed on fixed test sets from nuScenes/Waymo. Closed-loop driving evaluations — where the generated future feeds back into planning decisions — are not included.</li> </ul> <hr/> <h2 id="8-the-takeaway-for-a-first-reader">8. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>World Foundation Models are expensive to improve by retraining.</strong> WFM-TTS asks whether <strong>test-time compute scaling</strong> is an alternative. It is: test-time scaling laws hold for WFMs (monotonically improving quality with compute) even in a compute-optimal (FLOPs-matched) regime.</li> <li><strong>The key enabling technology is a ~9,000× faster efficient tokenizer decoder</strong> that replaces slow diffusion decoding and makes multi-candidate beam search over WFM token spaces practical. On top of this, <strong>rule-based physical-quality rewards</strong> guide beam selection more stably than learned preference models.</li> <li><strong>The headline result: COSMOS-4B + WFM-TTS ≥ COSMOS-12B (baseline)</strong>, with overall 6–12% quality gains. The companion <strong>five-dimension WFM evaluation toolkit</strong> (3D consistency, temporal consistency, spatial awareness, perceptual quality, text-video alignment) fills a measurement gap in the field.</li> </ol> <p>That’s the arc: WFMs can’t be improved cheaply by retraining → test-time scaling is the unexplored lever → fast decoder makes beam search feasible → rule-based rewards guide search → smaller model beats larger model on physical-quality metrics.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Cong, W., Zhu, H., Wang, P., Liu, B., Xu, D., Wang, K., Pan, D. Z., Wang, Y., Fan, Z., &amp; Wang, Z. (2025). <em>Can Test-Time Scaling Improve World Foundation Model?</em> COLM 2025. <a href="https://arxiv.org/abs/2503.24320">arXiv:2503.24320</a>.</li> <li>Project page: <a href="https://scalingwfm.github.io/">https://scalingwfm.github.io/</a></li> <li>Code: <a href="https://github.com/VITA-Group/WFM-TTS">https://github.com/VITA-Group/WFM-TTS</a></li> <li>COSMOS backbone: NVIDIA COSMOS (Agarwal et al., 2025).</li> <li>CUT3R (3D consistency evaluator): <a href="https://arxiv.org/abs/2501.12369">https://arxiv.org/abs/2501.12369</a>.</li> <li>VBench / VideoScore: video quality evaluation benchmarks.</li> <li>nuScenes / Waymo: autonomous driving video datasets.</li> <li>Related on this site: <a href="/log/blog/2026/dreamgen/">DreamGen paper review</a> — WFM as a data factory for robot trajectory generation; <a href="/log/blog/2026/dreamzero/">DreamZero paper review</a> — WFM as the policy backbone for physical generalization; <a href="/log/blog/2026/vpp/">VPP paper review</a> — WFM internal features as a predictive backbone for robot policies.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="world-models"/><category term="autonomous-driving"/><category term="video-generation"/><category term="inference-time-compute"/><category term="test-time-scaling"/><summary type="html"><![CDATA[Paper review of "Can Test-Time Scaling Improve World Foundation Model?" (Cong, Zhu et al., COLM 2025) — the first framework that applies test-time compute scaling to video-generative world models. A 9,000x-faster efficient decoder enables beam search + top-K pruning over the token space, rule-based rewards guide selection, and a 4B COSMOS model with WFM-TTS matches a 12B baseline. Introduces a new five-dimension WFM evaluation toolkit (3D consistency, temporal coherence, spatial awareness, perceptual quality, text-video alignment).]]></summary></entry><entry><title type="html">Monte Carlo Tree Diffusion — System 2 Planning with Diffusion Models</title><link href="https://yjkim-stat.github.io/log/blog/2026/mctd/" rel="alternate" type="text/html" title="Monte Carlo Tree Diffusion — System 2 Planning with Diffusion Models"/><published>2026-07-02T01:00:00+00:00</published><updated>2026-07-02T01:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/mctd</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/mctd/"><![CDATA[<p><strong>Paper.</strong> Jaesik Yoon, Hyeonseo Cho, Doojin Baek, Yoshua Bengio, Sungjin Ahn. <em>Monte Carlo Tree Diffusion for System 2 Planning.</em> ICML 2025 Spotlight. KAIST · SAP · Mila · NYU. <a href="https://arxiv.org/abs/2502.07202">[arXiv]</a> · <a href="https://sites.google.com/view/mctd-s2planning/home">[project page]</a> · <a href="https://github.com/ahn-ml/mctd">[code]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Diffusion models have emerged as capable offline planners — they can generate whole trajectories toward a goal by running a denoising process over a trajectory representation. But they are fundamentally one-shot samplers: giving a diffusion planner <em>more inference-time compute</em> (more samples, more steps) doesn’t reliably improve outcomes. Monte Carlo Tree Search (MCTS) has the opposite property — it was built to improve with compute by systematically exploring a search tree. <strong>MCTD</strong> bridges the two by reconceptualizing the denoising process as a tree-structured rollout. Partial denoising states become tree nodes; binary guidance schedules (guided vs. unguided sampling) become meta-actions that MCTS selects among; fast DDIM jumps stand in for cheap rollout simulations; and standard UCT backpropagation updates node values after each simulated plan is scored by a reward model. The result is a diffusion planner that can actually use extra compute — performance scales monotonically with the number of tree iterations, and on long-horizon maze tasks where all diffusion baselines collapse, MCTD achieves near-perfect success.</p> <hr/> <h2 id="1-the-problem--diffusion-planners-cant-scale-with-compute">1. The Problem — Diffusion Planners Can’t Scale with Compute</h2> <p>Offline diffusion planners (Diffuser and descendants) are trained on trajectory datasets to denoise random noise into a complete trajectory from current state to goal. This is powerful: you get behavioral diversity for free, planning is just sampling, and the whole thing is tractable to train.</p> <p>The weakness appears on hard long-horizon tasks. There are two obvious ways to give a diffusion planner more inference-time compute:</p> <table> <thead> <tr> <th>Strategy</th> <th>What it does</th> <th>Does it help?</th> </tr> </thead> <tbody> <tr> <td>More denoising steps</td> <td>Finer denoising trajectory</td> <td>Marginal or no gain</td> </tr> <tr> <td>Random search</td> <td>Sample N trajectories, pick best</td> <td>No systematic improvement</td> </tr> <tr> <td>Replanning</td> <td>Re-run the planner periodically</td> <td>Minor; not well-coordinated</td> </tr> </tbody> </table> <p>None of these strategies are <em>adaptive</em> — they don’t use the result of one computation to guide the next. In MCTS terms, they explore blindly rather than accumulating evidence about which branches are promising. For easy tasks this doesn’t matter (the single shot works). For hard tasks — long-horizon navigation through a complex maze — it means performance plateaus at the quality of the first sample.</p> <p>The “System 2” framing in the title is precise: System 1 thinking is fast and intuitive (single-shot diffusion), System 2 is slow and deliberate (MCTS-guided search). Diffusion planners have been stuck in System 1 mode. MCTD adds the System 2 loop.</p> <hr/> <h2 id="2-the-move--instantiate-mcts-inside-the-denoising-process">2. The Move — Instantiate MCTS Inside the Denoising Process</h2> <p>MCTS is defined by four steps: <strong>Select</strong> → <strong>Expand</strong> → <strong>Simulate</strong> → <strong>Backpropagate</strong>. MCTD maps each step onto something the diffusion planner can do natively.</p> <p>The key insight: the trajectory is divided into a sequence of temporally contiguous <strong>subplans</strong>. A partially denoised state — where some subplans have been denoised and others remain noisy — is a natural tree node. Children of a node are different ways to continue denoising (from the same partial state).</p> <hr/> <h2 id="3-the-three-core-mechanisms">3. The Three Core Mechanisms</h2> <h3 id="31-subplan-decomposition--tree-nodes">3.1 Subplan decomposition → tree nodes</h3> <p>Divide the full planned trajectory into $K$ subplan segments in time. Each tree node corresponds to a specific combination of (subplan index, partial denoising state). Deeper nodes have more subplans resolved; leaf nodes have a fully denoised trajectory.</p> <p>This decomposition is what makes tree branching meaningful: you can branch on <em>which future you commit to for the first subplan</em>, hold that fixed, and then explore alternatives for the second subplan. Without decomposition, branching mid-denoising would produce incoherent trajectories.</p> <h3 id="32-guidance-levels--meta-actions">3.2 Guidance levels → meta-actions</h3> <p>At each expansion, MCTD must decide <em>how</em> to continue denoising the next subplan. The available meta-actions are:</p> <ul> <li><strong>Guided (1):</strong> denoise under the goal-conditioned guidance signal → exploitation, pulls toward the known goal.</li> <li><strong>Unguided (0):</strong> denoise from the unconditional prior → exploration, diversifies the candidate futures.</li> </ul> <p>These guidance levels form the action space MCTS selects among. Node selection follows standard <strong>UCT</strong>:</p> \[a^* = \arg\max_a \left[ Q(s, a) + C \sqrt{\frac{\ln N(s)}{N(s,a)}} \right],\] <p>where $Q(s, a)$ is the mean reward of subtrees reached via action $a$ from node $s$, $N(s)$ is the visit count of $s$, and $C$ is the exploration constant. High UCT value = promising branch with remaining uncertainty.</p> <p>The binary guidance meta-action is the minimum useful abstraction: it’s coarse enough for MCTS to reason over efficiently, but it produces meaningfully different trajectories (exploitation vs. exploration) at each step. The full <strong>guidance schedule</strong> — $(g_1, g_2, \ldots, g_K)$ — is what MCTS is really searching over.</p> <h3 id="33-jumpy-denoising--simulation-oracle">3.3 Jumpy denoising → simulation oracle</h3> <p>After expanding a node, MCTD needs to evaluate it — to score “how good is this partial plan likely to be?” Running the full denoising chain to completion is too slow to do at every node. Instead, MCTD uses <strong>DDIM</strong> (which allows arbitrary step-size skips) to jump from the current partial denoising state to a fully denoised trajectory in a fraction of the steps.</p> <p>This “jumpy” simulation is an approximation, but it’s cheap and good enough for backpropagation — the reward model evaluates the fast-simulated complete trajectory and returns a scalar score.</p> <h3 id="34-backpropagation">3.4 Backpropagation</h3> <p>After simulation, the reward is backpropagated up the tree to update $Q(s, a)$ estimates for all ancestor nodes. The guidance schedule along the winning path accumulates positive weight; schedules along low-reward paths are down-weighted. Future selection iterations draw on this accumulated evidence.</p> <p>This is standard MCTS backpropagation — the contribution is that the tree structure and action space are well-defined enough for it to work inside a diffusion process.</p> <hr/> <h2 id="4-what-mctd-actually-buys">4. What MCTD Actually Buys</h2> <p>The argument for the method can be stated in one sentence:</p> <blockquote> <p>Diffusion planners were failing on hard tasks because good plans require coordinated decisions across subplan segments — decisions that can’t be made well in a single shot. MCTD gives the planner a way to <em>try</em> a commitment for the first subplan, <em>evaluate</em> consequences via fast rollout, and <em>revise</em> if the consequences look bad — the same feedback loop MCTS uses for any sequential decision problem.</p> </blockquote> <p>The analogy to chess engines is exact: a single-shot diffusion sample is like playing the first move that looks locally good. MCTD is like running a minimax search to see how the game evolves before committing.</p> <hr/> <h2 id="5-experiments">5. Experiments</h2> <h3 id="51-benchmarks">5.1 Benchmarks</h3> <p>MCTD is evaluated on <strong>OGBench</strong> (Offline Goal-Conditioned RL Benchmark) across:</p> <ul> <li><strong>PointMaze</strong> — navigation for a point-mass robot in mazes: Medium, Large, Giant.</li> <li><strong>AntMaze</strong> — same layouts with a complex ant robot (higher DOF).</li> <li><strong>Visual PointMaze</strong> — same task but with RGB image observations instead of state, testing robustness to partial observability.</li> </ul> <p>The backbone model is <strong>Diffuser</strong> (Janner et al., 2022) — a transformer diffusion model trained offline on trajectory datasets. The MCTS layer is inference-time only; no additional training is needed.</p> <h3 id="52-baselines">5.2 Baselines</h3> <table> <thead> <tr> <th>Baseline</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td>Diffuser</td> <td>Single-shot denoising plan</td> </tr> <tr> <td>Diffuser-Replanning</td> <td>Re-runs Diffuser at fixed intervals</td> </tr> <tr> <td>Diffuser-Random Search</td> <td>Samples N random trajectories, picks best by reward</td> </tr> <tr> <td>Diffusion Forcing</td> <td>Autoregressive sequential diffusion model</td> </tr> </tbody> </table> <p>Diffuser-Random Search is the most important baseline: it uses the same total denoising budget as MCTD but allocates it as parallel random samples rather than a guided tree search. Its performance determines whether the gain is from <em>more compute</em> or <em>better use of compute</em>.</p> <h3 id="53-results">5.3 Results</h3> <p><strong>PointMaze and AntMaze:</strong></p> <p>All standard diffusion baselines (Diffuser, Diffuser-Replanning, Diffusion Forcing) suffer dramatic performance drops as maze size increases from Medium → Large → Giant. Diffuser-Random Search performs no better than single-shot Diffuser, confirming that the issue is not compute quantity but compute quality.</p> <p>MCTD achieves near-perfect success across all maze sizes and robot morphologies. On PointMaze-Giant — the hardest task — MCTD reaches ~100% success while all baselines fail.</p> <p><strong>Inference-time scaling:</strong></p> <p>MCTD success rate increases monotonically with the number of tree iterations. This is the key property: unlike every diffusion baseline, MCTD has a genuine “use more compute → get better plans” lever. This is what the paper means by System 2 planning.</p> <p><strong>Visual PointMaze:</strong></p> <p>MCTD outperforms both Diffuser and Diffusion Forcing on the image-observation variant, showing that the tree-search structure adds robustness even when the input is partial/visual.</p> <h3 id="54-planning-time-cost">5.4 Planning time cost</h3> <p>The compute cost is real. Vanilla MCTD requires 8–40× more wall-clock time than a single-shot Diffuser, depending on maze size. On PointMaze-Giant, this reaches ~264 seconds per plan.</p> <p>This is addressed by a follow-up (<strong>Fast-MCTD</strong>, NeurIPS 2025) that introduces parallel tree rollouts with delayed updates and trajectory coarsening, recovering ~100× speedup while maintaining 100% success. That’s a downstream engineering result, not in this paper, but it shows the approach is practically viable.</p> <hr/> <h2 id="6-the-taxonomy-connection">6. The Taxonomy Connection</h2> <p>MCTD fits into the growing literature on <strong>inference-time scaling for decision-making</strong>:</p> <table> <thead> <tr> <th>Paper/system</th> <th>How it uses extra compute</th> </tr> </thead> <tbody> <tr> <td>Best-of-N sampling</td> <td>Random independent draws, pick max-reward</td> </tr> <tr> <td>Process Reward Models</td> <td>Score intermediate steps, rerank</td> </tr> <tr> <td>Tree-of-Thought / MCTS for LLMs</td> <td>Token-level tree search with language priors</td> </tr> <tr> <td><strong>MCTD</strong></td> <td>Tree search <em>inside</em> the denoising trajectory</td> </tr> </tbody> </table> <p>The distinctive feature of MCTD is that the “tree” is not over language tokens or high-level plans — it’s over <em>partially denoised trajectory states</em>. This is a lower level of abstraction, which makes each node geometrically meaningful and enables the fast-DDIM simulation trick.</p> <hr/> <h2 id="7-why-it-matters">7. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>It breaks the “one-shot” ceiling on diffusion planners.</strong> Until MCTD, running a diffusion planner with more compute didn’t help beyond a point. The inference-time scaling property is a new lever that diffusion-based planning didn’t have.</li> <li><strong>The MCTS-to-diffusion mapping is clean and reusable.</strong> The three mechanisms — subplan nodes, guidance meta-actions, jumpy simulation — are not specific to maze navigation. Any offline diffusion planner with a reward model can potentially apply the same wrapper.</li> <li><strong>It validates the System 2 framing for generative planners.</strong> The pattern — slow deliberate search over a space of possible futures, guided by evaluation of fast cheap simulations — is the same pattern AlphaGo used for Go and LLM tree-of-thought methods use for reasoning. MCTD is the trajectory-generation version of that pattern.</li> </ol> <hr/> <h2 id="8-limitations-worth-knowing">8. Limitations Worth Knowing</h2> <ul> <li><strong>Reward model dependency.</strong> Simulation scoring depends on a pre-trained reward model. If the reward model is inaccurate, backpropagation corrupts the search tree. There’s no mechanism to detect or correct for reward-model errors.</li> <li><strong>Offline training only.</strong> The diffusion backbone is trained on a fixed offline dataset; MCTD does not interact with the environment during search. Distributional shift between the offline data and the actual environment is not addressed.</li> <li><strong>Coarse guidance action space.</strong> Binary guided/unguided is the minimum meaningful action space. More expressive guidance schedules (continuous guidance strength, per-timestep variation) might improve performance but would also make the MCTS branching factor harder to manage.</li> <li><strong>Approximation error in jumpy denoising.</strong> DDIM simulation introduces approximation error relative to full denoising. This is acceptable in practice but can cause mispricing of nodes in subtler tasks.</li> <li><strong>No online replanning integration.</strong> MCTD plans a full trajectory offline before execution begins. There’s no mechanism for closed-loop real-time replanning as the environment evolves (though this is an obvious future extension).</li> </ul> <hr/> <h2 id="9-the-takeaway-for-a-first-reader">9. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Diffusion planners are one-shot samplers.</strong> More compute (random sampling, replanning) doesn’t help because there’s no adaptive feedback loop. MCTD adds one by embedding <strong>MCTS inside the denoising process</strong>: partially denoised subplans are tree nodes, binary guidance schedules are meta-actions, and fast DDIM jumps are the cheap simulation oracle.</li> <li><strong>UCT governs node selection</strong>, backpropagation updates value estimates from simulated plan rewards, and performance scales monotonically with inference-time compute — the defining property of System 2 planning that all diffusion baselines lack.</li> <li><strong>On hard long-horizon maze tasks</strong>, MCTD achieves ~100% success where all diffusion baselines fail. Diffuser-Random Search — the same compute budget, allocated randomly — shows no improvement over single-shot Diffuser, confirming the gain is from structured search, not more samples.</li> </ol> <p>That’s the arc: diffusion planners lack inference-time scaling → MCTS has it by construction → embed MCTS inside denoising via subplan nodes + guidance actions + jumpy simulation → unlock systematic compute scaling for diffusion-based planning.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Yoon, J., Cho, H., Baek, D., Bengio, Y., &amp; Ahn, S. (2025). <em>Monte Carlo Tree Diffusion for System 2 Planning.</em> ICML 2025 Spotlight. <a href="https://arxiv.org/abs/2502.07202">arXiv:2502.07202</a>.</li> <li>Project page: <a href="https://sites.google.com/view/mctd-s2planning/home">https://sites.google.com/view/mctd-s2planning/home</a></li> <li>Code: <a href="https://github.com/ahn-ml/mctd">https://github.com/ahn-ml/mctd</a></li> <li>Janner, M., Du, J., Tenenbaum, J., &amp; Levine, S. (2022). <em>Planning with Diffusion Models.</em> NeurIPS 2022. (Diffuser backbone.)</li> <li>Song, J., Meng, C., &amp; Ermon, S. (2021). <em>Denoising Diffusion Implicit Models.</em> ICLR 2021. (DDIM simulation oracle.)</li> <li>Fast-MCTD: <a href="https://arxiv.org/abs/2506.09498">arXiv:2506.09498</a> (follow-up, ~100× speedup via parallel rollouts and coarsening, NeurIPS 2025).</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="planning"/><category term="diffusion"/><category term="mcts"/><category term="offline-rl"/><category term="inference-time-compute"/><summary type="html"><![CDATA[Paper review of Monte Carlo Tree Diffusion (Yoon, Cho et al., ICML 2025 Spotlight) — a framework that embeds MCTS inside the diffusion denoising process, treating partially denoised subplans as tree nodes, binary guidance schedules as meta-actions, and fast DDIM rollouts as the simulation oracle. Near-perfect success on long-horizon maze tasks where all diffusion baselines fail, with monotonically improving performance as inference compute scales.]]></summary></entry></feed>