<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://yjkim-stat.github.io/log/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yjkim-stat.github.io/log/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-18T22:43:01+00:00</updated><id>https://yjkim-stat.github.io/log/feed.xml</id><title type="html">Research Log</title><subtitle>Paper reviews, recent research trends, and notes on machine learning research. </subtitle><entry><title type="html">SOAR — Teaching Models to Teach Themselves: Reasoning at the Edge of Learnability</title><link href="https://yjkim-stat.github.io/log/blog/2026/soar/" rel="alternate" type="text/html" title="SOAR — Teaching Models to Teach Themselves: Reasoning at the Edge of Learnability"/><published>2026-05-18T09:30:00+00:00</published><updated>2026-05-18T09:30:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/soar</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/soar/"><![CDATA[<p><strong>Paper.</strong> Shobhita Sundaram, John Quan, Ariel Kwiatkowski, Kartik Ahuja, Yann Ollivier, Julia Kempe. <em>Teaching Models to Teach Themselves: Reasoning at the Edge of Learnability.</em> arXiv:2601.18778, Feb 2026. MIT · NYU CDS · Meta FAIR. <a href="https://arxiv.org/abs/2601.18778">[arXiv]</a> · <a href="https://ssundaram21.github.io/soar/">[project page]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Reinforcement learning has become the standard recipe for pushing LLM reasoning skills. But RL has a hard wall: <strong>if the base model never solves a problem, RL has no signal to learn from.</strong> On the hardest math problems where the base model fails every one of 128 attempts, RL flatlines. SOAR proposes a way past this wall: train a <strong>teacher</strong> copy of the model whose job is to invent <em>stepping-stone</em> problems for a <strong>student</strong> copy, and pay the teacher in the currency of <em>the student’s measured improvement on real hard problems</em>. The result is a bilevel meta-RL loop that <strong>unlocks learning</strong> on benchmarks where base RL gets nothing — <strong>4× pass@1 on MATH, 2× on HARP</strong> — using nothing but the model’s own latent curriculum-design capacity.</p> <hr/> <h2 id="1-why-vanilla-rl-stalls-on-hard-problems">1. Why Vanilla RL Stalls on Hard Problems</h2> <p>RL for reasoning (RLVR, GRPO, REINFORCE++, …) optimizes a simple objective: roll out the model on a problem, check whether the answer is correct, and update toward whatever produced the correct answer.</p> <p>There’s a hidden precondition: <strong>at least <em>some</em> rollouts must succeed.</strong> If the model never solves the problem, every rollout has reward 0, the policy gradient is 0, and RL doesn’t move the parameters. This is the “fail@K” regime — and on competition math (MATH, HARP, OlympiadBench), a non-trivial fraction of problems sit in fail@128 for moderate-size models.</p> <p>You can try three obvious workarounds, and the paper rules each one out:</p> <table> <thead> <tr> <th>Workaround</th> <th>Why it isn’t enough</th> </tr> </thead> <tbody> <tr> <td><strong>Easier problems first</strong> (manual curriculum)</td> <td>Requires labeled difficulty + still hits the wall on the genuinely hard tail</td> </tr> <tr> <td><strong>Distill from a stronger model</strong></td> <td>Defeats the purpose of <em>self-improvement</em>; depends on a teacher you don’t have</td> </tr> <tr> <td><strong>Self-play with intrinsic rewards</strong> (novelty / difficulty bonuses)</td> <td>Suffers from instability and <em>diversity collapse</em> — the teacher converges to a few brittle problem types</td> </tr> </tbody> </table> <p>SOAR’s argument is that <em>self-play is the right shape</em> — but the <strong>teacher’s reward function</strong> has been the broken piece.</p> <hr/> <h2 id="2-the-soar-framework">2. The SOAR Framework</h2> <p>The full name is <strong>Self-Optimization via Asymmetric RL</strong>. The asymmetry is the key word: teacher and student have different jobs, different action spaces, and different rewards.</p> <h3 id="21-the-setup">2.1 The setup</h3> <ul> <li><strong>Two copies of the same model</strong>, both initialized from the target LLM (in the paper, Llama-3.2-3B-Instruct).</li> <li>One copy becomes the <strong>student</strong>. Its job: solve problems by generating chains of thought, then update its policy with RL.</li> <li>The other copy becomes the <strong>teacher</strong>. Its job: <em>generate problems and proposed answers</em> for the student to train on.</li> </ul> <h3 id="22-the-two-loops">2.2 The two loops</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Outer loop — teacher learns to teach
  for each iteration:
      teacher generates a batch of (question, answer) pairs   ← teacher policy

      ─── Inner loop — student trains briefly on those problems ───
      run short-burst RL on the student
          (≈ 10–20 update steps, batch size 8)
      ─────────────────────────────────────────────────────────────

      measure student's improvement on a small held-out set of
      *real, ground-truth hard problems*

      use that improvement as the teacher's reward
      update the teacher with RLOO
</code></pre></div></div> <p>The student is a <em>throwaway probe</em> in the outer loop: each outer iteration spins up a fresh student RL phase, measures its improvement, then discards it. The teacher accumulates the signal across iterations.</p> <h3 id="23-the-reward-design--the-contribution">2.3 The reward design — the contribution</h3> <p>Prior self-play methods reward the teacher with <strong>intrinsic proxies</strong> — “is this problem novel?”, “is it hard?”, “is the answer correct?”. These proxies have no necessary connection to whether the student can actually <em>learn</em> from the problem.</p> <p>SOAR replaces every proxy with one <strong>grounded signal</strong>:</p> <blockquote> <p><strong>Reward the teacher in proportion to how much the student improved on real hard problems.</strong></p> </blockquote> <p>This single substitution is what makes the loop work. Whatever the teacher discovers about good problem design has to <em>bottom out</em> in measurable learning progress on the actual benchmark.</p> <hr/> <h2 id="3-the-counterintuitive-finding-about-quality">3. The Counterintuitive Finding About “Quality”</h2> <p>If the teacher’s job is to produce useful training data, you’d guess the most important feature is <strong>correctness</strong> — does the proposed answer actually solve the proposed problem? SOAR’s analysis says no:</p> <blockquote> <p><strong>Structural quality and well-posedness matter more than solution correctness.</strong></p> </blockquote> <p>A clear, unambiguous, well-scaffolded problem with a <em>wrong</em> answer still produces a learnable gradient for the student. An ambiguous or malformed problem with a <em>correct</em> answer doesn’t.</p> <p>This matters for two reasons:</p> <ol> <li><strong>It explains why bootstrapping is possible.</strong> The teacher’s latent ability to produce well-posed questions is much stronger than its ability to solve hard problems. SOAR taps the former.</li> <li><strong>It rewrites the conventional wisdom</strong> that synthetic data must be filtered for solution correctness before training. SOAR works <em>without</em> that filter.</li> </ol> <hr/> <h2 id="4-diversity-and-stability">4. Diversity and Stability</h2> <p>Self-play has historically collapsed onto a narrow set of strategies. SOAR measures this directly using the <strong>Vendi Score</strong> (a semantic diversity metric over the teacher’s generated questions). The finding:</p> <ul> <li><strong>Grounded teacher (SOAR):</strong> diversity comparable to the base model — stable, broad, non-collapsing.</li> <li><strong>Intrinsic-reward teacher:</strong> severe diversity collapse — the teacher converges to a few stereotyped problems and the loop destabilizes.</li> </ul> <p>The mechanism is intuitive: an intrinsic reward will always be maximized by <em>Goodhart-ing</em> the proxy (find the cheapest way to look “novel” or “hard”); a <em>grounded</em> reward can only be maximized by genuinely helping the student learn, which requires distributional breadth.</p> <hr/> <h2 id="5-headline-results">5. Headline Results</h2> <p>All numbers are with Llama-3.2-3B-Instruct, evaluated on the <strong>fail@128</strong> subsets of each benchmark (problems where the base model never succeeds in 128 attempts).</p> <table> <thead> <tr> <th>Benchmark</th> <th>Base @128</th> <th>SOAR pass@1 (multiplier)</th> <th>SOAR pass@32 (multiplier)</th> </tr> </thead> <tbody> <tr> <td><strong>MATH</strong> (hardest subset)</td> <td>0/128</td> <td><strong>4×</strong></td> <td><strong>2×</strong></td> </tr> <tr> <td><strong>HARP</strong> (hardest subset)</td> <td>0/128</td> <td><strong>2×</strong></td> <td><strong>1.5×</strong></td> </tr> </tbody> </table> <p>These are <em>unlocks</em>, not refinements. The base model is at the literal floor; SOAR drives it off the floor without any external teacher and without any new ground-truth data beyond a small held-out probe set used to score the teacher.</p> <hr/> <h2 id="6-what-the-method-actually-says">6. What the Method Actually Says</h2> <p>The structural claim of the paper, stripped of acronyms:</p> <blockquote> <p>A pretrained LLM already possesses the <em>latent capacity</em> to design a useful curriculum for itself. Standard RL cannot access this capacity because the model’s solving ability and its curriculum-design ability are not the same skill. A bilevel loop where the latter is rewarded by the former exposes the latent capacity and turns it into measurable learning.</p> </blockquote> <p>This recasts a familiar question — “how do we get a model past a plateau?” — into a different one: “what part of the model is already above the plateau, and how do we route reward to it?”</p> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>Scale.</strong> Results are at 3B parameters. Whether the curriculum-design capacity scales with parameters (and whether SOAR is needed <em>at all</em> once base models cross some capability threshold) is an open question.</li> <li><strong>Reward latency.</strong> The teacher’s reward signal requires running a fresh student RL phase — outer-loop steps are expensive.</li> <li><strong>Probe set dependency.</strong> The grounded reward is computed on a <em>small subset</em> of real hard problems. If that subset is unrepresentative, the teacher will optimize for the wrong thing.</li> <li><strong>Modality.</strong> The paper covers math. Whether structural well-posedness is the right axis for code, science, or open-ended-reasoning benchmarks is plausible but unverified.</li> </ul> <hr/> <h2 id="8-why-this-matters">8. Why This Matters</h2> <p>Two reasons:</p> <ol> <li><strong>It targets the most painful wall in reasoning post-training.</strong> Most of the gains from RL evaporate on the hardest problems — the ones we most want models to solve. SOAR is the first recipe I’ve seen that produces non-trivial movement on fail@128 problems without external supervision.</li> <li><strong>It re-frames self-improvement as a <em>teacher-design</em> problem.</strong> The asymmetry isn’t a hack; it reflects a real fact about pretrained models — <em>generating well-posed problems is a different skill than solving them.</em> Future self-improvement methods that take this seriously have a credible direction to walk in.</li> </ol> <p>For context, this sits very close to the <a href="/log/blog/2026/learning-fast-and-slow/">Fast-Slow Training trend post</a> on this site — both papers argue that the way we currently spend RL samples is wasteful, and both propose loops that extract more signal per rollout. SOAR’s contribution is at the <em>task-distribution</em> level (what does the model train on?); FST’s is at the <em>adaptation-surface</em> level (what does the model update?).</p> <hr/> <h2 id="9-tldr">9. TL;DR</h2> <p>If you remember three things:</p> <ol> <li><strong>RL stalls when initial success is 0</strong> because zero rewards give zero gradient.</li> <li><strong>SOAR is a bilevel meta-RL loop</strong> where a teacher copy of the model generates problems and is rewarded in proportion to <em>the student’s measured improvement</em> on real hard problems — replacing the proxy rewards that destabilize prior self-play.</li> <li><strong>Grounded rewards beat intrinsic ones</strong>, structural quality of the generated problems beats solution correctness, and the result is a 4× pass@1 improvement on the MATH fail@128 subset using nothing but the base model’s own latent curriculum capacity.</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Sundaram, S., Quan, J., Kwiatkowski, A., Ahuja, K., Ollivier, Y., &amp; Kempe, J. (2026). <em>Teaching Models to Teach Themselves: Reasoning at the Edge of Learnability.</em> <a href="https://arxiv.org/abs/2601.18778">arXiv:2601.18778</a>.</li> <li>Project page: <a href="https://ssundaram21.github.io/soar/">https://ssundaram21.github.io/soar/</a></li> <li>Related on this site: <a href="/log/blog/2026/learning-fast-and-slow/">Fast-Slow Training trend note</a>, <a href="/log/blog/2026/meta-harness/">Meta-Harness review</a>.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="llm"/><category term="rl"/><category term="reasoning"/><category term="curriculum-learning"/><category term="self-play"/><summary type="html"><![CDATA[Paper review of SOAR (Sundaram et al., 2026) — a bilevel meta-RL recipe where a teacher LLM is rewarded for the student LLM's measured progress on hard problems, unlocking learning on math benchmarks where the base model scores 0/128.]]></summary></entry><entry><title type="html">Learning Fast and Slow: A New Recipe for Adapting LLMs Without Forgetting</title><link href="https://yjkim-stat.github.io/log/blog/2026/learning-fast-and-slow/" rel="alternate" type="text/html" title="Learning Fast and Slow: A New Recipe for Adapting LLMs Without Forgetting"/><published>2026-05-18T08:30:00+00:00</published><updated>2026-05-18T08:30:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/learning-fast-and-slow</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/learning-fast-and-slow/"><![CDATA[<p><strong>Source.</strong> GEPA team blog post, <a href="https://gepa-ai.github.io/gepa/blog/2026/05/11/learning-fast-and-slow/"><em>“Learning, Fast and Slow: Towards LLMs That Adapt Continually”</em></a>, May 11, 2026. Backed by Tiwari, Sareen, Agrawal, Gonzalez, Zaharia, Keutzer, Dhillon, Agarwal, &amp; Khatri, <em>Learning, Fast and Slow: Towards LLMs That Adapt Continually</em>, <a href="https://arxiv.org/abs/2605.12484">arXiv:2605.12484</a>.</p> <hr/> <h2 id="why-this-matters">Why This Matters</h2> <p>For two years the implicit assumption in LLM post-training has been “if you want the model to do a new task well, <strong>update the weights</strong>” — RLHF, RLAIF, GRPO, DPO, RLOO, REINFORCE++, etc. The cost of that assumption is becoming visible:</p> <ul> <li><strong>Sample inefficiency.</strong> Each new task burns through tens of thousands of rollouts.</li> <li><strong>Catastrophic forgetting.</strong> A weights-update on task A erodes performance on B.</li> <li><strong>Loss of plasticity.</strong> After a few RL phases the model becomes <em>unable to learn</em> further tasks — the policy has drifted so far from the base distribution that exploration collapses.</li> </ul> <p>“Learning, Fast and Slow” (LFS) proposes that we have been using only half of the available adaptation surface. There is a second, much cheaper handle — the <strong>textual context</strong> — that we routinely throw away between training runs. Combining the two surfaces yields <strong>Fast-Slow Training (FST)</strong>.</p> <hr/> <h2 id="the-two-speed-adaptation-taxonomy">The Two-Speed Adaptation Taxonomy</h2> <p>The framing borrows from Kahneman’s <em>Thinking, Fast and Slow</em> but inverts the labels deliberately:</p> <table> <thead> <tr> <th>Component</th> <th>Role</th> <th>Cost</th> <th>Persistence</th> </tr> </thead> <tbody> <tr> <td><strong>Slow — model weights $\theta$</strong></td> <td>Long-lived policy / skills</td> <td>Expensive (gradients, RL rollouts)</td> <td>Survives across sessions</td> </tr> <tr> <td><strong>Fast — prompt / instructions / in-context state</strong></td> <td>Task-level shaping</td> <td>Cheap (text edits)</td> <td>Per-session, can be swapped freely</td> </tr> </tbody> </table> <p>Most modern LLM post-training touches only the slow side. In-context learning touches only the fast side. Neither alone reaches the joint optimum the paper demonstrates is reachable.</p> <hr/> <h2 id="what-gepa-is-quick-refresher">What GEPA Is (Quick Refresher)</h2> <p><a href="https://arxiv.org/abs/2507.19457">GEPA</a> (Genetic-Pareto, ICLR 2026 Oral) is a <em>reflective prompt evolution</em> optimizer. Instead of taking a scalar reward and updating weights, GEPA:</p> <ol> <li><strong>Reads the full execution trace</strong> of a candidate prompt — error messages, profiling info, reasoning logs, intermediate outputs.</li> <li>Uses an LLM to <strong>diagnose</strong> the failure in natural language.</li> <li><strong>Mutates</strong> the prompt with a targeted edit that addresses the diagnosis.</li> <li>Maintains a <strong>Pareto frontier</strong> over multiple objectives via an evolutionary loop.</li> </ol> <p>The original GEPA paper showed it can <strong>outperform GRPO by ~6% on average (up to 20%)</strong> using <strong>up to 35× fewer rollouts</strong> across six tasks. The reason is information density: GRPO compresses an entire trajectory into one scalar; GEPA reads the trajectory itself.</p> <p>In FST, GEPA plays the role of the fast learner.</p> <hr/> <h2 id="fast-slow-training-fst">Fast-Slow Training (FST)</h2> <p>The algorithm is intentionally simple — most of the contribution is the fact that <em>the simple thing works</em>.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>repeat:
  # Fast phase — frozen weights, optimize the context
  c* = GEPA(theta_t, task)            # reflective prompt evolution

  # Slow phase — frozen context, update the weights
  theta_{t+1} = RL_update(theta_t, task, context=c*)
</code></pre></div></div> <p>Two consequences fall out:</p> <ol> <li><strong>The RL phase starts from a better operating point.</strong> GEPA has already pushed the <em>fast</em> state toward task-competence, so the slow update doesn’t need to invent that competence from scratch — it can <em>consolidate</em> it.</li> <li><strong>The fast state acts as a regularizer.</strong> Because much of the task-specific shaping is held in the prompt, the weight update needed to lock in the behavior is smaller, so $\theta$ drifts less from the base model.</li> </ol> <hr/> <h2 id="the-headline-numbers">The Headline Numbers</h2> <p>The paper reports these on a reasoning-task suite plus a continual-learning benchmark:</p> <table> <thead> <tr> <th>Metric</th> <th>Result</th> </tr> </thead> <tbody> <tr> <td>Sample efficiency vs. RL-only</td> <td><strong>Up to 3× fewer samples</strong> to reach RL’s peak</td> </tr> <tr> <td>Final reward</td> <td><strong>Above</strong> either RL-only or GEPA-only</td> </tr> <tr> <td>KL divergence from base model at convergence</td> <td><strong>Up to 70% lower</strong> than RL-only</td> </tr> <tr> <td>3-stage continual learning</td> <td><strong>FST near-peak on all three stages; RL completely stalls on stage 2, only partially recovers on stage 3</strong></td> </tr> <tr> <td>Plasticity (can the trained checkpoint still learn a new task?)</td> <td>RL-trained checkpoint barely adapts; FST-trained checkpoint roughly matches the base model</td> </tr> </tbody> </table> <p>The continual-learning result is the most striking. The RL-only model acquires task 1 well, then becomes a brick. The FST model keeps learning.</p> <hr/> <h2 id="why-it-works-the-story-beneath-the-numbers">Why It Works (the Story Beneath the Numbers)</h2> <p>Three ingredients reinforce each other:</p> <ol> <li><strong>GEPA extracts more information per sample than RL.</strong> Reading a trace is denser than reading a reward, so the fast loop converges in a handful of rollouts.</li> <li><strong>The fast loop pre-shapes the task.</strong> When the slow loop fires, it sees gradients that already point in a useful direction.</li> <li><strong>The slow loop barely has to move.</strong> Because the fast state carries the task-specific shaping, only the <em>transferable</em> piece of behavior gets baked into weights — which is exactly what you want when the next task arrives.</li> </ol> <p>The chain explains the plasticity finding without invoking any new mechanism: a model that hasn’t drifted far from the base distribution still has room to be moved.</p> <hr/> <h2 id="where-this-sits-in-the-trend-landscape">Where This Sits in the Trend Landscape</h2> <p>A few converging threads make FST timely:</p> <ul> <li><strong>Reflective prompt optimization is maturing.</strong> GEPA, DSPy’s optimizers, and the growing skill ecosystem (e.g. the <a href="/log/explorations/2026-05-18-grill-me-skills/"><code class="language-plaintext highlighter-rouge">grill-me</code></a> family, the <a href="/log/explorations/2026-05-18-research-paper-writing-skills/">research-paper-writing skill</a>) show that <em>prompts are now a first-class optimization target</em>.</li> <li><strong>The cost of RL is being noticed.</strong> Meta-Harness (<a href="/log/blog/2026/meta-harness/">review</a>) and FST both argue that we have been spending compute on the wrong loop. Meta-Harness optimizes the <em>harness</em>; FST optimizes the <em>prompt-conditioned RL process</em>. They are complementary moves in the same direction.</li> <li><strong>Continual learning is back on the agenda.</strong> Plasticity loss has been a research curiosity in classic RL for a decade; FST shows that it bites in modern LLM post-training too — and that the fast-slow decomposition is a credible mitigation.</li> </ul> <p>If you were betting on what 2026’s post-training stack will look like, the rough shape now reads: <strong>fast prompt search + small slow updates + careful KL control</strong>, rather than the current default of pure RL with ever-larger batch sizes.</p> <hr/> <h2 id="what-im-watching-for">What I’m Watching For</h2> <ul> <li><strong>Other “fast” choices.</strong> GEPA is one instance of the fast loop; retrieval, tool memory, and structured scratchpads are others. The FST scaffold doesn’t depend on GEPA specifically.</li> <li><strong>Multi-task aggregation.</strong> The reported continual benchmark is a 3-stage stream. Can FST hold up to dozens of tasks, the way a production system would need?</li> <li><strong>Reward-shaping by the fast loop.</strong> A speculative but natural extension: let GEPA also propose changes to the <em>reward signal</em> the slow loop optimizes against. If the fast loop can read traces, it can also read what the reward should have been.</li> </ul> <hr/> <h2 id="tldr">TL;DR</h2> <p>LLM adaptation has been treated as a single problem (“update the weights”). FST argues it is actually two: a <strong>slow</strong> update of parameters and a <strong>fast</strong> update of context, with GEPA filling the fast role. Interleaving the two reaches RL’s ceiling with <strong>~3× fewer samples</strong>, climbs <strong>higher</strong>, and — most importantly — leaves the model close enough to its base distribution to <strong>keep learning</strong> on new tasks. That last property is what makes this the most interesting post-training paper of the month.</p> <hr/> <h2 id="references">References</h2> <ul> <li>GEPA team blog (May 11, 2026): <em><a href="https://gepa-ai.github.io/gepa/blog/2026/05/11/learning-fast-and-slow/">Learning, Fast and Slow: Towards LLMs That Adapt Continually</a>.</em></li> <li>Paper: Tiwari et al., <em>Learning, Fast and Slow: Towards LLMs That Adapt Continually</em>, <a href="https://arxiv.org/abs/2605.12484">arXiv:2605.12484</a>.</li> <li>Background: Agrawal et al., <em>GEPA: Reflective Prompt Evolution Can Outperform Reinforcement Learning</em>, <a href="https://arxiv.org/abs/2507.19457">arXiv:2507.19457</a> (ICLR 2026 Oral).</li> <li>Related on this site: <a href="/log/blog/2026/meta-harness/">Meta-Harness review</a>, <a href="/log/explorations/2026-05-18-grill-me-skills/">grill-me skills</a>, <a href="/log/explorations/2026-05-18-research-paper-writing-skills/">research-paper-writing skill</a>.</li> </ul>]]></content><author><name></name></author><category term="trends"/><category term="llm"/><category term="rl"/><category term="prompt-optimization"/><category term="continual-learning"/><category term="gepa"/><summary type="html"><![CDATA[Trend note on the GEPA team's "Learning, Fast and Slow" — Fast-Slow Training (FST) interleaves slow RL weight updates with fast in-context prompt evolution via GEPA, hitting RL's ceiling with up to 3x fewer samples and staying close enough to the base model to keep learning new tasks afterward.]]></summary></entry><entry><title type="html">Parcae: Scaling Laws for Stable Looped Language Models</title><link href="https://yjkim-stat.github.io/log/blog/2026/parcae/" rel="alternate" type="text/html" title="Parcae: Scaling Laws for Stable Looped Language Models"/><published>2026-05-18T07:00:00+00:00</published><updated>2026-05-18T07:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/parcae</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/parcae/"><![CDATA[<p><strong>Paper.</strong> Hayden Prairie, Zachary Novack, Taylor Berg-Kirkpatrick, Daniel Y. Fu. <em>Parcae: Scaling Laws For Stable Looped Language Models.</em> arXiv:2604.12946, April 2026. UC San Diego · Together AI. <a href="https://arxiv.org/abs/2604.12946">[arXiv]</a> · <a href="https://sandyresearch.github.io/parcae/">[project page]</a> · <a href="https://www.together.ai/blog/parcae">[Together AI blog]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>A standard Transformer is a tall stack of $L$ distinct blocks. You pay for the depth twice: once in <strong>parameters</strong> (each block has its own weights) and once in <strong>compute</strong> (each block runs once). What if you could keep just one block of weights but run it $N$ times in a loop? You’d pay parameters for <em>one</em> block and compute for <em>N</em>. This is the <strong>looped transformer</strong>. It’s been tried before and it tends to <strong>blow up during training</strong> (the residual stream grows without bound; the loss spikes). Parcae diagnoses <em>why</em> (a spectral-norm argument borrowed from control theory), fixes it with a parameterization trick borrowed from state-space models, and shows that the resulting recipe gives clean <strong>scaling laws</strong>: a 770M Parcae model matches a 1.3B fixed-depth Transformer.</p> <hr/> <h2 id="1-the-background-you-need">1. The Background You Need</h2> <h3 id="what-a-transformer-block-does-sketch">What a Transformer block does (sketch)</h3> <p>Each Transformer block takes a sequence of vectors — the <strong>residual stream</strong> $x_t$ — and produces an updated sequence</p> \[x_{t+1} = x_t + f_\theta(x_t),\] <p>where $f_\theta$ is attention + MLP + normalization. The “+” is the <strong>residual connection</strong>: each block <em>adds</em> its contribution to a shared running representation. The depth of a Transformer is just the number of these additions.</p> <h3 id="what-looped-means">What “looped” means</h3> <p>In a looped transformer, you keep <strong>one</strong> block of weights $\theta$ (or a small group) and apply it repeatedly:</p> \[x_{t+1} = x_t + f_\theta(x_t), \quad t = 1, 2, \dots, N.\] <p>The block is reused $N$ times. <strong>Parameters stay the same; compute scales with $N$.</strong> This is appealing because most of inference cost is compute, and you can dial $N$ up at <em>test time</em> to spend more compute on harder inputs (a poor man’s “thinking longer”).</p> <p>Earlier work on this idea includes Universal Transformers, CoTFormer, and the recent line of <em>recurrent-depth models</em> (RDMs) the paper positions itself against.</p> <h3 id="why-it-usually-breaks">Why it usually breaks</h3> <p>The problem is sitting inside that “$+$”. Every loop iteration <em>adds</em> something to the residual stream. If the added pieces tend to grow, the residual stream explodes after a handful of iterations. In practice this shows up as <strong>loss spikes</strong> and <strong>divergent training runs</strong>. Fixed-depth Transformers don’t have this problem because each layer has its own LayerNorm + weights tuned for its specific point in the stack; loops do because the <em>same</em> block is responsible for the update at every depth.</p> <hr/> <h2 id="2-the-reframing-looping-as-a-dynamical-system">2. The Reframing: Looping as a Dynamical System</h2> <p>The paper’s first move is to stop thinking of the loop as “apply a neural network $N$ times” and start thinking of it as a <strong>discrete-time dynamical system</strong> over the residual stream:</p> \[x_{t+1} = A x_t + B u_t + \text{(nonlinearity)}.\] <p>After a <em>linear approximation</em> of the actual block, the loop looks like a linear recurrence with state $x_t$, input $u_t$ (token-level information injected at each step), and matrices $A, B$ baked out of $f_\theta$. This is exactly the setting where <strong>classical control theory</strong> has a clean answer:</p> <table> <thead> <tr> <th>Spectral norm $\rho(A)$</th> <th>Behavior</th> </tr> </thead> <tbody> <tr> <td>$&lt; 1$</td> <td><strong>Stable</strong> — state stays bounded</td> </tr> <tr> <td>$= 1$</td> <td>Marginally stable</td> </tr> <tr> <td>$&gt; 1$</td> <td><strong>Unstable</strong> — state grows without bound (residual explosion)</td> </tr> </tbody> </table> <p>The empirical finding: in existing looped architectures, $\rho(A) &gt; 1$, and the offending matrices are the <strong>injection parameters</strong> — the weights that decide how token-level information enters the residual stream at each loop step. That’s the diagnosis.</p> <hr/> <h2 id="3-the-fix-parcaes-parameterization">3. The Fix: Parcae’s Parameterization</h2> <p>Now they need to <strong>force</strong> $\rho(A) &lt; 1$ by construction, without killing the model’s expressiveness. They borrow a trick from state-space models like S4 and Mamba:</p> <ol> <li><strong>Work in continuous time.</strong> Replace the discrete recurrence $x_{t+1} = A x_t + \dots$ with its continuous-time counterpart $\dot{x}(t) = A_c x(t) + \dots$.</li> <li><strong>Constrain $A_c$ to be <em>negative diagonal</em>.</strong> Each diagonal entry is a learned negative scalar. A negative-diagonal $A_c$ has all eigenvalues on the negative real axis, which after discretization gives $\rho(A) &lt; 1$ automatically.</li> <li><strong>Discretize with a learned step size.</strong> Convert $A_c$ back to the actual discrete matrix $A$ using <strong>zero-order hold (ZOH)</strong> or <strong>Euler</strong> integration with a step size $\Delta$ that the model learns. The step size lets the loop control <em>how aggressively</em> each iteration updates the residual stream.</li> </ol> <p>The intuition: the negative diagonal makes every iteration a slight <strong>decay</strong> toward zero, while the rest of the block ($B u_t$ + the nonlinear residual) supplies the actual signal. The residual stream can grow, but only as long as the block is actively pushing — it can’t run away on its own.</p> <p><strong>Why this is more than a hack.</strong> It generalizes the standard residual recipe (which corresponds to a specific, ungoverned choice of $A$) to a small <em>parameterized family</em> of stable recurrences — and the parameters are learned end-to-end.</p> <hr/> <h2 id="4-scaling-laws-for-looped-training">4. Scaling Laws for Looped Training</h2> <p>Once training is stable, the natural next question is: <strong>how should you spend your compute?</strong> A fixed-depth Transformer at a given FLOP budget has one main knob (parameters vs. tokens, à la Chinchilla). A looped model has <em>two</em>: parameters/tokens <em>and</em> loop count $N$.</p> <p>The paper measures this carefully and reports two scaling laws.</p> <h3 id="41-training-time-looping-flop-optimal-recipe">4.1 Training-time looping (FLOP-optimal recipe)</h3> <p>When you’re allowed to tune $N$ during training:</p> <blockquote> <p><strong>Increase the mean recurrence $N$ and the training data together, following a power law.</strong></p> </blockquote> <p>At a fixed FLOP budget, you do better by training with <strong>more loops and fewer tokens</strong> than by training with one loop and lots of tokens. The optimal $N$ and the optimal token count both scale as power laws in the FLOP budget, with consistent exponents across the two scales they tested.</p> <h3 id="42-test-time-looping">4.2 Test-time looping</h3> <p>If you train at $N$ and then dial $N$ <em>up</em> at inference time:</p> <blockquote> <p><strong>Validation loss decays as a saturating exponential in $N$.</strong></p> </blockquote> <p>Each extra loop helps less than the previous one. There’s a soft ceiling beyond which more iterations stop helping. This matters for practitioners: you can spend extra inference compute on hard inputs, but with strongly diminishing returns.</p> <h3 id="43-the-pareto-frontier">4.3 The Pareto frontier</h3> <p>Plotting downstream quality vs. FLOPs, <strong>looped models trained at their optimal $N$ sit on a strictly better frontier than fixed-depth models</strong> at every FLOP budget the authors tested. Looping isn’t a trick that pays off only in special regimes — it’s Pareto-dominant.</p> <hr/> <h2 id="5-the-headline-number">5. The Headline Number</h2> <p>The result everyone is quoting:</p> <blockquote> <p>A <strong>770M-parameter</strong> Parcae model matches the downstream quality of a <strong>1.3B-parameter</strong> fixed-depth Transformer trained on the same data — by reusing the same layers across multiple loop iterations instead of adding more weights.</p> </blockquote> <p>Other numbers:</p> <ul> <li><strong>−6.3%</strong> validation perplexity vs. prior RDMs at matched parameters and data.</li> <li><strong>−9.1%</strong> perplexity on WikiText.</li> <li><strong>+1.8</strong> points on the average of several downstream benchmarks.</li> </ul> <p>This is not a “5%-better-than-the-baseline” paper. The headline is a <strong>~2× parameter-efficiency</strong> at downstream quality, with stable training as the enabling condition.</p> <hr/> <h2 id="6-why-it-matters">6. Why It Matters</h2> <p>Two beats:</p> <ol> <li><strong>Decoupling parameters from compute.</strong> Parcae is one of the cleanest demonstrations that you don’t need to grow the parameter count to grow the compute spent on each token. For deployment (memory bandwidth, KV cache, on-device inference) this is enormous — parameters cost RAM, loops cost only time, and you can choose how much time to spend per token.</li> <li><strong>The right abstraction for “stability in recurrent depth”.</strong> The paper’s contribution is not just an architecture — it’s the <strong>diagnosis</strong>. Once you frame looping as a dynamical system and look at the spectral norm, the fix is almost forced, and you can reason about <em>future</em> looped designs (different $A_c$ families, different discretization schemes) inside the same framework. That transferability is what makes me bet on this line of work.</li> </ol> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>Inference latency.</strong> “Parameter-efficient” doesn’t mean “wall-clock-efficient.” A 770M Parcae model that loops $N$ times costs roughly $N \times$ the per-token compute of a 770M fixed-depth model. The win is <em>vs. a 1.3B model</em>, not vs. its own parameter twin.</li> <li><strong>Saturating returns at test time.</strong> You can’t keep looping forever for free — the saturating exponential decay caps the practical benefit of test-time recursion.</li> <li><strong>Linearization.</strong> The stability argument relies on a <em>linear approximation</em> to the actual nonlinear block. The empirical results show this is enough in practice, but it’s not a global guarantee.</li> <li><strong>Architecture coverage.</strong> Reported results are at the 360M and 770M scale. Whether the scaling laws extrapolate cleanly to multi-billion parameter, instruction-tuned regimes is the next paper.</li> </ul> <hr/> <h2 id="8-the-takeaway-for-a-first-reader">8. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Looped transformers reuse the same block $N$ times</strong> — fewer parameters, more compute per token.</li> <li><strong>They blow up because the residual recurrence is unstable</strong>; formally, the spectral norm of the linearized update matrix exceeds 1.</li> <li><strong>Parcae forces stability by parameterizing the update as a discretized continuous-time recurrence with a negative-diagonal generator</strong> (a trick from state-space models), which is enough to make a 770M model match a 1.3B fixed-depth Transformer with clean scaling laws.</li> </ol> <p>That’s the whole arc: dynamical-system framing → spectral-norm diagnosis → SSM-style fix → cleaner scaling laws → 2× parameter efficiency.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Prairie, H., Novack, Z., Berg-Kirkpatrick, T., &amp; Fu, D. Y. (2026). <em>Parcae: Scaling Laws For Stable Looped Language Models.</em> arXiv:2604.12946.</li> <li>Project page &amp; blog post: <a href="https://sandyresearch.github.io/parcae/">https://sandyresearch.github.io/parcae/</a> · <a href="https://www.together.ai/blog/parcae">https://www.together.ai/blog/parcae</a></li> <li>Background on state-space discretization: S4 and Mamba (the source of the ZOH / negative-diagonal $A$ trick).</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="llm"/><category term="transformer"/><category term="architecture"/><category term="scaling-laws"/><category term="looped-models"/><summary type="html"><![CDATA[Beginner-friendly review of Parcae (Prairie et al., 2026) — a stable recipe for *looped* transformers that lets a 770M model match a 1.3B fixed-depth Transformer by running the same layers multiple times, derived from a residual-stream stability analysis.]]></summary></entry><entry><title type="html">Meta-Harness: End-to-End Optimization of Model Harnesses</title><link href="https://yjkim-stat.github.io/log/blog/2026/meta-harness/" rel="alternate" type="text/html" title="Meta-Harness: End-to-End Optimization of Model Harnesses"/><published>2026-05-18T01:00:00+00:00</published><updated>2026-05-18T01:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/meta-harness</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/meta-harness/"><![CDATA[<p><strong>Paper.</strong> Yoonho Lee, Roshen Nair, Qizheng Zhang, Kangwook Lee, Omar Khattab, Chelsea Finn. <em>Meta-Harness: End-to-End Optimization of Model Harnesses.</em> arXiv:2603.28052, March 2026. Stanford · MIT · KRAFTON. <a href="https://arxiv.org/abs/2603.28052">[arXiv]</a> · <a href="https://yoonholee.com/meta-harness/">[project page]</a> · <a href="https://github.com/stanford-iris-lab/meta-harness">[code]</a></p> <hr/> <h2 id="1-the-problem-the-paper-names">1. The Problem the Paper Names</h2> <p>LLM applications are not just a model — they are a model plus a <strong>harness</strong>: the surrounding code that decides what to store, what to retrieve, what to show the model, what tools to expose, and how the workflow runs. In modern systems (coding agents, RAG pipelines, long-running assistants) the harness is large, hand-tuned, and routinely matters more than the choice of base model.</p> <p>The paper’s claim is simple: <strong>the harness itself should be the optimization target</strong>, and it should be optimized end-to-end against task performance.</p> <p>Why this isn’t already solved:</p> <ul> <li><em>Prompt optimizers</em> (DSPy, OPRO, APE, etc.) tune <strong>prompts</strong>, not the surrounding code. They cannot rewrite the retriever, the context-management policy, or the tool wiring.</li> <li><em>Text optimizers</em> that do attempt program-level search compress feedback too aggressively — they hand the proposer scores or short summaries instead of the raw evidence needed to diagnose failures.</li> </ul> <h2 id="2-the-method">2. The Method</h2> <p>Meta-Harness is an <strong>outer loop</strong> over harness code. At each step, an <em>agentic proposer</em> (a coding agent — Claude Code in the reported runs) generates a new candidate harness, the harness is executed on a task suite, and the result is committed back into a shared filesystem.</p> <p>The critical design choice is <em>what the proposer is allowed to see</em>:</p> <table> <thead> <tr> <th>Channel</th> <th>Prior text optimizers</th> <th>Meta-Harness</th> </tr> </thead> <tbody> <tr> <td>Scores of past candidates</td> <td>✓</td> <td>✓</td> </tr> <tr> <td>LLM-written summaries of past runs</td> <td>sometimes</td> <td>✓</td> </tr> <tr> <td><strong>Source code of every prior candidate</strong></td> <td>✗</td> <td>✓</td> </tr> <tr> <td><strong>Raw execution traces / logs</strong></td> <td>✗</td> <td>✓</td> </tr> <tr> <td>Diagnostic budget per step</td> <td>≤ 26K tokens</td> <td>up to <strong>10M tokens</strong></td> </tr> </tbody> </table> <p>The proposer reads what it needs via standard tools (<code class="language-plaintext highlighter-rouge">grep</code>, <code class="language-plaintext highlighter-rouge">cat</code>, <code class="language-plaintext highlighter-rouge">ls</code>) over a directory that contains every prior candidate’s code, score, and trace. There is no compression layer between the proposer and the evidence.</p> <p>In the reported TerminalBench-2 run (10 iterations, Claude Opus 4.6) the proposer reads a <strong>median of 82 files per iteration</strong>, split roughly:</p> <ul> <li>41% — source code of prior harness candidates,</li> <li>40% — execution traces from prior runs,</li> <li>6% — score summaries,</li> <li>13% — other files.</li> </ul> <p>In other words, the proposer spends most of its attention budget on <strong>counterfactual diagnosis</strong>: it reads logs of failures, identifies the specific failure mode, and proposes a targeted code change grounded in concrete evidence.</p> <h2 id="3-experiments">3. Experiments</h2> <p>The paper evaluates on three deliberately heterogeneous domains.</p> <h3 id="31-online-text-classification">3.1 Online text classification</h3> <p>Meta-Harness optimizes a context-management harness against a state-of-the-art baseline.</p> <ul> <li><strong>+7.7 points</strong> over the SOTA context-management system.</li> <li><strong>4× fewer context tokens</strong> at inference time.</li> </ul> <p>This is the cleanest demonstration: the harness improves <em>and</em> shrinks simultaneously, which a prompt-only optimizer cannot achieve.</p> <h3 id="32-retrieval-augmented-math-reasoning-imo-level">3.2 Retrieval-augmented math reasoning (IMO-level)</h3> <p>A <strong>single</strong> harness discovered by Meta-Harness on one training model is evaluated on <strong>five held-out models</strong> over 200 IMO-level problems.</p> <ul> <li><strong>+4.7 points</strong> average gain across the five held-out models.</li> </ul> <p>The transfer matters more than the absolute number: it suggests Meta-Harness is not just overfitting to the optimizer’s own search model.</p> <h3 id="33-agentic-coding--terminalbench-2">3.3 Agentic coding — TerminalBench-2</h3> <p>Meta-Harness produces harnesses that <strong>surpass the best hand-engineered baselines</strong> on TerminalBench-2. The ablation here is the heart of the paper.</p> <h2 id="4-the-ablation-you-should-read-first">4. The Ablation You Should Read First</h2> <p>The same outer-loop search is run with three proposer interfaces:</p> <table> <thead> <tr> <th>Proposer sees</th> <th style="text-align: right">Median accuracy</th> <th style="text-align: right">Best accuracy</th> </tr> </thead> <tbody> <tr> <td>Scores only</td> <td style="text-align: right">~34.9</td> <td style="text-align: right"><strong>41.3</strong></td> </tr> <tr> <td>Scores + LLM-generated summaries</td> <td style="text-align: right">~34.9</td> <td style="text-align: right"><strong>38.7</strong></td> </tr> <tr> <td>Scores + summaries + source code + raw traces (full Meta-Harness)</td> <td style="text-align: right"><strong>50.0</strong></td> <td style="text-align: right"><strong>56.7</strong></td> </tr> </tbody> </table> <p>Two punchlines:</p> <ol> <li><strong>Raw traces are the active ingredient.</strong> Going from scores → full interface is a +15 jump in best accuracy.</li> <li><strong>Summaries can be actively harmful.</strong> LLM-generated summaries underperform scores-only on best accuracy (38.7 vs 41.3). The author’s reading is that summaries compress away the diagnostically useful detail — the very things a proposer needs to localize a bug.</li> </ol> <p>This is a strong empirical statement against the “summarize then propose” pattern that pervades current text-based optimizers.</p> <h2 id="5-where-this-fits">5. Where This Fits</h2> <p>Several recent trends converge here:</p> <ul> <li><strong>Agent harness design</strong> (Anthropic’s planner/generator/evaluator, Claude Code subagents, LangGraph supervisors). Meta-Harness sits one level up: instead of writing the harness, it <em>searches over</em> harnesses.</li> <li><strong>Filesystem as agent memory.</strong> Like Anthropic’s research-agent setups, Meta-Harness treats the filesystem as a long-term scratchpad shared across iterations. The shared filesystem is what makes the 10M-token diagnostic budget tractable — it’s lazily paged in by the proposer’s own tool calls, not stuffed into one context window.</li> <li><strong>Coding agents as optimizers.</strong> The proposer is just Claude Code with read access to past runs. This is a clean instance of <em>using a coding agent as a search operator</em> rather than as an end-user tool.</li> </ul> <h2 id="6-limitations--what-im-watching-for">6. Limitations / What I’m Watching For</h2> <ul> <li><strong>Cost.</strong> Each outer-loop step runs a coding agent with up to 10M tokens of diagnostic context plus a full harness evaluation. The paper reports 10 iterations on TerminalBench-2; broader sweeps are not cheap.</li> <li><strong>Eval signal.</strong> The method assumes a scalar score per candidate. Tasks without crisp automated evals (open-ended writing, subjective UX) need a separate evaluator agent — likely the next paper.</li> <li><strong>Search-model leakage.</strong> Meta-Harness optimizes a harness with model <em>A</em> in the loop; held-out evaluation on models <em>B–F</em> gives some evidence of transfer, but the search-model’s idiosyncrasies could still leak into the discovered code.</li> <li><strong>Convergence.</strong> The ablation reports <em>median</em> and <em>best</em> — it would be useful to see learning curves and whether the proposer plateaus or keeps improving with more iterations.</li> </ul> <h2 id="7-why-i-think-this-paper-matters">7. Why I Think This Paper Matters</h2> <p>For the past two years the “scaffolding” around LLMs — retrievers, context managers, tool routers, agent loops — has been written by hand, guarded by intuition, and improved by trial and error. Meta-Harness demonstrates that the scaffolding is itself an object that can be <strong>optimized</strong>, and that the bottleneck is not the search algorithm but <strong>how much raw evidence you let the proposer see</strong>.</p> <p>The takeaway is not “use Meta-Harness.” The takeaway is:</p> <blockquote> <p>If your optimization loop is summarizing the evidence before the optimizer sees it, you are throwing away the signal that makes optimization work.</p> </blockquote> <p>That single observation reframes how I want to design every multi-agent system from here on.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Lee, Y., Nair, R., Zhang, Q., Lee, K., Khattab, O., &amp; Finn, C. (2026). <em>Meta-Harness: End-to-End Optimization of Model Harnesses.</em> arXiv:2603.28052.</li> <li>Project page: <a href="https://yoonholee.com/meta-harness/">https://yoonholee.com/meta-harness/</a></li> <li>Code: <a href="https://github.com/stanford-iris-lab/meta-harness">https://github.com/stanford-iris-lab/meta-harness</a></li> <li>Related: my earlier notes on <a href="/log/explorations/2026-05-06-agent-harness-team/">agent harness design</a> and the <a href="/log/explorations/2026-05-18-research-agent-team-rules/">research agent team rules</a> that motivated this read.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="agents"/><category term="harness"/><category term="llm-serving"/><category term="optimization"/><category term="prompt-optimization"/><summary type="html"><![CDATA[Paper review of Meta-Harness (Lee et al., 2026) — an outer-loop search over the *code around the LLM*, driven by a coding-agent proposer that reads prior candidates and their raw execution traces through the filesystem.]]></summary></entry><entry><title type="html">Mamba: Linear-Time Sequence Modeling with Selective State Spaces</title><link href="https://yjkim-stat.github.io/log/blog/2025/mamba/" rel="alternate" type="text/html" title="Mamba: Linear-Time Sequence Modeling with Selective State Spaces"/><published>2025-10-27T15:00:00+00:00</published><updated>2025-10-27T15:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2025/mamba</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2025/mamba/"><![CDATA[<p>Mamba (Gu &amp; Dao, 2023) introduces a <strong>selective state space model</strong> that resolves the efficiency gap between SSMs and Transformers:</p> <p><strong>Key innovation — input-dependent transitions</strong>: Standard SSMs have fixed $A, B, C$ matrices. Mamba makes these <em>data-dependent</em> (parameterized by the input sequence), enabling selective <em>filtering</em> of information — the model learns what to remember and what to forget based on content, analogous to an LLM’s attention but without $O(L^2)$ cost.</p> <p><strong>Hardware-aware algorithm</strong>: A parallel scan algorithm exploits GPU memory hierarchy to avoid materializing the expanded state, achieving near-linear compute in practice.</p> <p><strong>Results</strong>:</p> <ul> <li>Matches or exceeds Transformer quality on language modeling at equivalent parameter counts</li> <li>Inference scales linearly with sequence length (vs. quadratic for attention)</li> <li>Particularly strong on long-sequence tasks where Transformers saturate</li> </ul> <p><strong>Paper</strong>: <a href="https://arxiv.org/abs/2312.00752">arXiv:2312.00752</a></p>]]></content><author><name></name></author><category term="paper-review"/><category term="ssm"/><category term="llm"/><summary type="html"><![CDATA[Selective state space model that matches Transformer quality with linear-time inference scaling.]]></summary></entry><entry><title type="html">SSM Part I: Modeling Sequences with Structured State Spaces</title><link href="https://yjkim-stat.github.io/log/blog/2025/ssm-structured-state-spaces/" rel="alternate" type="text/html" title="SSM Part I: Modeling Sequences with Structured State Spaces"/><published>2025-10-02T15:00:00+00:00</published><updated>2025-10-02T15:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2025/ssm-structured-state-spaces</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2025/ssm-structured-state-spaces/"><![CDATA[<h2 id="sequence-models">Sequence Models</h2> <p>A sequence model is a parameterized mapping where inputs and outputs are sequences of feature vectors, with parameters learned by gradient descent. Different architectures excel in different regimes:</p> <ul> <li><strong>RNNs</strong> for stateful sequential processing</li> <li><strong>CNNs</strong> for perceptual signals with local structure</li> <li><strong>Transformers</strong> for complex long-range interactions</li> <li><strong>Neural Differential Equations</strong> for irregular time series</li> </ul> <p>Key challenges: efficient training over full sequences, efficient inference at single timesteps, and modeling long-range dependencies without vanishing gradients.</p> <h2 id="state-space-models-ssms">State Space Models (SSMs)</h2> <p>SSMs offer a unified framework that is <strong>continuous, recurrent, and convolutional</strong> simultaneously. The continuous-time formulation:</p> \[x'(t) = A x(t) + B u(t), \quad y(t) = C x(t) + D u(t)\] <p>By choosing the right discretization, the same model becomes:</p> <ul> <li>A <strong>recurrent</strong> model for sequential inference</li> <li>A <strong>convolutional</strong> model for parallel training (via the State Space Kernel)</li> </ul> <h2 id="structured-ssms-s4">Structured SSMs (S4)</h2> <p>Naive SSMs require $O(N^2 L)$ operations and $O(N L)$ space — prohibitive for long sequences. <strong>S4</strong> achieves $O(N + L)$ complexity by structuring the state matrix.</p> <h3 id="long-range-dependencies-via-hippo">Long-Range Dependencies via HIPPO</h3> <p>The <strong>HIPPO</strong> (High-Order Polynomial Projection Operator) framework treats sequence modeling as <strong>online function approximation</strong> — the hidden state encodes a polynomial approximation of the input history. This gives principled handling of long-range dependencies.</p> <h3 id="efficient-computation">Efficient Computation</h3> <p>Three discretization options bridge continuous and discrete domains:</p> <ul> <li><strong>Euler’s method</strong>: simple but unstable</li> <li><strong>Bilinear</strong>: standard for SSMs</li> <li><strong>Zero-Order Hold</strong>: used in Mamba</li> </ul> <h2 id="diagonal-plus-low-rank-dplr">Diagonal Plus Low-Rank (DPLR)</h2> <p>To overcome computational bottlenecks, S4 uses the <strong>DPLR structure</strong>: $A = \Lambda + PQ^*$. Three techniques exploit this:</p> <ol> <li><strong>Kernel Generating Functions</strong>: compute outputs in frequency domain via FFT, avoiding explicit kernel materialization</li> <li><strong>Woodbury Identity</strong>: efficient matrix inversions for the low-rank correction</li> <li><strong>Cauchy Matrices</strong>: reduce complexity to $O((M+N)\log(M+N))$</li> </ol> <h2 id="numerical-stability">Numerical Stability</h2> <p><strong>Hurwitz matrices</strong> (eigenvalues with negative real parts) ensure stable dynamics. The <strong>Hurwitz DPLR form</strong> $A := \Lambda - PP^*$ guarantees numerical stability while preserving the computational structure.</p> <h2 id="takeaways">Takeaways</h2> <p>SSMs provide a principled and efficient alternative to attention for long-range modeling. The continuous → discrete bridge, the convolutional ↔ recurrent equivalence, and the structured matrix algebra all work together to make linear-time sequence modeling practical.</p>]]></content><author><name></name></author><category term="notes"/><category term="ssm"/><summary type="html"><![CDATA[Study notes on structured state space models (S4) — recurrent, convolutional, and continuous views unified.]]></summary></entry><entry><title type="html">LLM-JEPA: Large Language Models Meet Joint Embedding Predictive Architectures</title><link href="https://yjkim-stat.github.io/log/blog/2025/llm-jepa/" rel="alternate" type="text/html" title="LLM-JEPA: Large Language Models Meet Joint Embedding Predictive Architectures"/><published>2025-09-28T15:00:00+00:00</published><updated>2025-09-28T15:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2025/llm-jepa</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2025/llm-jepa/"><![CDATA[<h2 id="motivation">Motivation</h2> <p>Autoregressive LLMs are strong generators but their representations may be suboptimal because next-token prediction is a <em>local</em> objective. Joint Embedding Predictive Architectures (JEPA), inspired by LeCun’s energy-based framework, train representations by predicting <em>embeddings</em> rather than tokens — potentially capturing higher-level semantic structure.</p> <h2 id="approach">Approach</h2> <p>LLM-JEPA combines two training objectives:</p> <ol> <li><strong>Standard next-token prediction</strong> (preserving generation capability)</li> <li><strong>JEPA-style embedding loss</strong>: given paired inputs (e.g., natural language + regular expression for the same concept), predict the embedding of one view from the other</li> </ol> <p>A <code class="language-plaintext highlighter-rouge">[PRED]</code> token implements the predictor using the LLM’s own weights, keeping architecture unchanged. Separate forward passes for each view prevent attention from mixing information between views.</p> <h2 id="results">Results</h2> <p>Gains across model families (Llama 3, Gemma 2, OpenELM, OLMo) on:</p> <ul> <li>Natural language → regex conversion</li> <li>Mathematical reasoning (GSM8K)</li> <li>SQL generation (Spider)</li> <li>Sentiment analysis in pre-training experiments</li> </ul> <h2 id="trade-offs">Trade-offs</h2> <ul> <li><strong>Training cost triples</strong>: each view pair requires additional forward passes</li> <li>Requires datasets with naturally aligned multiple views — not universally applicable</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="llm"/><category term="representation-learning"/><summary type="html"><![CDATA[Combining autoregressive language modeling with JEPA-style joint embedding prediction to improve representations.]]></summary></entry><entry><title type="html">RL Study Notes — Lecture 2: Value Functions and Bellman Equations</title><link href="https://yjkim-stat.github.io/log/blog/2025/rl-study-notes-l2/" rel="alternate" type="text/html" title="RL Study Notes — Lecture 2: Value Functions and Bellman Equations"/><published>2025-09-28T15:00:00+00:00</published><updated>2025-09-28T15:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2025/rl-study-notes-l2</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2025/rl-study-notes-l2/"><![CDATA[<h2 id="value-functions-revisited">Value Functions Revisited</h2> <p>Two complementary value functions measure expected long-term return:</p> <p><strong>State-value</strong> $V^\pi(s)$: expected return starting from state $s$ following policy $\pi$.</p> <p><strong>Action-value (Q)</strong> $Q^\pi(s, a)$: expected return starting from state $s$, taking action $a$, then following $\pi$.</p> <p>Relationship: $V^\pi(s) = \sum_a \pi(a \mid s) Q^\pi(s, a)$</p> <h2 id="bellman-equation">Bellman Equation</h2> <p>The recursive structure underlying all value-based RL:</p> \[V^\pi(s) = \mathbb{E}_{\pi, P}\!\left[r + \gamma V^\pi(s')\right]\] <p>In words: <strong>current value = immediate reward + discounted next value</strong>.</p> <p>This recursion enables iterative computation (value iteration, policy evaluation) instead of summing infinite trajectories.</p> <h2 id="optimal-value-functions">Optimal Value Functions</h2> <p>The <em>best</em> possible value over all policies:</p> \[V^*(s) = \max_\pi V^\pi(s), \quad Q^*(s, a) = \max_\pi Q^\pi(s, a)\] <p><strong>Bellman optimality equation</strong>:</p> \[V^*(s) = \max_a \mathbb{E}_P[r + \gamma V^*(s')]\] <p>This is the foundation for value iteration, Q-learning, and policy improvement theorems.</p> <h2 id="gridworld-example">Gridworld Example</h2> <p>A simple navigation grid illustrates how rewards and transitions combine into value estimates. Walking through the iteration by hand makes the recursion tangible: each state’s value depends only on its neighbors’ values plus immediate rewards.</p> <p>The exercise also demonstrates how $\gamma$ controls the planning horizon — higher $\gamma$ propagates reward signal further from terminal states.</p>]]></content><author><name></name></author><category term="notes"/><category term="rl"/><category term="rlhf"/><summary type="html"><![CDATA[Value functions, Bellman equations, optimal policies, and a Gridworld example.]]></summary></entry><entry><title type="html">RL Study Notes — Lecture 1: MDP, Behavior Cloning, DAgger</title><link href="https://yjkim-stat.github.io/log/blog/2025/rl-study-notes-l1/" rel="alternate" type="text/html" title="RL Study Notes — Lecture 1: MDP, Behavior Cloning, DAgger"/><published>2025-09-21T17:00:00+00:00</published><updated>2025-09-21T17:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2025/rl-study-notes-l1</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2025/rl-study-notes-l1/"><![CDATA[<h2 id="markov-decision-process-mdp">Markov Decision Process (MDP)</h2> <p>The standard formalism for RL: $\mathcal{M} = (\mathcal{S}, \mathcal{A}, P, R, \gamma)$</p> <ul> <li>$\mathcal{S}$: state space</li> <li>$\mathcal{A}$: action space</li> <li>$P(s’ \mid s, a)$: transition probability</li> <li>$R(s, a)$: reward</li> <li>$\gamma \in [0, 1)$: discount factor</li> </ul> <p><strong>Markov property</strong>: the future depends only on the <em>current</em> state-action pair, not the full history.</p> <h2 id="rl-objective">RL Objective</h2> <p>Maximize expected discounted return under policy $\pi$:</p> \[J(\pi) = \mathbb{E}_{\tau \sim \pi}\!\left[\sum_{t=0}^{\infty} \gamma^t r_t\right]\] <p>This measures long-run performance, with $\gamma$ trading off immediate vs. future reward.</p> <h2 id="value-functions">Value Functions</h2> <ul> <li><strong>State-value</strong>: $V^\pi(s) = \mathbb{E}_\pi[\sum_t \gamma^t r_t \mid s_0 = s]$ — how good is being in state $s$?</li> <li><strong>Action-value</strong>: $Q^\pi(s, a) = \mathbb{E}_\pi[\sum_t \gamma^t r_t \mid s_0 = s, a_0 = a]$ — how good is taking action $a$ in state $s$?</li> </ul> <p>Both satisfy <strong>Bellman equations</strong> linking immediate reward to discounted future value.</p> <h2 id="imitation-learning">Imitation Learning</h2> <p><strong>Behavior Cloning (BC)</strong>: supervised learning from expert demonstrations.</p> <ul> <li>Cross-entropy loss for discrete actions, squared error for continuous</li> <li><strong>Compounding error problem</strong>: small mistakes drift the agent into states the expert never visited, where the policy is undefined</li> </ul> <p><strong>DAgger (Dataset Aggregation)</strong>: iterative refinement</p> <ol> <li>Train policy on expert data</li> <li>Roll out the learned policy to collect <em>its own</em> state distribution</li> <li>Query expert for correct actions on those states</li> <li>Aggregate and retrain</li> </ol> <p>This progressively aligns training data with the learner’s actual deployment distribution, fixing BC’s distribution shift problem.</p> <p><em>(Optimal value functions and inverse RL deferred to a later lecture.)</em></p>]]></content><author><name></name></author><category term="notes"/><category term="rl"/><category term="rlhf"/><summary type="html"><![CDATA[Foundational concepts for reinforcement learning applied to language models — MDPs, value functions, and imitation learning.]]></summary></entry><entry><title type="html">HICRA: Hierarchical Credit Assignment for LLM Reasoning</title><link href="https://yjkim-stat.github.io/log/blog/2025/hicra/" rel="alternate" type="text/html" title="HICRA: Hierarchical Credit Assignment for LLM Reasoning"/><published>2025-09-21T16:00:00+00:00</published><updated>2025-09-21T16:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2025/hicra</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2025/hicra/"><![CDATA[<h2 id="key-finding">Key Finding</h2> <p>HICRA identifies that reinforcement learning improves LLM reasoning through an <strong>emergent two-phase hierarchy</strong>:</p> <ol> <li><strong>Early training</strong>: fixes low-level <em>procedural</em> tokens (arithmetic steps, formatting)</li> <li><strong>Later training</strong>: gains come from high-level <em>strategic planning</em> tokens (problem decomposition, approach selection)</li> </ol> <p>Standard GRPO distributes credit uniformly across all tokens, missing this hierarchy.</p> <h2 id="method">Method</h2> <p>Rather than uniform credit assignment, HICRA <strong>amplifies gradients on planning tokens</strong> — the actual bottleneck for reasoning improvement. This targeted credit assignment reflects where the model’s decisions actually matter.</p> <p>Evidence for the hierarchy:</p> <ul> <li>“Aha moments” cluster around strategic decision points, not procedural execution</li> <li>Length scaling patterns in RL training align with planning-token density</li> </ul> <h2 id="results">Results</h2> <ul> <li>Outperforms GRPO-based approaches on mathematical and multi-step reasoning tasks</li> <li>Integrates into various RL frameworks as an incremental modification</li> <li>Provides both an empirical observation and an actionable algorithm</li> </ul> <p><strong>Paper</strong>: <a href="https://arxiv.org/abs/2509.03646">arXiv:2509.03646</a></p>]]></content><author><name></name></author><category term="paper-review"/><category term="llm"/><category term="reasoning"/><category term="rl"/><summary type="html"><![CDATA[Emergent two-phase hierarchy in RL training — fix procedural tokens early, then optimize strategic planning tokens.]]></summary></entry></feed>