<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://yjkim-stat.github.io/log/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yjkim-stat.github.io/log/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-02T04:58:54+00:00</updated><id>https://yjkim-stat.github.io/log/feed.xml</id><title type="html">Research Log</title><subtitle>Paper reviews, recent research trends, and notes on machine learning research. </subtitle><entry><title type="html">WFM-TTS — Test-Time Scaling for World Foundation Models</title><link href="https://yjkim-stat.github.io/log/blog/2026/wfm-tts/" rel="alternate" type="text/html" title="WFM-TTS — Test-Time Scaling for World Foundation Models"/><published>2026-07-02T03:00:00+00:00</published><updated>2026-07-02T03:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/wfm-tts</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/wfm-tts/"><![CDATA[<p><strong>Paper.</strong> Wenyan Cong, Hanqing Zhu, Peihao Wang, Bangya Liu, Dejia Xu, Kevin Wang, David Z. Pan, Yan Wang, Zhiwen Fan, Zhangyang Wang. <em>Can Test-Time Scaling Improve World Foundation Model?</em> COLM 2025. UT Austin · UW-Madison · NVIDIA. <a href="https://arxiv.org/abs/2503.24320">[arXiv]</a> · <a href="https://scalingwfm.github.io/">[project page]</a> · <a href="https://github.com/VITA-Group/WFM-TTS">[code]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>World Foundation Models (WFMs) — video-generative models that predict future visual states given past observations and control inputs — have become load-bearing components in Physical AI stacks for autonomous driving and robotics. But they’re expensive to make better: pretraining costs are enormous, and post-training improvements are bottlenecked by curated paired data. This paper asks whether <strong>test-time compute scaling</strong> is a third path — one that has been transformative for LLM reasoning but has never been systematically applied to WFMs. The answer is yes: the authors introduce <strong>WFM-TTS</strong>, the first test-time scaling framework for WFMs. The central engineering challenge is that naive multi-candidate search is impractical over continuous video tokens with a diffusion-based decoder. WFM-TTS solves this with a replacement <strong>efficient tokenizer decoder that is ~9,000× faster</strong> than diffusion decoding, then applies <strong>beam search + top-K pruning</strong> guided by <strong>rule-based physical-quality rewards</strong>. A new <strong>five-dimension evaluation toolkit</strong> (3D consistency, temporal consistency, spatial awareness, perceptual quality, text-video alignment) provides the measurement infrastructure the field was missing. The headline result: a <strong>4B-parameter COSMOS model with WFM-TTS matches or outperforms a 12B-parameter baseline</strong>, and test-time scaling laws hold — more compute monotonically improves output quality.</p> <hr/> <h2 id="1-the-problem--wfms-are-hard-to-scale-easy-to-evaluate-poorly">1. The Problem — WFMs Are Hard to Scale, Easy to Evaluate Poorly</h2> <h3 id="11-why-wfms-matter">1.1 Why WFMs matter</h3> <p>A World Foundation Model takes a sequence of past observations (e.g., camera frames from an autonomous vehicle) and ego-action commands and predicts the next video frames — what the world will look like if the agent does those things. For physical AI:</p> <ul> <li><strong>Autonomous driving:</strong> the vehicle can “imagine” its trajectory before executing it.</li> <li><strong>Robotics:</strong> the robot can simulate proposed motions to check for collisions or physical plausibility before committing.</li> </ul> <p>This is the “planning by imagination” paradigm, and it scales directly with how good the WFM is.</p> <h3 id="12-the-scaling-wall">1.2 The scaling wall</h3> <p>WFMs are trained on massive video datasets with large compute budgets. Making them better has two levers:</p> <table> <thead> <tr> <th>Lever</th> <th>Cost</th> <th>Constraint</th> </tr> </thead> <tbody> <tr> <td>More pretraining data / compute</td> <td>Enormous</td> <td>Diminishing returns; data quality ceiling</td> </tr> <tr> <td>Post-training fine-tuning</td> <td>Moderate</td> <td>Bounded by curated paired (video, action) data</td> </tr> <tr> <td><strong>Test-time compute scaling</strong></td> <td>Per-query</td> <td><strong>Unexplored for WFMs</strong></td> </tr> </tbody> </table> <p>Test-time scaling — the idea behind LLM chain-of-thought and OpenAI o1 — trades inference compute for quality without retraining. It works for LLMs because you can generate multiple candidates and use reward models to pick the best. For WFMs the obvious extension is blocked by two problems: (1) diffusion-based decoders are far too slow for multi-candidate generation, and (2) no standardized reward or evaluation toolkit exists to score candidate videos systematically.</p> <p>WFM-TTS addresses both.</p> <h3 id="13-the-missing-evaluation-infrastructure">1.3 The missing evaluation infrastructure</h3> <p>A secondary problem: there’s no agreed metric for WFM output quality. FID and FVD measure frame quality and video quality respectively, but they don’t capture physical plausibility — whether the generated world is physically consistent in 3D, whether objects maintain their spatial relationships, whether the generated scene is temporally coherent across frames. Without those measures, you can’t run principled reward-guided search, and you can’t compare WFMs systematically.</p> <hr/> <h2 id="2-wfm-tts--the-framework">2. WFM-TTS — The Framework</h2> <p>WFM-TTS has three components: an efficient decoder, a search procedure, and a reward signal. The backbone model is <strong>NVIDIA COSMOS</strong> — an autoregressive WFM that operates over discrete video tokens.</p> <h3 id="21-the-efficient-tokenizer-decoder">2.1 The efficient tokenizer decoder</h3> <p>The standard COSMOS decoding pipeline reconstructs video frames from latent tokens using a diffusion-based decoder. This is accurate but extremely slow — slow enough that generating even a handful of candidate videos for comparison is computationally infeasible in a test-time search loop.</p> <p>WFM-TTS replaces the diffusion decoder with an <strong>efficient tokenizer decoder</strong> that:</p> <ul> <li>Maintains consistent quality trends relative to the diffusion decoder (same ordering of candidates by quality).</li> <li>Runs approximately <strong>9,000× faster</strong>.</li> </ul> <p>This speedup is what unlocks beam search. Without it, test-time scaling for WFMs is a dead end.</p> <h3 id="22-probability-based-top-k-pruning">2.2 Probability-based top-K pruning</h3> <p>At each generation step, the autoregressive WFM produces a distribution over possible next tokens. WFM-TTS generates K candidate continuations and prunes the lowest-scoring ones by token probability. Only the top-K beams survive to the next step.</p> <p>This is the standard beam-search pruning strategy, now made feasible by the fast decoder.</p> <h3 id="23-beam-search">2.3 Beam search</h3> <p>The search runs over the full discrete token space of the WFM:</p> <ol> <li>Expand each surviving beam to K children.</li> <li>Score each child using the efficient decoder + reward function.</li> <li>Keep the top-K children; discard the rest.</li> <li>Repeat until the full video sequence is generated.</li> </ol> <p>The output is the highest-scoring complete video sequence found during search.</p> <h3 id="24-rule-based-rewards">2.4 Rule-based rewards</h3> <p>The reward signal that guides beam selection is deliberately simple: <strong>rule-based physical quality metrics</strong> derived from measurable properties of the generated video. Examples include:</p> <ul> <li>Aesthetic quality score.</li> <li>Object permanence / temporal consistency score.</li> <li>3D geometric consistency (via CUT3R, a 3D reconstruction model).</li> </ul> <p>The paper compares rule-based rewards against learned preference models and finds that <strong>rule-based rewards consistently win</strong> on both stability and alignment with qualitative human preference. Learned rewards introduce noise; rule-based rewards are deterministic and directly tied to measurable physical properties.</p> <hr/> <h2 id="3-the-wfm-evaluation-toolkit">3. The WFM Evaluation Toolkit</h2> <p>Alongside the search framework, WFM-TTS introduces a <strong>five-dimension evaluation toolkit</strong> that serves as both the reward signal during search and the benchmark for comparing WFMs broadly.</p> <table> <thead> <tr> <th>Dimension</th> <th>What it measures</th> <th>How</th> </tr> </thead> <tbody> <tr> <td><strong>3D consistency</strong></td> <td>Geometric coherence of the generated scene</td> <td>CUT3R 3D reconstruction from the video</td> </tr> <tr> <td><strong>Temporal consistency</strong></td> <td>Smooth transitions; no object teleportation</td> <td>Frame-to-frame coherence metrics</td> </tr> <tr> <td><strong>Spatial relationship awareness</strong></td> <td>Correct relative positions of objects</td> <td>Object detection + spatial reasoning</td> </tr> <tr> <td><strong>Perceptual quality</strong></td> <td>Visual fidelity and aesthetics</td> <td>VBench / VideoScore</td> </tr> <tr> <td><strong>Text-video alignment</strong></td> <td>Generated video matches the action conditioning</td> <td>Cross-modal similarity</td> </tr> </tbody> </table> <p>Standard metrics (FID, FVD) are evaluated separately on the <strong>nuScenes</strong> and <strong>Waymo</strong> distributions, covering 900 test input videos (150 nuScenes scenes + 750 Waymo clips from 150 scenes × 5 samples).</p> <p>The toolkit is modular and is released as open-source infrastructure for WFM research — filling the measurement gap that had been blocking principled comparison of WFMs.</p> <hr/> <h2 id="4-results">4. Results</h2> <h3 id="41-test-time-scaling-law-holds-for-wfms">4.1 Test-time scaling law holds for WFMs</h3> <p>The paper’s central empirical finding:</p> <blockquote> <p>Performance on every evaluation metric increases monotonically as test-time compute (number of beam-search iterations, candidate count) increases — even in a compute-optimal (FLOPs-matched) setting.</p> </blockquote> <p>This is the WFM analog of the LLM test-time scaling law. It’s not obvious that it would hold, because WFMs operate over continuous-valued visual predictions rather than discrete language tokens. It does hold.</p> <h3 id="42-4b-matches-12b">4.2 4B matches 12B</h3> <p>The most striking headline:</p> <blockquote> <p><strong>COSMOS-4B + WFM-TTS ≥ COSMOS-12B (baseline, no TTS)</strong></p> </blockquote> <p>A model with 3× fewer parameters, given more inference-time compute via WFM-TTS, matches or outperforms the larger model on overall quality. Human evaluators prefer the WFM-TTS-enhanced outputs.</p> <p>This is the same compute-optimal crossover point seen in LLM scaling — you can substitute inference compute for model capacity, and at some point the substitution is favorable.</p> <h3 id="43-quantitative-gains">4.3 Quantitative gains</h3> <p>Overall improvements of <strong>6–12% across evaluation dimensions</strong> relative to greedy/baseline decoding (no test-time search).</p> <h3 id="44-rule-based--preference-based-rewards">4.4 Rule-based &gt; preference-based rewards</h3> <p>Across all settings, rule-based reward functions outperform learned preference models for beam search guidance. The preference models introduce calibration noise; the rule-based rewards are stable and physically grounded.</p> <hr/> <h2 id="5-the-broader-context">5. The Broader Context</h2> <h3 id="51-connection-to-llm-test-time-scaling">5.1 Connection to LLM test-time scaling</h3> <p>The LLM test-time scaling research established that “more compute at inference = better outputs” when search is <em>structured</em> — not random sampling but principled search with evaluation signals. WFM-TTS is the first paper to extend this to physical world modeling, establishing that the same principle applies across modalities.</p> <p>The key enabling difference: language tokens are cheap to decode; video frames from diffusion are not. The 9,000× speedup in the efficient decoder is therefore the structural prerequisite for the entire framework, the way softmax temperature is the prerequisite for language model sampling.</p> <h3 id="52-relation-to-dreamgen-and-dreamzero">5.2 Relation to DreamGen and DreamZero</h3> <p>In the robotics world-model lineage reviewed on this site (<a href="/log/blog/2026/dreamgen/">DreamGen</a>, <a href="/log/blog/2026/dreamzero/">DreamZero</a>), the WFM is used to generate synthetic trajectories or as a policy backbone. WFM-TTS is upstream of both those use cases: it makes the WFM better at the video-generation task itself, without changing the architecture or training. Better WFM → better synthetic trajectories for DreamGen → better downstream policies. Better WFM → better predictive features for VPP-style backbones.</p> <p>This is the “test-time compute as a lever for WFM quality” argument; the downstream benefits to the robotics pipeline are a corollary.</p> <hr/> <h2 id="6-why-it-matters">6. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>It establishes test-time scaling as a first-class lever for WFMs.</strong> Until this paper, test-time compute scaling was LLM territory. WFM-TTS shows that physical world modeling obeys the same scaling law, provided you solve the decoder-speed bottleneck. The implication: the “more inference compute → better quality” recipe now spans language reasoning and physical simulation.</li> <li><strong>The efficient decoder is the enabling contribution.</strong> A 9,000× speedup that maintains quality ordering is nontrivial engineering. Without it, beam search over WFM token spaces is impractical and the rest of the framework doesn’t exist. This is a reusable component for anyone working with autoregressive tokenized WFMs.</li> <li><strong>The evaluation toolkit fills a genuine gap.</strong> FID and FVD were borrowed from image generation and video generation respectively — they don’t measure physical plausibility. A toolkit that captures 3D consistency, temporal coherence, and spatial relationship awareness is necessary infrastructure for the WFM research community.</li> </ol> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>COSMOS-specific.</strong> All experiments use NVIDIA COSMOS at 4B and 12B scale. Whether the approach extends to diffusion-based WFMs (rather than autoregressive tokenized WFMs) requires different search designs.</li> <li><strong>Autonomous driving focus.</strong> Evaluation datasets are nuScenes and Waymo — driving-centric. Robotics scenarios are mentioned but not quantitatively evaluated. The rewards and toolkit metrics may need domain adaptation for manipulation tasks.</li> <li><strong>Rule-based rewards have a quality ceiling.</strong> They are stable and physically grounded, but they measure specific observable properties. Tasks requiring richer goal-conditioned evaluation (did the agent accomplish a specific behavior?) may need learned rewards anyway.</li> <li><strong>Beam search still multiplies inference cost.</strong> Even with the 9,000× speedup, keeping K beams active multiplies per-query compute by K. At large K, the practical deployment cost is non-trivial. The paper doesn’t fully analyze latency-quality Pareto curves under production constraints.</li> <li><strong>Offline evaluation.</strong> Quality is assessed on fixed test sets from nuScenes/Waymo. Closed-loop driving evaluations — where the generated future feeds back into planning decisions — are not included.</li> </ul> <hr/> <h2 id="8-the-takeaway-for-a-first-reader">8. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>World Foundation Models are expensive to improve by retraining.</strong> WFM-TTS asks whether <strong>test-time compute scaling</strong> is an alternative. It is: test-time scaling laws hold for WFMs (monotonically improving quality with compute) even in a compute-optimal (FLOPs-matched) regime.</li> <li><strong>The key enabling technology is a ~9,000× faster efficient tokenizer decoder</strong> that replaces slow diffusion decoding and makes multi-candidate beam search over WFM token spaces practical. On top of this, <strong>rule-based physical-quality rewards</strong> guide beam selection more stably than learned preference models.</li> <li><strong>The headline result: COSMOS-4B + WFM-TTS ≥ COSMOS-12B (baseline)</strong>, with overall 6–12% quality gains. The companion <strong>five-dimension WFM evaluation toolkit</strong> (3D consistency, temporal consistency, spatial awareness, perceptual quality, text-video alignment) fills a measurement gap in the field.</li> </ol> <p>That’s the arc: WFMs can’t be improved cheaply by retraining → test-time scaling is the unexplored lever → fast decoder makes beam search feasible → rule-based rewards guide search → smaller model beats larger model on physical-quality metrics.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Cong, W., Zhu, H., Wang, P., Liu, B., Xu, D., Wang, K., Pan, D. Z., Wang, Y., Fan, Z., &amp; Wang, Z. (2025). <em>Can Test-Time Scaling Improve World Foundation Model?</em> COLM 2025. <a href="https://arxiv.org/abs/2503.24320">arXiv:2503.24320</a>.</li> <li>Project page: <a href="https://scalingwfm.github.io/">https://scalingwfm.github.io/</a></li> <li>Code: <a href="https://github.com/VITA-Group/WFM-TTS">https://github.com/VITA-Group/WFM-TTS</a></li> <li>COSMOS backbone: NVIDIA COSMOS (Agarwal et al., 2025).</li> <li>CUT3R (3D consistency evaluator): <a href="https://arxiv.org/abs/2501.12369">https://arxiv.org/abs/2501.12369</a>.</li> <li>VBench / VideoScore: video quality evaluation benchmarks.</li> <li>nuScenes / Waymo: autonomous driving video datasets.</li> <li>Related on this site: <a href="/log/blog/2026/dreamgen/">DreamGen paper review</a> — WFM as a data factory for robot trajectory generation; <a href="/log/blog/2026/dreamzero/">DreamZero paper review</a> — WFM as the policy backbone for physical generalization; <a href="/log/blog/2026/vpp/">VPP paper review</a> — WFM internal features as a predictive backbone for robot policies.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="world-models"/><category term="autonomous-driving"/><category term="video-generation"/><category term="inference-time-compute"/><category term="test-time-scaling"/><summary type="html"><![CDATA[Paper review of "Can Test-Time Scaling Improve World Foundation Model?" (Cong, Zhu et al., COLM 2025) — the first framework that applies test-time compute scaling to video-generative world models. A 9,000x-faster efficient decoder enables beam search + top-K pruning over the token space, rule-based rewards guide selection, and a 4B COSMOS model with WFM-TTS matches a 12B baseline. Introduces a new five-dimension WFM evaluation toolkit (3D consistency, temporal coherence, spatial awareness, perceptual quality, text-video alignment).]]></summary></entry><entry><title type="html">Monte Carlo Tree Diffusion — System 2 Planning with Diffusion Models</title><link href="https://yjkim-stat.github.io/log/blog/2026/mctd/" rel="alternate" type="text/html" title="Monte Carlo Tree Diffusion — System 2 Planning with Diffusion Models"/><published>2026-07-02T01:00:00+00:00</published><updated>2026-07-02T01:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/mctd</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/mctd/"><![CDATA[<p><strong>Paper.</strong> Jaesik Yoon, Hyeonseo Cho, Doojin Baek, Yoshua Bengio, Sungjin Ahn. <em>Monte Carlo Tree Diffusion for System 2 Planning.</em> ICML 2025 Spotlight. KAIST · SAP · Mila · NYU. <a href="https://arxiv.org/abs/2502.07202">[arXiv]</a> · <a href="https://sites.google.com/view/mctd-s2planning/home">[project page]</a> · <a href="https://github.com/ahn-ml/mctd">[code]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Diffusion models have emerged as capable offline planners — they can generate whole trajectories toward a goal by running a denoising process over a trajectory representation. But they are fundamentally one-shot samplers: giving a diffusion planner <em>more inference-time compute</em> (more samples, more steps) doesn’t reliably improve outcomes. Monte Carlo Tree Search (MCTS) has the opposite property — it was built to improve with compute by systematically exploring a search tree. <strong>MCTD</strong> bridges the two by reconceptualizing the denoising process as a tree-structured rollout. Partial denoising states become tree nodes; binary guidance schedules (guided vs. unguided sampling) become meta-actions that MCTS selects among; fast DDIM jumps stand in for cheap rollout simulations; and standard UCT backpropagation updates node values after each simulated plan is scored by a reward model. The result is a diffusion planner that can actually use extra compute — performance scales monotonically with the number of tree iterations, and on long-horizon maze tasks where all diffusion baselines collapse, MCTD achieves near-perfect success.</p> <hr/> <h2 id="1-the-problem--diffusion-planners-cant-scale-with-compute">1. The Problem — Diffusion Planners Can’t Scale with Compute</h2> <p>Offline diffusion planners (Diffuser and descendants) are trained on trajectory datasets to denoise random noise into a complete trajectory from current state to goal. This is powerful: you get behavioral diversity for free, planning is just sampling, and the whole thing is tractable to train.</p> <p>The weakness appears on hard long-horizon tasks. There are two obvious ways to give a diffusion planner more inference-time compute:</p> <table> <thead> <tr> <th>Strategy</th> <th>What it does</th> <th>Does it help?</th> </tr> </thead> <tbody> <tr> <td>More denoising steps</td> <td>Finer denoising trajectory</td> <td>Marginal or no gain</td> </tr> <tr> <td>Random search</td> <td>Sample N trajectories, pick best</td> <td>No systematic improvement</td> </tr> <tr> <td>Replanning</td> <td>Re-run the planner periodically</td> <td>Minor; not well-coordinated</td> </tr> </tbody> </table> <p>None of these strategies are <em>adaptive</em> — they don’t use the result of one computation to guide the next. In MCTS terms, they explore blindly rather than accumulating evidence about which branches are promising. For easy tasks this doesn’t matter (the single shot works). For hard tasks — long-horizon navigation through a complex maze — it means performance plateaus at the quality of the first sample.</p> <p>The “System 2” framing in the title is precise: System 1 thinking is fast and intuitive (single-shot diffusion), System 2 is slow and deliberate (MCTS-guided search). Diffusion planners have been stuck in System 1 mode. MCTD adds the System 2 loop.</p> <hr/> <h2 id="2-the-move--instantiate-mcts-inside-the-denoising-process">2. The Move — Instantiate MCTS Inside the Denoising Process</h2> <p>MCTS is defined by four steps: <strong>Select</strong> → <strong>Expand</strong> → <strong>Simulate</strong> → <strong>Backpropagate</strong>. MCTD maps each step onto something the diffusion planner can do natively.</p> <p>The key insight: the trajectory is divided into a sequence of temporally contiguous <strong>subplans</strong>. A partially denoised state — where some subplans have been denoised and others remain noisy — is a natural tree node. Children of a node are different ways to continue denoising (from the same partial state).</p> <hr/> <h2 id="3-the-three-core-mechanisms">3. The Three Core Mechanisms</h2> <h3 id="31-subplan-decomposition--tree-nodes">3.1 Subplan decomposition → tree nodes</h3> <p>Divide the full planned trajectory into $K$ subplan segments in time. Each tree node corresponds to a specific combination of (subplan index, partial denoising state). Deeper nodes have more subplans resolved; leaf nodes have a fully denoised trajectory.</p> <p>This decomposition is what makes tree branching meaningful: you can branch on <em>which future you commit to for the first subplan</em>, hold that fixed, and then explore alternatives for the second subplan. Without decomposition, branching mid-denoising would produce incoherent trajectories.</p> <h3 id="32-guidance-levels--meta-actions">3.2 Guidance levels → meta-actions</h3> <p>At each expansion, MCTD must decide <em>how</em> to continue denoising the next subplan. The available meta-actions are:</p> <ul> <li><strong>Guided (1):</strong> denoise under the goal-conditioned guidance signal → exploitation, pulls toward the known goal.</li> <li><strong>Unguided (0):</strong> denoise from the unconditional prior → exploration, diversifies the candidate futures.</li> </ul> <p>These guidance levels form the action space MCTS selects among. Node selection follows standard <strong>UCT</strong>:</p> \[a^* = \arg\max_a \left[ Q(s, a) + C \sqrt{\frac{\ln N(s)}{N(s,a)}} \right],\] <p>where $Q(s, a)$ is the mean reward of subtrees reached via action $a$ from node $s$, $N(s)$ is the visit count of $s$, and $C$ is the exploration constant. High UCT value = promising branch with remaining uncertainty.</p> <p>The binary guidance meta-action is the minimum useful abstraction: it’s coarse enough for MCTS to reason over efficiently, but it produces meaningfully different trajectories (exploitation vs. exploration) at each step. The full <strong>guidance schedule</strong> — $(g_1, g_2, \ldots, g_K)$ — is what MCTS is really searching over.</p> <h3 id="33-jumpy-denoising--simulation-oracle">3.3 Jumpy denoising → simulation oracle</h3> <p>After expanding a node, MCTD needs to evaluate it — to score “how good is this partial plan likely to be?” Running the full denoising chain to completion is too slow to do at every node. Instead, MCTD uses <strong>DDIM</strong> (which allows arbitrary step-size skips) to jump from the current partial denoising state to a fully denoised trajectory in a fraction of the steps.</p> <p>This “jumpy” simulation is an approximation, but it’s cheap and good enough for backpropagation — the reward model evaluates the fast-simulated complete trajectory and returns a scalar score.</p> <h3 id="34-backpropagation">3.4 Backpropagation</h3> <p>After simulation, the reward is backpropagated up the tree to update $Q(s, a)$ estimates for all ancestor nodes. The guidance schedule along the winning path accumulates positive weight; schedules along low-reward paths are down-weighted. Future selection iterations draw on this accumulated evidence.</p> <p>This is standard MCTS backpropagation — the contribution is that the tree structure and action space are well-defined enough for it to work inside a diffusion process.</p> <hr/> <h2 id="4-what-mctd-actually-buys">4. What MCTD Actually Buys</h2> <p>The argument for the method can be stated in one sentence:</p> <blockquote> <p>Diffusion planners were failing on hard tasks because good plans require coordinated decisions across subplan segments — decisions that can’t be made well in a single shot. MCTD gives the planner a way to <em>try</em> a commitment for the first subplan, <em>evaluate</em> consequences via fast rollout, and <em>revise</em> if the consequences look bad — the same feedback loop MCTS uses for any sequential decision problem.</p> </blockquote> <p>The analogy to chess engines is exact: a single-shot diffusion sample is like playing the first move that looks locally good. MCTD is like running a minimax search to see how the game evolves before committing.</p> <hr/> <h2 id="5-experiments">5. Experiments</h2> <h3 id="51-benchmarks">5.1 Benchmarks</h3> <p>MCTD is evaluated on <strong>OGBench</strong> (Offline Goal-Conditioned RL Benchmark) across:</p> <ul> <li><strong>PointMaze</strong> — navigation for a point-mass robot in mazes: Medium, Large, Giant.</li> <li><strong>AntMaze</strong> — same layouts with a complex ant robot (higher DOF).</li> <li><strong>Visual PointMaze</strong> — same task but with RGB image observations instead of state, testing robustness to partial observability.</li> </ul> <p>The backbone model is <strong>Diffuser</strong> (Janner et al., 2022) — a transformer diffusion model trained offline on trajectory datasets. The MCTS layer is inference-time only; no additional training is needed.</p> <h3 id="52-baselines">5.2 Baselines</h3> <table> <thead> <tr> <th>Baseline</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td>Diffuser</td> <td>Single-shot denoising plan</td> </tr> <tr> <td>Diffuser-Replanning</td> <td>Re-runs Diffuser at fixed intervals</td> </tr> <tr> <td>Diffuser-Random Search</td> <td>Samples N random trajectories, picks best by reward</td> </tr> <tr> <td>Diffusion Forcing</td> <td>Autoregressive sequential diffusion model</td> </tr> </tbody> </table> <p>Diffuser-Random Search is the most important baseline: it uses the same total denoising budget as MCTD but allocates it as parallel random samples rather than a guided tree search. Its performance determines whether the gain is from <em>more compute</em> or <em>better use of compute</em>.</p> <h3 id="53-results">5.3 Results</h3> <p><strong>PointMaze and AntMaze:</strong></p> <p>All standard diffusion baselines (Diffuser, Diffuser-Replanning, Diffusion Forcing) suffer dramatic performance drops as maze size increases from Medium → Large → Giant. Diffuser-Random Search performs no better than single-shot Diffuser, confirming that the issue is not compute quantity but compute quality.</p> <p>MCTD achieves near-perfect success across all maze sizes and robot morphologies. On PointMaze-Giant — the hardest task — MCTD reaches ~100% success while all baselines fail.</p> <p><strong>Inference-time scaling:</strong></p> <p>MCTD success rate increases monotonically with the number of tree iterations. This is the key property: unlike every diffusion baseline, MCTD has a genuine “use more compute → get better plans” lever. This is what the paper means by System 2 planning.</p> <p><strong>Visual PointMaze:</strong></p> <p>MCTD outperforms both Diffuser and Diffusion Forcing on the image-observation variant, showing that the tree-search structure adds robustness even when the input is partial/visual.</p> <h3 id="54-planning-time-cost">5.4 Planning time cost</h3> <p>The compute cost is real. Vanilla MCTD requires 8–40× more wall-clock time than a single-shot Diffuser, depending on maze size. On PointMaze-Giant, this reaches ~264 seconds per plan.</p> <p>This is addressed by a follow-up (<strong>Fast-MCTD</strong>, NeurIPS 2025) that introduces parallel tree rollouts with delayed updates and trajectory coarsening, recovering ~100× speedup while maintaining 100% success. That’s a downstream engineering result, not in this paper, but it shows the approach is practically viable.</p> <hr/> <h2 id="6-the-taxonomy-connection">6. The Taxonomy Connection</h2> <p>MCTD fits into the growing literature on <strong>inference-time scaling for decision-making</strong>:</p> <table> <thead> <tr> <th>Paper/system</th> <th>How it uses extra compute</th> </tr> </thead> <tbody> <tr> <td>Best-of-N sampling</td> <td>Random independent draws, pick max-reward</td> </tr> <tr> <td>Process Reward Models</td> <td>Score intermediate steps, rerank</td> </tr> <tr> <td>Tree-of-Thought / MCTS for LLMs</td> <td>Token-level tree search with language priors</td> </tr> <tr> <td><strong>MCTD</strong></td> <td>Tree search <em>inside</em> the denoising trajectory</td> </tr> </tbody> </table> <p>The distinctive feature of MCTD is that the “tree” is not over language tokens or high-level plans — it’s over <em>partially denoised trajectory states</em>. This is a lower level of abstraction, which makes each node geometrically meaningful and enables the fast-DDIM simulation trick.</p> <hr/> <h2 id="7-why-it-matters">7. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>It breaks the “one-shot” ceiling on diffusion planners.</strong> Until MCTD, running a diffusion planner with more compute didn’t help beyond a point. The inference-time scaling property is a new lever that diffusion-based planning didn’t have.</li> <li><strong>The MCTS-to-diffusion mapping is clean and reusable.</strong> The three mechanisms — subplan nodes, guidance meta-actions, jumpy simulation — are not specific to maze navigation. Any offline diffusion planner with a reward model can potentially apply the same wrapper.</li> <li><strong>It validates the System 2 framing for generative planners.</strong> The pattern — slow deliberate search over a space of possible futures, guided by evaluation of fast cheap simulations — is the same pattern AlphaGo used for Go and LLM tree-of-thought methods use for reasoning. MCTD is the trajectory-generation version of that pattern.</li> </ol> <hr/> <h2 id="8-limitations-worth-knowing">8. Limitations Worth Knowing</h2> <ul> <li><strong>Reward model dependency.</strong> Simulation scoring depends on a pre-trained reward model. If the reward model is inaccurate, backpropagation corrupts the search tree. There’s no mechanism to detect or correct for reward-model errors.</li> <li><strong>Offline training only.</strong> The diffusion backbone is trained on a fixed offline dataset; MCTD does not interact with the environment during search. Distributional shift between the offline data and the actual environment is not addressed.</li> <li><strong>Coarse guidance action space.</strong> Binary guided/unguided is the minimum meaningful action space. More expressive guidance schedules (continuous guidance strength, per-timestep variation) might improve performance but would also make the MCTS branching factor harder to manage.</li> <li><strong>Approximation error in jumpy denoising.</strong> DDIM simulation introduces approximation error relative to full denoising. This is acceptable in practice but can cause mispricing of nodes in subtler tasks.</li> <li><strong>No online replanning integration.</strong> MCTD plans a full trajectory offline before execution begins. There’s no mechanism for closed-loop real-time replanning as the environment evolves (though this is an obvious future extension).</li> </ul> <hr/> <h2 id="9-the-takeaway-for-a-first-reader">9. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Diffusion planners are one-shot samplers.</strong> More compute (random sampling, replanning) doesn’t help because there’s no adaptive feedback loop. MCTD adds one by embedding <strong>MCTS inside the denoising process</strong>: partially denoised subplans are tree nodes, binary guidance schedules are meta-actions, and fast DDIM jumps are the cheap simulation oracle.</li> <li><strong>UCT governs node selection</strong>, backpropagation updates value estimates from simulated plan rewards, and performance scales monotonically with inference-time compute — the defining property of System 2 planning that all diffusion baselines lack.</li> <li><strong>On hard long-horizon maze tasks</strong>, MCTD achieves ~100% success where all diffusion baselines fail. Diffuser-Random Search — the same compute budget, allocated randomly — shows no improvement over single-shot Diffuser, confirming the gain is from structured search, not more samples.</li> </ol> <p>That’s the arc: diffusion planners lack inference-time scaling → MCTS has it by construction → embed MCTS inside denoising via subplan nodes + guidance actions + jumpy simulation → unlock systematic compute scaling for diffusion-based planning.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Yoon, J., Cho, H., Baek, D., Bengio, Y., &amp; Ahn, S. (2025). <em>Monte Carlo Tree Diffusion for System 2 Planning.</em> ICML 2025 Spotlight. <a href="https://arxiv.org/abs/2502.07202">arXiv:2502.07202</a>.</li> <li>Project page: <a href="https://sites.google.com/view/mctd-s2planning/home">https://sites.google.com/view/mctd-s2planning/home</a></li> <li>Code: <a href="https://github.com/ahn-ml/mctd">https://github.com/ahn-ml/mctd</a></li> <li>Janner, M., Du, J., Tenenbaum, J., &amp; Levine, S. (2022). <em>Planning with Diffusion Models.</em> NeurIPS 2022. (Diffuser backbone.)</li> <li>Song, J., Meng, C., &amp; Ermon, S. (2021). <em>Denoising Diffusion Implicit Models.</em> ICLR 2021. (DDIM simulation oracle.)</li> <li>Fast-MCTD: <a href="https://arxiv.org/abs/2506.09498">arXiv:2506.09498</a> (follow-up, ~100× speedup via parallel rollouts and coarsening, NeurIPS 2025).</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="planning"/><category term="diffusion"/><category term="mcts"/><category term="offline-rl"/><category term="inference-time-compute"/><summary type="html"><![CDATA[Paper review of Monte Carlo Tree Diffusion (Yoon, Cho et al., ICML 2025 Spotlight) — a framework that embeds MCTS inside the diffusion denoising process, treating partially denoised subplans as tree nodes, binary guidance schedules as meta-actions, and fast DDIM rollouts as the simulation oracle. Near-perfect success on long-horizon maze tasks where all diffusion baselines fail, with monotonically improving performance as inference compute scales.]]></summary></entry><entry><title type="html">Video Prediction Policy — Predictive Visual Representations as the Policy Backbone</title><link href="https://yjkim-stat.github.io/log/blog/2026/vpp/" rel="alternate" type="text/html" title="Video Prediction Policy — Predictive Visual Representations as the Policy Backbone"/><published>2026-06-13T02:00:00+00:00</published><updated>2026-06-13T02:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/vpp</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/vpp/"><![CDATA[<p><strong>Paper.</strong> Yucheng Hu, Yanjiang Guo, Pengchao Wang, Xiaoyu Chen, Yen-Jen Wang, Jianke Zhang, Koushil Sreenath, Chaochao Lu, Jianyu Chen. <em>Video Prediction Policy: A Generalist Robot Policy with Predictive Visual Representations.</em> ICML 2025 Spotlight / arXiv:2412.14803, Dec 2024. Tsinghua · UC Berkeley · RobotEra · Shanghai AI Lab · Shanghai Qi Zhi Institute. <a href="https://arxiv.org/abs/2412.14803">[arXiv]</a> · <a href="https://video-prediction-policy.github.io/">[project page]</a> · <a href="https://github.com/roboterax/video-prediction-policy">[code]</a> · <a href="https://openreview.net/forum?id=c0dhw1du33">[OpenReview]</a></p> <blockquote> <p>📑 <strong>Companion slide deck.</strong> A self-contained HTML walk-through of this review (in Korean) is available at <a href="/log/assets/paper_review_html_slides/VPP%20%EB%85%BC%EB%AC%B8%20%ED%95%B4%EC%84%A4%20(standalone).html">VPP 논문 해설 (standalone).html</a>. The deck and this post share the same structure; the post is the long-form English version.</p> </blockquote> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Generalist robot policies need a visual backbone — something that turns a camera image into a feature vector the action head can operate on. The dominant backbones (R3M, MVP, DINO-v2, CLIP) are pretrained with <em>single-image reconstruction</em> or <em>two-image contrastive</em> objectives. These produce <strong>static</strong> features: they encode what’s currently in the scene, but they don’t encode <em>what’s about to happen</em>. For embodied control, that’s the wrong shape — the policy needs <em>predictive</em> features. VPP’s argument is that modern <strong>video diffusion models</strong> already carry predictive features inside them as a side effect of being trained to generate the next frames, and these features can be used directly as the backbone of a robot policy. The recipe is two parts. (1) Fine-tune a general-purpose video diffusion foundation model on a mix of robot data and internet human manipulation video to make it a <strong>text-conditioned manipulation video predictor</strong>. (2) Take the <em>internal representations</em> of that fine-tuned video model — not the generated video, but the intermediate features — as the conditioning signal for a Diffusion Transformer action head, which learns an <strong>implicit inverse-dynamics mapping</strong> from “predicted future representation” to “action that gets you there.” The result: <strong>+41.5% relative improvement on Calvin ABC-D</strong> generalization vs. previous SOTA, an average task length of <strong>4.33</strong> on Calvin ABC, <strong>+31.6%</strong> on complex real-world dexterous manipulation, and a <strong>single VPP policy</strong> solving <strong>100+ tasks</strong> on a real-world dexterous hand.</p> <hr/> <h2 id="1-the-problem--static-features-dont-carry-dynamics">1. The Problem — Static Features Don’t Carry Dynamics</h2> <p>The standard “vision encoder for robot policy” recipe pretrains a backbone with one of two objectives:</p> <table> <thead> <tr> <th>Family</th> <th>Pretraining objective</th> <th>What it captures</th> </tr> </thead> <tbody> <tr> <td>R3M, MVP, time-contrastive</td> <td>contrastive learning on video clips</td> <td>semantic / spatial similarity</td> </tr> <tr> <td>DINO-v2, MAE, CLIP</td> <td>reconstruction / self-supervised on still images</td> <td>static scene content</td> </tr> </tbody> </table> <p>These backbones produce <strong>good static features</strong>: “what’s in the image right now.” But embodied control is fundamentally a <em>temporal</em> problem. The right feature for “should I move left or right?” is not just “there’s a cup at (x, y) in the current frame” — it’s “the cup is currently here, and if I act, it will be there.” That <em>predictive</em> component is what static encoders miss.</p> <p>The structural complaint VPP makes: <strong>the backbone has been optimized for the wrong task</strong>. A backbone optimized to predict the <em>future</em> frame is a backbone that has, by construction, internalized the dynamics that policy learning needs.</p> <hr/> <h2 id="2-the-move--video-diffusion-as-a-predictive-feature-extractor">2. The Move — Video Diffusion as a Predictive Feature Extractor</h2> <p>Video diffusion models (VDMs) are trained to generate the next $K$ frames given prior frames and a text instruction. To do that well, they have to internalize:</p> <ul> <li><strong>Object permanence and trajectories</strong> — objects move continuously, don’t teleport.</li> <li><strong>Contact and articulation</strong> — fingers close around handles, cups tilt under gravity.</li> <li><strong>Causality</strong> — actions implied by the prompt produce specific visual consequences.</li> </ul> <p>This is exactly the dynamics knowledge robot policies need. VPP’s bet is that <strong>you don’t need the VDM to actually generate the video at policy inference time</strong> — its <em>intermediate features</em> already carry the predictive information.</p> <p>The mental shift: <strong>the video model is not used as a generator at deployment. It is used as a feature extractor whose features happen to encode predicted dynamics.</strong> The action head reads those features and emits actions.</p> <hr/> <h2 id="3-the-architecture">3. The Architecture</h2> <p>VPP has two trainable components plus one frozen-after-fine-tune backbone.</p> <h3 id="31-component-1--the-video-prediction-model">3.1 Component 1 — The video prediction model</h3> <p>Start from a general-purpose video diffusion foundation model (image-to-video). Fine-tune on:</p> <ul> <li>Robot teleoperation data (the target embodiment’s videos).</li> <li>Internet human manipulation video (broad action prior).</li> </ul> <p>The fine-tuned model becomes a <strong>text-conditioned manipulation video predictor</strong>: given a current frame and a language instruction, it can predict how the scene will evolve.</p> <p>This is the same first step in <a href="/log/blog/2026/dreamgen/">DreamGen</a> — the generative model is adapted to the embodiment. The difference is what comes next.</p> <h3 id="32-component-2--the-action-head">3.2 Component 2 — The action head</h3> <p>A <strong>Diffusion Transformer (DiT) policy</strong> that:</p> <ul> <li>Reads the <strong>internal features</strong> of the video prediction model (not the generated video — the intermediate activations) as conditioning.</li> <li>Reads the current observation and the language instruction.</li> <li>Outputs an action chunk via standard diffusion-policy denoising.</li> </ul> <p>The action head is small relative to the VDM. It’s the only part that touches the robot’s action space.</p> <h3 id="33-implicit-inverse-dynamics">3.3 Implicit inverse dynamics</h3> <p>The action head learns an <strong>implicit inverse-dynamics mapping</strong>:</p> <blockquote> <p>Given the VDM’s predicted next-state representation (implicitly encoded in its features), output the action that, when executed, will actually reach that state.</p> </blockquote> <p>This is the same structural pattern as <a href="/log/blog/2026/lapa/">LAPA</a>’s inverse-dynamics trick, but at the <em>feature</em> level instead of the <em>frame</em> level. LAPA’s IDM read two frames and produced a discrete code; VPP’s implicit IDM reads the VDM’s predictive features and produces a continuous action. Both are recovering the action implicit in a visual prediction.</p> <h3 id="34-the-inference-time-efficiency-trick">3.4 The inference-time efficiency trick</h3> <p>A naive use of a VDM at inference would require generating the full video — slow, with many denoising steps. VPP sidesteps this:</p> <ul> <li>The features used for conditioning are read <strong>early in the denoising trajectory</strong> (or even from a single forward pass), so the policy doesn’t wait for a full clean video to be sampled.</li> <li>The DiT action head itself runs on these features, so the full-video generative cost is avoided.</li> </ul> <p>This is what makes VPP usable as a real-time policy despite a heavy video-model backbone.</p> <hr/> <h2 id="4-why-predictive-features-beat-static-ones">4. Why Predictive Features Beat Static Ones</h2> <p>The intuition the paper formalizes:</p> <blockquote> <p>A frame’s static features describe <strong>what is</strong>. The internal features of a video diffusion model trained to predict the next frames describe <strong>what is</strong> <em>and</em> <strong>what will be</strong>. For inverse- dynamics-style action learning, you need both.</p> </blockquote> <p>A static-feature backbone forces the policy head to do all the dynamics modeling on its own — from limited demonstration data. A predictive-feature backbone <em>gives</em> the policy head a head start: the dynamics are already implicit in the conditioning signal, distilled from internet-scale video pretraining.</p> <p>The mechanism is the same one <a href="/log/blog/2026/dreamzero/">DreamZero</a> leans on at the <em>backbone</em> level — internet video is a dense, scalable source of physical dynamics, and a model trained on it inherits a usable world prior. VPP’s contribution is showing that you don’t need to collapse the action head into the video backbone (as DreamZero does later); a clean separation where the VDM is <em>just</em> a feature extractor already wins decisively.</p> <hr/> <h2 id="5-headline-results">5. Headline Results</h2> <h3 id="51-calvin-abc-d-sim-generalization">5.1 Calvin ABC-D (sim, generalization)</h3> <table> <thead> <tr> <th>Metric</th> <th>VPP vs. previous SOTA</th> </tr> </thead> <tbody> <tr> <td>Relative improvement</td> <td><strong>+41.5%</strong></td> </tr> <tr> <td>Average task length on Calvin ABC</td> <td><strong>4.33</strong></td> </tr> </tbody> </table> <p>Calvin ABC-D measures generalization to unseen task combinations across rooms. The 41.5% relative gain is the headline benchmark result.</p> <h3 id="52-real-world-dexterous-manipulation">5.2 Real-world dexterous manipulation</h3> <table> <thead> <tr> <th>Metric</th> <th>Result</th> </tr> </thead> <tbody> <tr> <td>Success rate improvement vs. baselines</td> <td><strong>+31.6%</strong></td> </tr> <tr> <td>Real-world tasks solved by a single VPP policy</td> <td><strong>100+</strong> on dexterous hand</td> </tr> </tbody> </table> <p>The dexterous-hand result is the most striking. A <em>single policy</em> solving 100+ tasks on a high-DOF hand is the kind of generality result that, until recently, required heavy multi-task pretraining on per-task labeled data. VPP gets it from the predictive-feature conditioning.</p> <h3 id="53-evaluation-breadth">5.3 Evaluation breadth</h3> <ul> <li><strong>Simulated:</strong> Calvin, MetaWorld.</li> <li><strong>Real:</strong> Panda arm manipulation, XHand dexterous hand manipulation.</li> </ul> <p>The cross-platform consistency of the gains supports the claim that the predictive-feature backbone — not benchmark-specific tuning — is what’s doing the work.</p> <hr/> <h2 id="6-the-taxonomy-of-using-a-video-model-for-robot-learning">6. The Taxonomy of Using a Video Model for Robot Learning</h2> <p>Reading VPP alongside the three NVIDIA GEAR papers reviewed on this site produces a clean taxonomy of how a modern video model can power robot learning. Each paper uses the video model at a different layer of the pipeline:</p> <table> <thead> <tr> <th>Paper</th> <th>The video model is used as…</th> </tr> </thead> <tbody> <tr> <td><a href="/log/blog/2026/lapa/">LAPA</a></td> <td>a <strong>label extractor</strong> — encodes frame pairs as discrete latent action codes for VLA pretraining</td> </tr> <tr> <td><a href="/log/blog/2026/dreamgen/">DreamGen</a></td> <td>a <strong>data factory</strong> — dreams synthetic videos for new behaviors / environments, with pseudo-actions recovered downstream</td> </tr> <tr> <td><strong>VPP (this paper)</strong></td> <td>a <strong>feature extractor</strong> — its internal predictive representations condition a separate action head</td> </tr> <tr> <td><a href="/log/blog/2026/dreamzero/">DreamZero</a></td> <td>a <strong>policy backbone</strong> — the video model itself emits both predicted frames and actions through parallel heads</td> </tr> </tbody> </table> <p>The four occupy a Pareto-like frontier. <strong>LAPA</strong> is cheapest at deploy time (the VDM doesn’t run; only the label-extracted VLA does). <strong>VPP</strong> is the cleanest separation of concerns (VDM features → action head). <strong>DreamGen</strong> trades online efficiency for offline data-amortization. <strong>DreamZero</strong> maximally collapses the architecture at the cost of needing the largest model at deploy time.</p> <p>Choosing between them is a <em>decision frame</em> (<a href="/log/explorations/2026-05-18-when-and-how-to-use-agents/">Ch. 2</a>-style) based on what you actually have:</p> <ul> <li>Lots of unlabeled video, small embodied dataset → <strong>LAPA</strong>.</li> <li>A few demos for the source task, want behavior coverage → <strong>DreamGen</strong>.</li> <li>Strong VDM, limited real-time GPU budget for the policy → <strong>VPP</strong>.</li> <li>Plenty of real-time compute, want the cleanest architecture → <strong>DreamZero</strong>.</li> </ul> <p>VPP is the most <em>practitioner-friendly</em> point on this frontier because the action head is small and the VDM only runs as a feature extractor.</p> <hr/> <h2 id="7-why-it-matters">7. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>It establishes predictive features as a backbone primitive.</strong> For two years the field’s default backbones (R3M, MVP, DINO-v2) have been static. VPP shows that swapping to a VDM-derived predictive backbone produces a measurable jump in generalization benchmarks. That’s a reusable substrate, not a one-off result.</li> <li><strong>It makes “use the VDM as a feature extractor” a respectable choice.</strong> Before VPP, the implicit assumption was “if you have a VDM, you should generate videos with it.” VPP shows that <em>just reading the intermediate features</em> is often enough, and is dramatically cheaper at inference. This is the same lesson transformer-based feature reuse taught vision two years ago — the inside of the model is at least as useful as the output.</li> <li><strong>It defines the action head minimally.</strong> The DiT action head is small relative to the VDM. This means the <em>policy-specific</em> tuning is cheap; you can adapt to new embodiments by retraining the action head while keeping the VDM features fixed. The architecture mirrors the <a href="/log/blog/2026/lapa/">LAPA fine-tune-the-decoder</a> pattern, generalized to continuous actions and predictive features.</li> </ol> <hr/> <h2 id="8-limitations-worth-knowing">8. Limitations Worth Knowing</h2> <ul> <li><strong>VDM quality caps the policy.</strong> Just like <a href="/log/blog/2026/dreamgen/">DreamGen</a>, if the video model is bad at instruction-following or physics, the predictive features inherit those flaws. There is no DreamGen Bench-style explicit measurement in this paper, but the dependency is real.</li> <li><strong>The feature-extraction step has a wall-clock cost.</strong> The efficiency tricks bring inference into a usable regime, but the VDM is still a much heavier backbone than R3M or DINO-v2. On edge robots without GPU support, this is a serious deployment constraint.</li> <li><strong>Inverse dynamics is implicit.</strong> The action head is not trained against an explicit inverse-dynamics target — it learns the mapping by gradient descent on action-prediction loss. This works in practice but lacks the interpretability of an explicit IDM head.</li> <li><strong>VDM fine-tuning data still has to exist.</strong> The video prediction model needs robot + human-manipulation data to be fine-tuned. This is <em>cheaper</em> than action-labeled robot data (which is what was avoided), but it’s not zero.</li> <li><strong>Calvin-style benchmarks aren’t open-world.</strong> Calvin and MetaWorld are constrained simulated environments. The dexterous- hand real-world result is impressive, but the gap to fully unconstrained deployment remains.</li> </ul> <hr/> <h2 id="9-the-takeaway-for-a-first-reader">9. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Vision backbones for robot policy have been pretrained for the wrong task</strong> — static reconstruction / contrastive objectives capture <em>what is</em>, not <em>what will be</em>. <strong>Video diffusion models already carry the missing predictive signal</strong> inside their features.</li> <li><strong>VPP uses a fine-tuned VDM as a feature extractor</strong>, reads its intermediate features as conditioning, and trains a small <strong>Diffusion Transformer action head</strong> that learns an <strong>implicit inverse-dynamics mapping</strong> from predicted future representation to action.</li> <li><strong>The result is +41.5% on Calvin ABC-D</strong>, +31.6% on real-world dexterous manipulation, and a single policy solving <strong>100+ real-world dexterous-hand tasks</strong>, while keeping the action head small and the deployment story clean.</li> </ol> <p>That’s the arc: identify the missing predictive signal in robot backbones → notice that VDMs already encode it → use VDM features (not videos) → train an implicit-IDM action head → win across sim and real with one policy architecture.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Hu, Y., Guo, Y., Wang, P., Chen, X., Wang, Y.-J., Zhang, J., Sreenath, K., Lu, C., &amp; Chen, J. (2025). <em>Video Prediction Policy: A Generalist Robot Policy with Predictive Visual Representations.</em> ICML 2025 Spotlight. <a href="https://arxiv.org/abs/2412.14803">arXiv:2412.14803</a>.</li> <li>Project page: <a href="https://video-prediction-policy.github.io/">https://video-prediction-policy.github.io/</a></li> <li>Code: <a href="https://github.com/roboterax/video-prediction-policy">https://github.com/roboterax/video-prediction-policy</a></li> <li>Background backbones: R3M, MVP, DINO-v2, CLIP.</li> <li>Related on this site (the “video model in robot learning” taxonomy): <a href="/log/blog/2026/lapa/">LAPA paper review</a> — video model as <strong>label extractor</strong>; <a href="/log/blog/2026/dreamgen/">DreamGen paper review</a> — video model as <strong>data factory</strong>; <a href="/log/blog/2026/dreamzero/">DreamZero paper review</a> — video model as <strong>policy backbone</strong>; <a href="/log/blog/2026/flow-matching/">Flow Matching paper review</a> — the training objective these video models depend on.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="robotics"/><category term="video-diffusion"/><category term="world-models"/><category term="vla"/><category term="representation-learning"/><summary type="html"><![CDATA[Paper review of Video Prediction Policy (Hu, Guo et al., ICML 2025 Spotlight) — a generalist robot policy that conditions on the internal predictive representations of a fine-tuned video diffusion model and learns an implicit inverse-dynamics action head on top. +41.5% relative improvement on Calvin ABC-D and a single policy solving 100+ real-world dexterous-hand tasks.]]></summary></entry><entry><title type="html">DreamGen — Scaling Robot Learning by Dreaming Trajectories</title><link href="https://yjkim-stat.github.io/log/blog/2026/dreamgen/" rel="alternate" type="text/html" title="DreamGen — Scaling Robot Learning by Dreaming Trajectories"/><published>2026-06-13T00:00:00+00:00</published><updated>2026-06-13T00:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/dreamgen</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/dreamgen/"><![CDATA[<p><strong>Paper.</strong> Joel Jang, Seonghyeon Ye, Zongyu Lin, Jiannan Xiang, Johan Bjorck, Yu Fang, Fengyuan Hu, Spencer Huang, Kaushil Kundalia, Yen-Chen Lin, Loic Magne, Ajay Mandlekar, Avnish Narayan, You Liang Tan, Guanzhi Wang, Jing Wang, Qi Wang, Yinzhen Xu, Xiaohui Zeng, Kaiyuan Zheng, Ruijie Zheng, Ming-Yu Liu, Luke Zettlemoyer, Dieter Fox, Jan Kautz, Scott Reed, Yuke Zhu, Linxi Fan. <em>DreamGen: Unlocking Generalization in Robot Learning through Video World Models.</em> CoRL 2025 / arXiv:2505.12705, May 2025. NVIDIA GEAR Lab. <a href="https://arxiv.org/abs/2505.12705">[arXiv]</a> · <a href="https://research.nvidia.com/labs/gear/dreamgen/">[project page]</a> · <a href="https://github.com/NVIDIA/GR00T-Dreams">[code]</a></p> <blockquote> <p>📑 <strong>Companion slide deck.</strong> A self-contained HTML walk-through of this review (in Korean) is available at <a href="/log/assets/paper_review_html_slides/DreamGen%20%EB%85%BC%EB%AC%B8%20%ED%95%B4%EC%84%A4%20(standalone).html">DreamGen 논문 해설 (standalone).html</a>. The deck and this post share the same structure; the post is the long-form English version.</p> </blockquote> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Robot learning has been stuck on a structural problem: behaviors and environments don’t transfer cheaply. To get a robot to do a new task in a new room, you typically need to teleoperate demonstrations <em>of that task in that room</em>. The teleoperation rig becomes the bottleneck — humans are slow, robots are expensive, and the data scales linearly with the number of (behavior, environment) pairs you want to cover. DreamGen reframes the problem: <strong>what if we <em>dream</em> the new trajectories instead of collecting them?</strong> The recipe is four stages. (1) Fine-tune an image-to-video diffusion model with a LoRA on a small amount of real teleoperation data so it produces videos in the target robot’s visual / kinematic style. (2) Roll out the fine-tuned video model with new initial frames and new language instructions to generate photorealistic synthetic videos of behaviors and environments the robot has <em>never</em> been teleoperated through. (3) Recover <strong>pseudo-actions</strong> from those dreamed videos via an inverse- dynamics model (IDM) or a LAPA-style latent action model. (4) Train a visuomotor policy on the resulting <code class="language-plaintext highlighter-rouge">(synthetic video, recovered action)</code> pairs — “neural trajectories.” The headline: a humanoid robot performs <strong>22 new behaviors in both seen and unseen environments</strong> while having been teleoperated on only a <strong>single pick-and-place task in one environment</strong>. The paradigm shift the paper argues for: <strong>scaling human teleoperation → scaling GPU compute through world models</strong>.</p> <hr/> <h2 id="1-the-problem--teleoperation-doesnt-scale">1. The Problem — Teleoperation Doesn’t Scale</h2> <p>The dominant recipe for training a VLA or visuomotor policy needs <code class="language-plaintext highlighter-rouge">(observation, language, action)</code> triples <em>for every (behavior, environment) combination you want the robot to handle</em>. The practical math:</p> <table> <thead> <tr> <th>Axis</th> <th>Cost</th> </tr> </thead> <tbody> <tr> <td>New behavior on familiar robot in familiar env</td> <td>hours of teleoperation</td> </tr> <tr> <td>New environment (lighting, layout, objects)</td> <td>recollect everything</td> </tr> <tr> <td>New embodiment</td> <td>recollect everything <em>again</em></td> </tr> <tr> <td>Tail behaviors (the actually-useful ones)</td> <td>impossible at scale</td> </tr> </tbody> </table> <p>This is the wall LAPA’s predecessor work (<a href="/log/blog/2026/lapa/">review</a>) addressed at the <em>pretraining</em> stage by extracting implicit actions from video. DreamGen attacks the same wall at the <em>trajectory generation</em> stage: instead of waiting for a human to teleoperate the behaviors, <strong>manufacture the trajectories with a generative model</strong>.</p> <hr/> <h2 id="2-the-move--neural-trajectories">2. The Move — Neural Trajectories</h2> <p>A <strong>neural trajectory</strong> is a synthetic robot trajectory composed of:</p> <ol> <li>A photorealistic <strong>video</strong> of the robot performing the task (generated by a video world model).</li> <li>A sequence of <strong>pseudo-actions</strong> recovered from that video (inferred by an IDM or latent action model).</li> </ol> <p>Crucially, the pseudo-actions are in the <em>target robot’s action space</em>. Once you have the <code class="language-plaintext highlighter-rouge">(video, action)</code> pair, any policy that trains on <code class="language-plaintext highlighter-rouge">(observation, action)</code> triples can consume it.</p> <p>DreamGen’s bet: <strong>if the video world model is good enough at modeling the embodiment, the policies trained on its dreams will transfer to the real robot.</strong></p> <hr/> <h2 id="3-the-four-stage-pipeline">3. The Four-Stage Pipeline</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Stage 1                        Stage 2                       Stage 3                  Stage 4
─────────                      ─────────                     ─────────                 ─────────
small real                      fine-tuned                    pseudo-action            visuomotor
teleoperation        ─────→     video world         ─────→    recovery        ─────→   policy on
demos (one task,                model (LoRA)                  (IDM or                  neural
one env)                                                      LAPA-style)              trajectories

                                ↓ rollouts
                                synthetic videos
                                of NEW behaviors
                                + NEW environments
</code></pre></div></div> <h3 id="31-stage-1--video-world-model-fine-tuning">3.1 Stage 1 — Video World Model Fine-Tuning</h3> <p>Take an off-the-shelf <strong>image-to-video generative model</strong> (the paper evaluates Cosmos, WAN 2.1, Hunyuan, CogVideoX) and fine-tune it with <strong>LoRA</strong> on a small set of real teleoperation videos.</p> <p>The LoRA’s job is to <strong>inject the robot’s kinematics and the environment’s dynamics into the prior</strong>:</p> <ul> <li><em>Kinematics:</em> how this robot’s arm / gripper / base actually moves (joint limits, link lengths, gripper closure dynamics).</li> <li><em>Embodiment appearance:</em> what the robot looks like in the generated frames.</li> <li><em>Scene physics:</em> how objects in this lab respond to contact.</li> </ul> <p>Critically, the LoRA preserves the <strong>general world prior</strong> in the base model. The fine-tuned model can still generate scenes and objects that were never in the teleoperation data — it just generates them with the right robot in the foreground.</p> <h3 id="32-stage-2--video-world-model-rollout">3.2 Stage 2 — Video World Model Rollout</h3> <p>Now generate synthetic videos. Inputs:</p> <ul> <li>An <strong>initial frame</strong> (from simulation or a single captured photo) showing the start state.</li> <li>A <strong>language instruction</strong> (“pour the cup”, “open the drawer”, “wipe the table”) describing the behavior.</li> </ul> <p>The fine-tuned model produces a photorealistic video of the robot performing the behavior. The initial frames don’t have to match the teleoperation distribution — they can be from new environments. This is the step where <strong>behavior × environment combinatorial coverage actually happens</strong>.</p> <p>In the paper’s headline experiment, this stage generates <strong>50 neural trajectories</strong> per behavior, for <strong>14 novel behavior tasks</strong>.</p> <h3 id="33-stage-3--pseudo-action-recovery">3.3 Stage 3 — Pseudo-Action Recovery</h3> <p>The generated videos contain no action labels. DreamGen recovers them by running each video through one of:</p> <ul> <li><strong>An inverse-dynamics model (IDM)</strong> — supervised on the real teleoperation data to predict actions from observation sequences. Same data as Stage 1; different head.</li> <li><strong>A LAPA-style latent action model</strong> — extracts discrete latent action codes from frame pairs (<a href="/log/blog/2026/lapa/">LAPA review</a>). The decoder maps codes to robot actions.</li> </ul> <p>Both pathways produce per-frame pseudo-actions. The pseudo-actions are noisy by construction; the policy training step has to absorb that noise. The fact that it does is itself a finding.</p> <h3 id="34-stage-4--policy-training-on-neural-trajectories">3.4 Stage 4 — Policy Training on Neural Trajectories</h3> <p>Now train a visuomotor policy on the neural trajectories as if they were real teleoperation data. The paper shows two regimes:</p> <ul> <li><strong>Pure neural-trajectory training</strong> (no real data after Stage 1’s one task) — surprisingly works.</li> <li><strong>Co-training</strong> — mix neural trajectories with whatever small real trajectory set you have. This yields <strong>consistent gains on both IDM-pseudo-action and LAPA-pseudo-action variants across all data regimes</strong>.</li> </ul> <hr/> <h2 id="4-dreamgen-bench--the-world-modeling-benchmark">4. DreamGen Bench — The World-Modeling Benchmark</h2> <p>A practical question DreamGen has to answer: <em>which video model should I use as the world model?</em> The paper ships <strong>DreamGen Bench</strong> to make this decision empirically rather than by vibes.</p> <h3 id="41-what-it-measures">4.1 What it measures</h3> <p>Two metrics:</p> <ol> <li><strong>Instruction following</strong> — does the generated video actually depict the behavior the prompt asked for?</li> <li><strong>Physics following</strong> — is the motion in the video physically plausible (no teleporting objects, no impossible contact, no broken kinematics)?</li> </ol> <h3 id="42-the-four-contenders">4.2 The four contenders</h3> <p>Cosmos, WAN 2.1, Hunyuan, CogVideoX — the major open image-to-video models at the time of writing.</p> <h3 id="43-the-most-useful-finding">4.3 The most useful finding</h3> <blockquote> <p><strong>DreamGen Bench scores correlate strongly with downstream policy success.</strong></p> </blockquote> <p>This is the result that turns DreamGen from a recipe into a <em>research program</em>. It means improving the video world model (better instruction following, better physics) <strong>provably</strong> improves the downstream robot policy. The robot people now have a direct reason to care about progress in video generation, and the video people have a benchmark that measures something other than “is the video pretty.”</p> <p>7,000 neural trajectories per model are generated for the benchmark.</p> <hr/> <h2 id="5-headline-results">5. Headline Results</h2> <h3 id="51-the-humanoid-generalization-claim">5.1 The humanoid generalization claim</h3> <blockquote> <p>A humanoid robot performs <strong>22 new behaviors in both seen and unseen environments</strong> while requiring teleoperation data from <strong>only a single pick-and-place task in one environment</strong>.</p> </blockquote> <p>The 22 novel behaviors include <strong>pouring</strong>, <strong>opening/closing articulated objects</strong>, and <strong>manipulating a variety of tools</strong> — tasks that are very different in kinematic structure from pick-and-place.</p> <p>The setup is the strongest possible test of the data-amortization claim: real teleoperation supervision is constant (one task, one environment); behavior diversity comes <em>entirely</em> from the dreamed trajectories.</p> <h3 id="52-co-training-consistency">5.2 Co-training consistency</h3> <p>Across both pseudo-action recovery pathways (IDM and LAPA) and across <strong>all data regimes</strong> (small / medium / large real-trajectory budgets), <strong>co-training with neural trajectories improves the policy</strong>. This is the robust version of the result — the gain isn’t limited to a particular regime.</p> <h3 id="53-scaling-per-task">5.3 Scaling per task</h3> <p>A few real teleoperation trajectories per task — <strong>10 to 13</strong> — combined with neural trajectories, suffice to lift policy success across all evaluated robots. This is the data-budget DreamGen wants people to think in: a <em>handful</em> of real demos plus thousands of dreamed ones.</p> <hr/> <h2 id="6-what-the-method-actually-says">6. What the Method Actually Says</h2> <p>Stripped of acronyms, the structural claim:</p> <blockquote> <p>A modern image-to-video diffusion model has internalized enough physical and visual structure that, with a small LoRA on teleoperation data, it becomes a usable simulator of the target robot. Once you have a usable simulator, you can dream the trajectories for behaviors and environments you’ve never teleoperated, recover pseudo-actions via inverse dynamics or latent action models, and train policies on the result. The cost of new behaviors and new environments moves from <strong>human-hours at a teleoperation rig</strong> to <strong>GPU-hours at a video model</strong>.</p> </blockquote> <p>This is the data-side analog of the architecture-side argument <a href="/log/blog/2026/dreamzero/">DreamZero</a> makes a year later. DreamGen treats the video model as a <em>dataset generator</em>; DreamZero treats it as the <em>policy backbone itself</em>. Both rely on the same belief: <strong>video is the right abstraction for robot learning because it carries dynamics that action labels alone cannot</strong>.</p> <hr/> <h2 id="7-why-it-matters">7. Why It Matters</h2> <p>Three reasons, in increasing order of generality:</p> <ol> <li><strong>It changes the cost structure of robot data.</strong> Robot data has been priced in <em>human-hours of teleoperation</em>. DreamGen prices it in <em>GPU-hours of dreaming</em>. GPU-hours scale with money; teleoperation-hours scale with people. This is the kind of substitution that, if it holds up, reshapes the field’s resource model.</li> <li><strong>It turns video-generation progress into robot-policy progress.</strong> Through DreamGen Bench, every improvement in instruction-following or physics-following video generation now directly translates into a downstream robot win. The two research communities (generative video, robot learning) now have a <em>measurable</em> common interest.</li> <li><strong>It completes the NVIDIA GEAR triad.</strong> <ul> <li><a href="/log/blog/2026/lapa/">LAPA (Oct 2024)</a> — pretrain a VLA on action-label-free video by extracting discrete latent actions.</li> <li><strong>DreamGen (May 2025)</strong> — <em>generate</em> the videos themselves and recover pseudo-actions to train policies.</li> <li><a href="/log/blog/2026/dreamzero/">DreamZero (Feb 2026)</a> — collapse both into a single video-diffusion backbone that jointly predicts video and action.</li> </ul> <p>Read in order, these three papers tell one consistent story: <strong>video is the substrate; action is a derived quantity; the research question is where in the pipeline you do the extraction</strong>.</p> </li> </ol> <hr/> <h2 id="8-limitations-worth-knowing">8. Limitations Worth Knowing</h2> <ul> <li><strong>Video-model fidelity caps the policy.</strong> If the world model hallucinates physics (sliding cups, ghost contacts) the policy will learn the hallucinations. DreamGen Bench’s physics metric exists to measure this exactly; the implication is that some behaviors are simply not yet dreamable well enough for policy training.</li> <li><strong>Pseudo-actions are noisy.</strong> Both IDM and LAPA recover actions from observation sequences with error. The paper shows the policy absorbs this, but extreme-precision tasks (sub-millimeter insertion, delicate manipulation) may not survive the noise.</li> <li><strong>Initial-frame coverage limits behavior coverage.</strong> Stage 2 needs an initial frame for each scenario. If the only initial frames you have come from one room, the dreams can extend to new behaviors but not to genuinely new visual environments. The paper handles this with simulation-rendered initial frames, but the dependency exists.</li> <li><strong>Embodiment-specific LoRA.</strong> Each new robot embodiment needs its own LoRA fine-tune (and the teleoperation data to do that fine-tune). DreamGen reduces but does not eliminate per-embodiment supervision.</li> <li><strong>CoRL benchmarks aren’t real homes.</strong> The 22 humanoid behaviors are an impressive proof-of-concept; the gap between “humanoid pours water in a lab” and “household robot deploys to customers” is still large.</li> </ul> <hr/> <h2 id="9-the-takeaway-for-a-first-reader">9. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Robot data was priced in teleoperation-hours.</strong> DreamGen reprices it in GPU-hours by <strong>dreaming synthetic robot trajectories from a video world model</strong> and recovering pseudo-actions from those dreams.</li> <li><strong>The pipeline is four stages:</strong> <em>fine-tune the video model on a small amount of real demos (LoRA)</em> → <em>roll out videos of new behaviors/environments</em> → <em>recover actions via IDM or LAPA-style latent action model</em> → <em>train policies on the resulting neural trajectories</em>.</li> <li><strong>The headline:</strong> a humanoid performs <strong>22 new behaviors in seen and unseen environments</strong> from a <strong>single pick-and-place teleoperation source task</strong>. <strong>DreamGen Bench scores correlate with downstream policy success</strong>, turning video-generation progress into robot-policy progress.</li> </ol> <p>That’s the arc: teleoperation doesn’t scale → dream the trajectories → recover the actions → train the policy → measure the world model’s quality as a robot-learning signal.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Jang, J., Ye, S., <em>et al.</em> (2025). <em>DreamGen: Unlocking Generalization in Robot Learning through Video World Models.</em> CoRL 2025. <a href="https://arxiv.org/abs/2505.12705">arXiv:2505.12705</a>.</li> <li>Project page: <a href="https://research.nvidia.com/labs/gear/dreamgen/">https://research.nvidia.com/labs/gear/dreamgen/</a></li> <li>Code: <a href="https://github.com/NVIDIA/GR00T-Dreams">https://github.com/NVIDIA/GR00T-Dreams</a></li> <li>Background: Cosmos / WAN 2.1 / Hunyuan / CogVideoX (the four video world models evaluated).</li> <li>Related on this site: <a href="/log/blog/2026/lapa/">LAPA paper review</a> — the same first authors’ earlier work on extracting latent actions from video, which DreamGen uses as one of its pseudo-action recovery pathways; <a href="/log/blog/2026/dreamzero/">DreamZero paper review</a> — the same lab’s follow-up that collapses video and action prediction into one backbone; <a href="/log/blog/2026/flow-matching/">Flow Matching paper review</a> — the training objective the video world models in this lineage depend on.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="robotics"/><category term="world-models"/><category term="video-diffusion"/><category term="data-synthesis"/><category term="vla"/><summary type="html"><![CDATA[Paper review of DreamGen (Jang, Ye et al., NVIDIA GEAR, CoRL 2025) — a four-stage pipeline that fine-tunes a video world model on a small amount of teleoperation data, dreams synthetic robot videos for new behaviors and environments, recovers pseudo-actions via IDM or latent-action models, and trains visuomotor policies on the resulting "neural trajectories." Enables a humanoid to perform 22 new behaviors from just a single pick-and-place teleoperation source task.]]></summary></entry><entry><title type="html">LAPA — Latent Action Pretraining from Action-Label-Free Video</title><link href="https://yjkim-stat.github.io/log/blog/2026/lapa/" rel="alternate" type="text/html" title="LAPA — Latent Action Pretraining from Action-Label-Free Video"/><published>2026-06-08T07:00:00+00:00</published><updated>2026-06-08T07:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/lapa</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/lapa/"><![CDATA[<p><strong>Paper.</strong> Seonghyeon Ye, Joel Jang, Byeongguk Jeon, Sejune Joo, Jianwei Yang, Baolin Peng, Ajay Mandlekar, Reuben Tan, Yu-Wei Chao, Bill Yuchen Lin, Lars Liden, Kimin Lee, Jianfeng Gao, Luke Zettlemoyer, Dieter Fox, Minjoon Seo. <em>Latent Action Pretraining from Videos.</em> ICLR 2025. KAIST · UW · Microsoft · NVIDIA · AI2. <a href="https://arxiv.org/abs/2410.11758">[arXiv]</a> · <a href="https://latentactionpretraining.github.io/">[project page]</a> · <a href="https://github.com/LatentActionPretraining/LAPA">[code]</a> · <a href="https://huggingface.co/latent-action-pretraining/LAPA-7B-openx">[HF checkpoint]</a></p> <blockquote> <p>📑 <strong>Companion slide deck.</strong> A self-contained HTML walk-through of this review (in Korean) is available at <a href="/log/assets/paper_review_html_slides/LAPA%20%EB%85%BC%EB%AC%B8%20%ED%95%B4%EC%84%A4%20(standalone).html">LAPA 논문 해설 (standalone).html</a>. The deck and this post share the same structure; the post is the long-form English version.</p> </blockquote> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Modern Vision-Language-Action (VLA) models — OpenVLA, RT-2-X, Octo — are trained on robot demonstration data in which every frame has a ground-truth action label. That label is <strong>expensive</strong>: it requires a human teleoperator (or motion-captured expert) recording joint commands at every timestep, on a specific robot, for every task. The world has <em>enormous</em> amounts of unlabeled video (humans manipulating objects, robots in the wild, household scenes) but none of it has action labels in the VLA sense. LAPA’s question is simple: <strong>can we pretrain a VLA on action-label-free video, and recover the action label only at fine-tuning time with a small amount of robot data?</strong> The answer is yes, via a two-stage recipe. First, a VQ-VAE-style inverse-dynamics model learns to encode each <em>pair of consecutive frames</em> into a <strong>discrete latent action</strong> — the “label” the video implicitly carries. Then a VLA backbone is pretrained to predict these discrete latent codes from observations</p> <ul> <li>language, on internet-scale video. Finally, a tiny decoder is fine-tuned on robot data to translate the latent code into actual joint actions. The result: <strong>+6.22% over OpenVLA on real-robot evaluation</strong> with <strong>~30× lower pretraining compute</strong> and <strong>zero ground-truth action labels</strong> at the pretraining stage.</li> </ul> <hr/> <h2 id="1-the-problem--vlas-are-pinned-to-labeled-robot-data">1. The Problem — VLAs Are Pinned to Labeled Robot Data</h2> <p>The dominant VLA recipe (OpenVLA, RT-2-X, Octo) treats a robot policy as a <em>language-conditioned next-action predictor</em>:</p> \[\pi_\theta(a_t \mid o_t, \ell), \qquad a_t \in \text{robot action space}.\] <p>To train, you need <code class="language-plaintext highlighter-rouge">(o_t, ℓ, a_t)</code> triples — and the $a_t$ has to be in the <em>target robot’s joint/end-effector action space</em>. This ties the training distribution to:</p> <ul> <li><strong>Teleoperated robot data.</strong> Expensive, slow to collect.</li> <li><strong>A specific embodiment.</strong> Cross-embodiment transfer (Open X-Embodiment) helps but the action spaces still have to be reconciled.</li> </ul> <p>Meanwhile, the world has orders of magnitude more <strong>action-label- free</strong> video:</p> <table> <thead> <tr> <th>Video source</th> <th>Quantity</th> <th>Has actions?</th> </tr> </thead> <tbody> <tr> <td>Teleoperated robot data (Open-X)</td> <td>~1M trajectories</td> <td>✅ joint commands</td> </tr> <tr> <td>Internet manipulation video</td> <td>~∞</td> <td>❌ no action labels</td> </tr> <tr> <td>Egocentric video (Ego4D, etc.)</td> <td>10K+ hrs</td> <td>❌ no action labels</td> </tr> <tr> <td>In-the-wild robot footage</td> <td>?</td> <td>❌ no action labels</td> </tr> </tbody> </table> <p><strong>The structural complaint:</strong> the supervision pipeline is wasting ~99% of the available training data because action labels are gating access to it. LAPA’s bet is that <em>the action labels are implicit in the video</em> — two consecutive frames already encode “what action was taken.” We just have to extract them.</p> <hr/> <h2 id="2-the-reframing--recover-implicit-actions-from-frame-pairs">2. The Reframing — Recover Implicit Actions from Frame Pairs</h2> <p>LAPA’s framing has three moves:</p> <ol> <li><strong>Define a latent action space.</strong> Not the robot’s joint space; a learned, <em>discrete</em> latent space whose codes describe the transitions between consecutive frames.</li> <li><strong>Pretrain a VLA to predict latent codes</strong>, not joint commands. This uses <em>any</em> video — no action labels required.</li> <li><strong>Fine-tune a small decoder</strong> from latent codes to real robot actions, using a small amount of labeled robot data.</li> </ol> <p>The latent space is the universal currency. Pretraining happens in that currency; only the final fine-tune touches the embodiment- specific action space.</p> <hr/> <h2 id="3-stage-1--latent-action-quantization">3. Stage 1 — Latent Action Quantization</h2> <p>The first model LAPA trains is an <strong>inverse-dynamics VQ-VAE</strong>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  frame x_t  ─┐
              ├─→ Encoder ─→ continuous latent ─→ VQ codebook ─→ discrete code z_t
 frame x_{t+1}┘                                                       │
                                                                      ▼
              ┌──────── Decoder ──────────────────────────────────────┘
              │ generates x_{t+1} from (x_t, z_t)
              ▼
            x_{t+1}'  ←  reconstruction loss vs ground-truth x_{t+1}
</code></pre></div></div> <h3 id="31-what-the-encoder-learns">3.1 What the encoder learns</h3> <p>The encoder reads two consecutive frames and emits a discrete code $z_t$ that captures <em>what changed between them</em>. This is the classic <strong>inverse dynamics model</strong> target: infer the action that caused the transition $(x_t, x_{t+1})$.</p> <p>VQ-VAE quantization forces the latent space to be <strong>discrete</strong>, which is what makes downstream language-modeling-style pretraining clean. The codebook size is a hyperparameter (a few thousand codes); each code corresponds to one “kind” of inter-frame transition.</p> <h3 id="32-what-the-decoder-learns">3.2 What the decoder learns</h3> <p>The decoder is the <strong>forward dynamics model</strong>: given the current frame $x_t$ and a discrete code $z_t$, predict the next frame $x_{t+1}$. The reconstruction loss between the predicted and true $x_{t+1}$ trains the whole encoder-decoder + codebook end to end.</p> <h3 id="33-nsvq--gradient-collapse-fix">3.3 NSVQ — gradient collapse fix</h3> <p>Vanilla VQ-VAE training suffers from <strong>gradient collapse</strong>: most codebook entries go unused because the nearest-neighbor lookup has zero gradient. LAPA uses <strong>NSVQ</strong> (Vali &amp; Bäckström, 2022), which replaces the quantization error with the product of the original error and a normalized noise vector. This keeps gradients flowing through more of the codebook and prevents the collapse.</p> <h3 id="34-what-you-have-at-the-end-of-stage-1">3.4 What you have at the end of Stage 1</h3> <p>A discrete vocabulary of inter-frame actions, learned <strong>purely from video</strong> with <strong>no action labels needed</strong>. Every frame pair in your video corpus now has an implicit “label” — its latent code.</p> <hr/> <h2 id="4-stage-2--latent-vla-pretraining">4. Stage 2 — Latent VLA Pretraining</h2> <p>Now treat the latent codes as the <strong>target tokens</strong> for a language-style VLA model.</p> <h3 id="41-setup">4.1 Setup</h3> <ul> <li><strong>Backbone:</strong> a Vision-Language model (Prismatic-7B class).</li> <li><strong>Inputs:</strong> current observation $o_t$ + task description $\ell$.</li> <li><strong>Target:</strong> the discrete latent code $z_t$ produced by the Stage-1 encoder on the next frame.</li> <li><strong>Loss:</strong> standard next-token prediction (or classification over the codebook).</li> </ul> <h3 id="42-the-data-unlock">4.2 The data unlock</h3> <p>This pretraining task uses <strong>any video corpus</strong>, because the only “label” needed is the Stage-1 encoder’s output on a frame pair — and the Stage-1 encoder runs on any frames. LAPA’s headline pretraining run uses the <strong>Open-X Embodiment</strong> corpus, but the recipe is <em>compatible with</em> internet-scale unlabeled video.</p> <p>The compute story is dramatic. For pretraining on Open-X:</p> <table> <thead> <tr> <th>Model</th> <th>Compute</th> </tr> </thead> <tbody> <tr> <td>OpenVLA</td> <td>~21,500 A100-hours</td> </tr> <tr> <td><strong>LAPA</strong></td> <td><strong>272 H100-hours (8 H100 × 34 h)</strong></td> </tr> </tbody> </table> <p>That’s roughly <strong>30–40× lower pretraining compute</strong> to <em>beat</em> OpenVLA on the downstream benchmark, while never seeing a single ground-truth action label at this stage.</p> <h3 id="43-the-intuition">4.3 The intuition</h3> <p>The model is learning the same competence OpenVLA learns — “given this observation and instruction, what <em>kind of motion</em> should follow?” — but it expresses the answer in a vocabulary (the codebook) that doesn’t require the model to commit to a specific joint angle in a specific embodiment. That’s why it can absorb training signal much more cheaply: the target is lower- dimensional and embodiment-agnostic.</p> <hr/> <h2 id="5-stage-3--fine-tune-a-latent--action-decoder">5. Stage 3 — Fine-tune a Latent → Action Decoder</h2> <p>The final step bridges the latent code to actual robot actions.</p> <h3 id="51-setup">5.1 Setup</h3> <ul> <li>Take the latent-pretrained VLA.</li> <li>Add a small action decoder head that maps the latent code $z_t$ (plus the current observation) to the real action $a_t$ in the target robot’s space.</li> <li>Fine-tune on a <strong>small</strong> amount of labeled robot data — orders of magnitude less than what OpenVLA used.</li> </ul> <h3 id="52-what-survives-what-changes">5.2 What survives, what changes</h3> <ul> <li><strong>Survives:</strong> the VLA backbone, which has already learned “observation + language → latent action” from huge video data.</li> <li><strong>Changes:</strong> only the latent-to-real-action mapping. This is the embodiment-specific part, and it’s the only thing that needs embodiment-specific data.</li> </ul> <p>This is the cross-embodiment version of “freeze the backbone, re-train the head.” LAPA’s recipe makes that pattern work for <em>action</em> spaces, not just classification spaces.</p> <hr/> <h2 id="6-headline-results">6. Headline Results</h2> <h3 id="61-real-robot-evaluation-vs-openvla">6.1 Real-robot evaluation vs OpenVLA</h3> <table> <thead> <tr> <th>Metric</th> <th>Result</th> </tr> </thead> <tbody> <tr> <td>Average success on real-robot eval</td> <td><strong>+6.22% over OpenVLA</strong></td> </tr> <tr> <td>Pretraining compute</td> <td><strong>~30× lower</strong> than OpenVLA</td> </tr> <tr> <td>Ground-truth action labels at pretraining</td> <td><strong>0</strong></td> </tr> </tbody> </table> <p>The pattern: LAPA matches or beats a VLA trained on millions of <em>labeled</em> trajectories, with a tiny fraction of the compute, and without using any action labels during pretraining.</p> <h3 id="62-cross-embodiment">6.2 Cross-embodiment</h3> <p>LAPA significantly outperforms OpenVLA on <strong>2 out of 3</strong> unseen embodiments. The latent-action representation transfers across robots better than the per-embodiment action label does, because the latent vocabulary was never tied to one robot in the first place.</p> <h3 id="63-the-fine-tune-efficiency">6.3 The fine-tune efficiency</h3> <p>Fine-tuning a small action decoder on a <em>small</em> amount of labeled data is enough to unlock the policy. The compute asymmetry — heavy on data-cheap pretraining, light on data-expensive fine-tuning — is the practical win for any team that doesn’t own a teleoperation rig.</p> <hr/> <h2 id="7-what-the-method-actually-says">7. What the Method Actually Says</h2> <p>Stripped of acronyms, the structural claim:</p> <blockquote> <p>Action labels are not the unique source of supervision for robot policies. The transition between two consecutive video frames is already a noisy label of <em>what action was taken</em>. If we extract that label as a discrete code via a VQ-VAE inverse- dynamics model, we can pretrain a VLA on <strong>any video</strong> — action-labeled or not — and then specialize to a particular robot with a tiny labeled fine-tune. The pretraining bottleneck moves from labeled-data quantity to video-corpus quantity, which is orders of magnitude larger.</p> </blockquote> <p>This is the <em>pretraining</em> analog of the architecture argument <a href="/log/blog/2026/dreamzero/">DreamZero</a> makes a year later: instead of treating action as the primary modality and adding a vision module, <strong>treat video as the primary modality and recover action from it</strong>. LAPA does this through a discrete code; DreamZero does it through joint video+action generative modeling. Both arguments start with the same observation about data availability.</p> <hr/> <h2 id="8-why-it-matters">8. Why It Matters</h2> <p>Three reasons:</p> <ol> <li><strong>It dissolves the labeled-data bottleneck for VLA pretraining.</strong> Robot foundation models have been compute-bound and data-bound simultaneously — labels are scarce and expensive. LAPA shifts the bottleneck to <em>video availability</em>, which is effectively unbounded.</li> <li><strong>The discrete-latent-action vocabulary is a reusable primitive.</strong> Treating action as a sequence of discrete codes slots cleanly into LLM-style training pipelines (next-token prediction, tokenizer reuse, mixture-of-data training). The subsequent work — UniVLA, GR00T, the <a href="/log/blog/2026/dreamzero/">DreamZero</a> lineage — borrows variations of this primitive.</li> <li><strong>It validates the “pretrain on video, specialize on robot data” recipe.</strong> This was a stated dream for years; LAPA is the first paper to ship it convincingly on standard VLA benchmarks without losing to action-supervised baselines.</li> </ol> <p>The trend connection is direct. The same first author (Seonghyeon Ye) led both LAPA (Oct 2024, ICLR 2025) and <a href="/log/blog/2026/dreamzero/">DreamZero</a> (Feb 2026). The arc is internally consistent: LAPA proved that <strong>video contains action supervision</strong>; DreamZero replaces the discrete codebook with a <strong>video-diffusion backbone</strong> that learns the dynamics implicitly and emits actions as a parallel head. The two papers are different architectural answers to the same underlying question — and reading them together is the cleanest way to see where robot foundation modeling is going.</p> <hr/> <h2 id="9-limitations-worth-knowing">9. Limitations Worth Knowing</h2> <ul> <li><strong>The latent codebook is fixed at Stage 1.</strong> If the codebook is too small, you lose action granularity; too large and the pretraining task becomes harder. The paper picks a working size but doesn’t fully ablate this knob.</li> <li><strong>Inverse dynamics assumes near-Markov frame pairs.</strong> Two frames are usually enough to identify the action, but for slow or fine-grained motions, two-frame inverse dynamics undercounts. Multi-frame windows would help; they aren’t in the recipe.</li> <li><strong>Fine-tune data is still required.</strong> LAPA reduces but does not eliminate the need for robot-specific labeled data — the latent-to-action decoder needs <em>something</em> to ground the codes. How small that <em>something</em> can get is the practical question.</li> <li><strong>Internet video isn’t yet the main pretraining corpus.</strong> The reported run uses Open-X. The paper’s framing strongly implies internet video will work, but the headline numbers are on Open-X. Whether the gain holds on truly unconstrained internet video is the obvious next experiment.</li> <li><strong>The latent space isn’t interpretable.</strong> Each code is “some inter-frame transition” — it doesn’t correspond to a named primitive like “grasp” or “push.” For debugging and safety audits, this is harder than a hand-engineered action vocabulary.</li> </ul> <hr/> <h2 id="10-the-takeaway-for-a-first-reader">10. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>VLAs were data-bottlenecked by action labels.</strong> LAPA breaks the bottleneck by <strong>extracting implicit actions from video</strong> via a VQ-VAE inverse-dynamics model that emits a <em>discrete latent code</em> for each frame pair.</li> <li><strong>Pretraining the VLA to predict latent codes</strong> is action-label-free, scales to any video corpus, and costs <strong>~30× less compute</strong> than the action-supervised alternative (OpenVLA) while ultimately winning on downstream real-robot evaluation by <strong>+6.22%</strong>.</li> <li><strong>Fine-tuning a small latent → action decoder</strong> on a tiny amount of labeled robot data is enough to deploy the policy on a real robot. The pretrained backbone transfers; only the embodiment-specific head needs replacement.</li> </ol> <p>That’s the arc: action labels are a bottleneck → extract them from video as discrete codes → pretrain a VLA on those codes → fine-tune a small decoder for the target robot → beat the action-supervised baseline at a fraction of the compute.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Ye, S., Jang, J., <em>et al.</em> (2024). <em>Latent Action Pretraining from Videos.</em> ICLR 2025. <a href="https://arxiv.org/abs/2410.11758">arXiv:2410.11758</a>.</li> <li>Project page: <a href="https://latentactionpretraining.github.io/">https://latentactionpretraining.github.io/</a></li> <li>Code: <a href="https://github.com/LatentActionPretraining/LAPA">https://github.com/LatentActionPretraining/LAPA</a></li> <li>Checkpoint: <a href="https://huggingface.co/latent-action-pretraining/LAPA-7B-openx">https://huggingface.co/latent-action-pretraining/LAPA-7B-openx</a></li> <li>Vali, M. H. &amp; Bäckström, T. (2022). <em>NSVQ: Noise Substitution in Vector Quantization</em>.</li> <li>Background: OpenVLA (Kim et al., 2024); Open X-Embodiment (Open X-Embodiment Collaboration, 2024); RT-2-X.</li> <li>Related on this site: <a href="/log/blog/2026/dreamzero/">DreamZero paper review</a> — the same author’s follow-up that replaces the discrete codebook with a video-diffusion world model; <a href="/log/blog/2026/flow-matching/">Flow Matching paper review</a> — the training objective the video-diffusion backbones in this lineage are built on.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="robotics"/><category term="vla"/><category term="world-models"/><category term="pretraining"/><category term="video"/><summary type="html"><![CDATA[Paper review of LAPA (Ye, Jang et al., ICLR 2025) — a two-stage recipe that learns discrete latent actions from video frame pairs via VQ-VAE, pretrains a Vision-Language-Action model to predict these latent actions from observations and language, then fine-tunes a small action decoder on robot data. Beats OpenVLA by +6.22% on real-robot evaluation with ~30× lower pretraining compute and no ground-truth action labels at scale.]]></summary></entry><entry><title type="html">Flow Matching — The Simulation-Free Recipe Under Modern Diffusion</title><link href="https://yjkim-stat.github.io/log/blog/2026/flow-matching/" rel="alternate" type="text/html" title="Flow Matching — The Simulation-Free Recipe Under Modern Diffusion"/><published>2026-06-08T05:00:00+00:00</published><updated>2026-06-08T05:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/flow-matching</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/flow-matching/"><![CDATA[<p><strong>Paper.</strong> Yaron Lipman, Ricky T. Q. Chen, Heli Ben-Hamu, Maximilian Nickel, Matt Le. <em>Flow Matching for Generative Modeling.</em> ICLR 2023. Meta AI · FAIR. <a href="https://arxiv.org/abs/2210.02747">[arXiv]</a> · <a href="https://openreview.net/forum?id=PqvMRDCJT9t">[OpenReview]</a></p> <blockquote> <p>📑 <strong>Companion slide deck.</strong> A self-contained HTML walk-through of this review (in Korean) is available at <a href="/log/assets/paper_review_html_slides/Flow%20Matching%20%EB%85%BC%EB%AC%B8%20%ED%95%B4%EC%84%A4%20(standalone).html">Flow Matching 논문 해설 (standalone).html</a>. The deck and this post share the same structure; the post is the long-form English version.</p> </blockquote> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Continuous Normalizing Flows (CNFs) are an <em>elegant</em> generative model — sample $x_0$ from a simple prior, integrate an ODE $\dot{x} = v_\theta(t, x)$, get a sample from a complicated target. They’ve been theoretically attractive for years and practically unusable, because the standard training objective (maximum likelihood) requires solving the ODE forward and backward on every training step. That’s the “simulation cost.” <strong>Flow Matching</strong> proposes a different objective: instead of training to maximize likelihood, <strong>directly regress the vector field $v_\theta$ against a target vector field that produces a chosen probability path</strong>. The catch is that the marginal target vector field is intractable. The contribution is showing that you can construct it as a mixture of <em>per-sample conditional</em> vector fields, and the <strong>conditional flow matching (CFM) loss</strong> — which only ever sees the conditional field — has the <strong>same gradient as the intractable marginal loss</strong>. This eliminates simulation entirely. With <strong>optimal-transport conditional paths</strong> (straight-line interpolation between noise and data), CNFs trained this way <strong>outperform DDPM on CIFAR-10 and ImageNet on FID, NLL, <em>and</em> NFE simultaneously</strong> — the first time those three knobs moved together. The recipe is now the training objective under Stable Diffusion 3, Wan, and DreamZero.</p> <hr/> <h2 id="1-the-background--why-cnfs-were-stuck">1. The Background — Why CNFs Were Stuck</h2> <p>A CNF defines a generative model via a learned vector field $v_\theta: [0,1] \times \mathbb{R}^d \to \mathbb{R}^d$ acting through the ODE</p> \[\frac{dx(t)}{dt} = v_\theta(t, x(t)), \qquad x(0) \sim p_0.\] <p>The flow $\phi_t$ pushes the prior $p_0$ forward to a model distribution $p_t$. Training maximizes the log-likelihood under the change-of-variables formula:</p> \[\log p_1(x) = \log p_0(\phi_1^{-1}(x)) - \int_0^1 \nabla \cdot v_\theta(t, \phi_t^{-1}(x)) \, dt.\] <p>This is <em>gorgeous</em> — exact likelihood, single network — and <em>expensive</em>: each gradient step needs an ODE solve for every sample, and gradients flow through the solver. Scaling CNFs to ImageNet was a known open problem precisely because of this simulation tax.</p> <hr/> <h2 id="2-the-move--regress-the-vector-field-directly">2. The Move — Regress the Vector Field Directly</h2> <p>Flow Matching’s reframing is to <em>give up on maximum likelihood as the training signal</em> and instead specify a target by saying what <strong>probability path</strong> $p_t$ you want the model to follow, then <strong>regress the model’s vector field against the field that produces that path</strong>.</p> <h3 id="21-the-intractable-flow-matching-loss">2.1 The (intractable) Flow Matching loss</h3> <p>Fix a probability path $p_t$ that interpolates $p_0$ (noise) to $p_1$ (data). Suppose we know the vector field $u_t$ that generates $p_t$ (in the sense that pushing $p_0$ along $u_t$ yields $p_t$). Then the natural loss is</p> \[\mathcal{L}_{\text{FM}}(\theta) = \mathbb{E}_{t, x \sim p_t} \big\| v_\theta(t, x) - u_t(x) \big\|^2.\] <p>A clean MSE between two vector fields. <strong>Simulation-free</strong>: no ODE solve, no divergence integral. If we had $u_t$, we’d be done.</p> <p>The problem: $u_t$ is <strong>intractable</strong> for non-trivial paths. We can’t write it down in closed form for the marginal path; that’s exactly the thing the model is supposed to learn.</p> <h3 id="22-the-cfm-trick--replace-the-marginal-with-the-conditional">2.2 The CFM trick — replace the marginal with the conditional</h3> <p>Here’s the move that makes everything work. Construct $p_t$ as a <strong>mixture of conditional paths</strong>:</p> \[p_t(x) = \int p_t(x \mid x_1) \, q(x_1) \, dx_1,\] <p>where $x_1 \sim q$ is a data sample and $p_t(\cdot \mid x_1)$ is a <strong>simple conditional path</strong> from the prior to a point mass (or near-point-mass) at $x_1$. For the conditional path, the generating vector field $u_t(x \mid x_1)$ is <em>tractable in closed form</em> — it’s just the field that smoothly transports prior samples to $x_1$.</p> <p>Now define the <strong>Conditional Flow Matching</strong> loss:</p> \[\mathcal{L}_{\text{CFM}}(\theta) = \mathbb{E}_{t, x_1 \sim q, x \sim p_t(\cdot \mid x_1)} \big\| v_\theta(t, x) - u_t(x \mid x_1) \big\|^2.\] <p>The model now regresses against a <em>conditional</em> field — which we have a closed form for — instead of the marginal field — which we don’t.</p> <h3 id="23-the-theorem-that-makes-this-work">2.3 The theorem that makes this work</h3> <p>The paper’s central technical result:</p> <blockquote> <p>$\nabla_\theta \mathcal{L}<em>{\text{FM}}(\theta) = \nabla</em>\theta \mathcal{L}_{\text{CFM}}(\theta).$</p> </blockquote> <p>The two losses are <strong>not equal</strong> (they differ by a constant that depends only on the data and the path), but their gradients are <strong>identical</strong>. Optimizing the tractable CFM loss is equivalent to optimizing the intractable FM loss.</p> <p>The proof uses the fact that the marginal vector field $u_t(x)$ is the <strong>posterior expectation</strong> of the conditional fields:</p> \[u_t(x) = \mathbb{E}_{q(x_1 \mid x_t = x)} \big[ u_t(x \mid x_1) \big],\] <p>i.e. the marginal field at $x$ is a weighted average of the conditional fields toward each possible data point, weighted by how likely each data point is to have produced $x$ at time $t$. The MSE regression to a marginal mean is equivalent (in gradient) to regression to the per-sample target.</p> <p>This is the same structure that underlies <strong>denoising score matching</strong> — and the connection is not accidental. Flow Matching <strong>generalizes</strong> score matching: with a specific choice of conditional path (the variance-exploding or variance-preserving diffusion path) and a specific reparameterization, CFM reduces to the standard score-matching loss. <em>Diffusion is one instance of Flow Matching</em>, not the other way around.</p> <hr/> <h2 id="3-optimal-transport-conditional-paths">3. Optimal-Transport Conditional Paths</h2> <p>Now the design freedom: which conditional path do we use? FM works for any path, but the paper makes a specific recommendation that turns out to dominate.</p> <h3 id="31-the-ot-conditional-path">3.1 The OT conditional path</h3> <p>Pick the conditional path that <strong>linearly interpolates</strong> between a prior sample and a data sample:</p> \[\mu_t(x_1) = t \, x_1, \qquad \sigma_t(x_1) = 1 - (1 - \sigma_{\min}) t,\] <p>i.e. $x \sim \mathcal{N}(t x_1, (1 - (1-\sigma_{\min}) t)^2 I)$. At $t=0$, you’re at the standard Gaussian prior; at $t=1$, you’re at $x_1$ with a tiny residual variance $\sigma_{\min}$.</p> <p>The associated conditional vector field is</p> \[u_t(x \mid x_1) = \frac{x_1 - (1 - \sigma_{\min}) x}{1 - (1 - \sigma_{\min}) t},\] <p>which — at $\sigma_{\min} \to 0$ — is the <strong>constant velocity</strong> $x_1 - x_0$ along a <strong>straight line</strong> from prior sample $x_0$ to data sample $x_1$.</p> <p>This is the <strong>displacement interpolant</strong> from optimal transport, restricted to per-sample conditionals.</p> <h3 id="32-why-straight-lines-win">3.2 Why straight lines win</h3> <p>Diffusion conditional paths are <strong>curved</strong> — they push the sample toward $x_1$ along a noise-scaled trajectory that bends through high-noise regions before settling. Curved trajectories require more ODE-solver steps to integrate accurately at inference time.</p> <p>OT paths are <strong>straight</strong>. A straight integral curve can be integrated with very few steps — in the limit of an ideal model, <em>one step</em>. Empirically the gain is dramatic: same FID, fraction of the function evaluations.</p> <h3 id="33-what-fm-ot-inherits">3.3 What FM-OT inherits</h3> <p>The OT path keeps every good property of the diffusion path while shortening the integration:</p> <ul> <li>Closed-form conditional field (no learning $u_t$).</li> <li>Closed-form sampling from $p_t(x \mid x_1)$ (just a Gaussian).</li> <li>Straight integration → low NFE.</li> </ul> <p>Nothing about the framework changes when you swap the conditional path; only the per-sample $u_t$ formula changes. The CFM gradient- equivalence theorem still holds.</p> <hr/> <h2 id="4-headline-results">4. Headline Results</h2> <p>All numbers are training the same U-Net architecture with the same budget on the same hyperparameters; the <strong>only</strong> variable is the training objective. Lower is better on all three metrics.</p> <h3 id="41-cifar-10">4.1 CIFAR-10</h3> <table> <thead> <tr> <th>Method</th> <th>NLL ↓</th> <th>FID ↓</th> <th>NFE ↓</th> </tr> </thead> <tbody> <tr> <td>DDPM</td> <td>3.12</td> <td>7.48</td> <td>274</td> </tr> <tr> <td>FM (diffusion path)</td> <td>3.10</td> <td>8.06</td> <td>183</td> </tr> <tr> <td><strong>FM-OT</strong></td> <td><strong>2.99</strong></td> <td><strong>6.35</strong></td> <td><strong>142</strong></td> </tr> </tbody> </table> <h3 id="42-imagenet-3232">4.2 ImageNet 32×32</h3> <table> <thead> <tr> <th>Method</th> <th>NLL ↓</th> <th>FID ↓</th> <th>NFE ↓</th> </tr> </thead> <tbody> <tr> <td>DDPM</td> <td>3.54</td> <td>6.99</td> <td>262</td> </tr> <tr> <td><strong>FM-OT</strong></td> <td><strong>3.53</strong></td> <td><strong>5.02</strong></td> <td><strong>122</strong></td> </tr> </tbody> </table> <h3 id="43-imagenet-6464">4.3 ImageNet 64×64</h3> <table> <thead> <tr> <th>Method</th> <th>NLL ↓</th> <th>FID ↓</th> <th>NFE ↓</th> </tr> </thead> <tbody> <tr> <td>DDPM</td> <td>3.32</td> <td>17.36</td> <td>264</td> </tr> <tr> <td><strong>FM-OT</strong></td> <td><strong>3.31</strong></td> <td><strong>14.45</strong></td> <td><strong>138</strong></td> </tr> </tbody> </table> <h3 id="44-imagenet-128128">4.4 ImageNet 128×128</h3> <p>FM-OT: NLL <strong>2.90</strong>, FID <strong>20.9</strong> — first time a CNF result is on this scale.</p> <h3 id="45-whats-striking-about-the-table">4.5 What’s striking about the table</h3> <p>Generative-model literature typically improves <em>one</em> of (FID, NLL, NFE) by hurting the others — better quality usually costs more function evaluations; better likelihood usually costs visual quality. <strong>FM-OT improves all three at once</strong> on every dataset. That kind of clean dominance is rare and is the reason this paper landed as hard as it did.</p> <hr/> <h2 id="5-what-the-method-actually-says">5. What the Method Actually Says</h2> <p>Stripped of acronyms, the structural claim:</p> <blockquote> <ol> <li>Continuous Normalizing Flows are a useful generative model class, and the obstacle to using them was the simulation cost of maximum-likelihood training.</li> <li>You can replace likelihood with <strong>vector-field regression</strong>. The marginal target is intractable, but if you express the target path as a mixture of <em>per-sample conditional</em> paths, the conditional regression has the <em>same gradient</em> as the marginal regression.</li> <li>The choice of conditional path is a design knob. <strong>Straight-line (optimal-transport) paths</strong> are dramatically more efficient at inference time than the curved paths implicit in diffusion training.</li> <li>Diffusion training is one instance of this framework, not the other way around. The framework gives you a strictly larger design space, and within it FM-OT is a sweet spot.</li> </ol> </blockquote> <p>This is the kind of result that doesn’t just improve a benchmark — it reorganizes the field around a different abstraction.</p> <hr/> <h2 id="6-why-it-matters--three-years-later">6. Why It Matters — Three Years Later</h2> <p>Reading this paper in 2026, with the benefit of hindsight, three things stand out:</p> <ol> <li><strong>It became the training objective under most modern generative models.</strong> Stable Diffusion 3, Wan, Cosmos, and the <a href="/log/blog/2026/dreamzero/">DreamZero</a> backbone all train with flow matching (often with OT paths) rather than the original score-matching diffusion objective. What looked like a clever theoretical paper in 2022 became <em>the</em> default by 2024.</li> <li><strong>The CFM trick is reusable.</strong> Any time you want to regress a network against an intractable marginal target that’s a mixture of tractable conditionals — molecule generation, video synthesis, robot action chunks — the same gradient-equivalence argument applies. The paper effectively opened a generic recipe.</li> <li><strong>It exposed a knob the field didn’t know it had.</strong> Before this paper, “what probability path do we use?” wasn’t a question you saw in diffusion papers — the variance-preserving and variance-exploding paths were treated as <em>given</em>. Flow Matching made the path itself a design choice, which is what unlocked straight-line OT paths and, later, more exotic choices (rectified flows, consistency flow matching, etc.).</li> </ol> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>Theory is asymptotic.</strong> The gradient-equivalence theorem holds in expectation over infinitely many samples. At finite batch size, CFM has higher variance than directly fitting the marginal field would (if we could). In practice this is fine, but it’s worth knowing the theorem is not a free lunch.</li> <li><strong>OT path is <em>displacement</em> OT, not a true Monge optimal map.</strong> The “optimal transport” path is per-sample straight-line interpolation between a noise sample and a data sample — <em>which noise sample pairs with which data sample is random</em>. True OT would pair them to minimize transport cost, which the original paper does not do. The follow-up <em>minibatch OT</em> work (<a href="https://arxiv.org/abs/2302.00482">Tong et al., 2023</a>) closes that gap.</li> <li><strong>No conditional generation in the original.</strong> The paper covers unconditional generative modeling. Class- or text-conditioned FM required follow-up engineering; Stable Diffusion 3 and Wan are the modern conditional implementations.</li> <li><strong>Architecture not the contribution.</strong> The U-Net used is the standard one from the diffusion literature. The contribution is the <em>training objective</em>; the architecture is a control variable. This is also why the recipe transferred so cleanly to other backbones (DiT, transformer-based video models) later.</li> </ul> <hr/> <h2 id="8-the-takeaway-for-a-first-reader">8. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>CNFs are simulation-free trainable</strong> if you regress the model’s vector field against a target field instead of maximizing likelihood. The target field is intractable in the marginal but tractable in the conditional, and the <strong>conditional MSE has the same gradient as the marginal MSE</strong>.</li> <li><strong>The conditional path is a design choice.</strong> <strong>OT (straight-line) paths</strong> dominate diffusion paths because straight curves integrate cheaply at inference time.</li> <li><strong>The result is the first generative training recipe that wins on FID, NLL, <em>and</em> NFE simultaneously</strong> on CIFAR-10 and ImageNet, and the recipe became the default training objective under Stable Diffusion 3, Wan, and modern video / world models.</li> </ol> <p>That’s the arc: maximum-likelihood CNFs were stuck → reframe training as vector-field regression → use the conditional trick to make the regression tractable → use OT paths to make inference cheap → end up with the substrate under most modern generative modeling.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Lipman, Y., Chen, R. T. Q., Ben-Hamu, H., Nickel, M., &amp; Le, M. (2023). <em>Flow Matching for Generative Modeling.</em> ICLR 2023. <a href="https://arxiv.org/abs/2210.02747">arXiv:2210.02747</a>.</li> <li>Tong, A. <em>et al.</em> (2023). <em>Improving and generalizing flow-based generative models with minibatch optimal transport.</em> <a href="https://arxiv.org/abs/2302.00482">arXiv:2302.00482</a>.</li> <li>Background: DDPM (Ho et al., 2020); Score-based generative modeling (Song et al., 2021).</li> <li>Related on this site: <a href="/log/blog/2026/dreamzero/">DreamZero paper review</a> — the world-action-model backbone that depends on this objective; <a href="/log/blog/2026/parcae/">Parcae paper review</a> — for the discretization tricks Parcae borrows from SSMs and FM shares structural family with.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="generative-models"/><category term="diffusion"/><category term="flow-matching"/><category term="optimal-transport"/><category term="theory"/><summary type="html"><![CDATA[Paper review of Flow Matching for Generative Modeling (Lipman et al., ICLR 2023) — the paper that turned Continuous Normalizing Flows from "elegant but untrainable" into a simulation-free regression problem, introduced optimal-transport conditional paths that beat diffusion on FID/NLL/NFE simultaneously, and became the training objective under SD3, Wan, and DreamZero.]]></summary></entry><entry><title type="html">DreamZero — World Action Models as Zero-shot Robot Policies</title><link href="https://yjkim-stat.github.io/log/blog/2026/dreamzero/" rel="alternate" type="text/html" title="DreamZero — World Action Models as Zero-shot Robot Policies"/><published>2026-06-08T01:00:00+00:00</published><updated>2026-06-08T01:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/dreamzero</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/dreamzero/"><![CDATA[<p><strong>Paper.</strong> Seonghyeon Ye, Yunhao Ge, Kaiyuan Zheng, Shenyuan Gao, Sihyun Yu, George Kurian, Suneel Indupuru, You Liang Tan, Chuning Zhu, Jiannan Xiang, Ayaan Malik, Kyungmin Lee, William Liang, Nadun Ranawaka, Jiasheng Gu, Yinzhen Xu, Guanzhi Wang, Fengyuan Hu, Avnish Narayan, Johan Bjorck, Jing Wang, Gwanghyun Kim, Dantong Niu, Ruijie Zheng, Yuqi Xie, Jimmy Wu, Qi Wang, Ryan Julian, Danfei Xu, Yilun Du, Yevgen Chebotar, Scott Reed, Jan Kautz, Yuke Zhu, Linxi “Jim” Fan, Joel Jang, <em>et al.</em> <em>World Action Models are Zero-shot Policies.</em> arXiv:2602.15922, Feb 2026. NVIDIA GEAR Lab. <a href="https://arxiv.org/abs/2602.15922">[arXiv]</a> · <a href="https://dreamzero0.github.io/">[project page]</a> · <a href="https://github.com/dreamzero0/dreamzero">[code]</a> · <a href="https://huggingface.co/GEAR-Dreams/DreamZero-DROID">[HF checkpoint]</a></p> <blockquote> <p>📑 <strong>Companion slide deck.</strong> A self-contained HTML walk-through of this review (in Korean) is available at <a href="/log/assets/paper_review_html_slides/DreamZero%20%EB%85%BC%EB%AC%B8%20%ED%95%B4%EC%84%A4%20(standalone).html">DreamZero 논문 해설 (standalone).html</a>. The deck and this post share the same structure; the post is the long-form English version.</p> </blockquote> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>State-of-the-art Vision-Language-Action (VLA) models — OpenVLA, pi-zero, RT-2 — generalize <em>semantically</em> (new objects, new phrasings) but fail to generalize to <em>new physical motions</em> in new environments. They’ve been trained as policies, conditioned on massive amounts of robot demonstration data; what they’ve never seen, they can’t do. DreamZero proposes a different shape: take a <strong>pretrained video diffusion model</strong> (which has already internalized how the visual world evolves) and train it to <em>jointly predict the next video frames <strong>and</strong> the actions that produce them</em>. The result is a <strong>World Action Model (WAM)</strong> that uses video as a dense, physically-grounded representation of dynamics. With a 14B autoregressive video DiT backbone, flow-matching, and a KV-cache trick that swaps generated frames for ground-truth observations every action chunk, DreamZero hits <strong>62.2% average task progress on DROID</strong> vs. <strong>27.4% for the best pretrained VLA baseline</strong> (2×+ improvement), reaches <strong>49% task progress on tasks with unseen verbs</strong> vs. <strong>25–32%</strong> for SOTA VLAs, and runs <strong>closed-loop control at ~7Hz</strong> on Blackwell-class GPUs.</p> <hr/> <h2 id="1-the-problem--why-vlas-stall-on-new-physical-motions">1. The Problem — Why VLAs Stall on New Physical Motions</h2> <p>A VLA is fundamentally a <em>policy</em>. It maps <code class="language-plaintext highlighter-rouge">(observation, language instruction) → action</code>. Training data is human demonstrations of robot tasks, often in the millions or tens of millions of episodes.</p> <p>VLAs have proven excellent at one kind of generalization and bad at another:</p> <table> <thead> <tr> <th>Generalization axis</th> <th>VLA status</th> <th>Why</th> </tr> </thead> <tbody> <tr> <td>Semantic (new object names, new phrasings)</td> <td><strong>Good</strong></td> <td>Inherits VLM’s language + vision priors</td> </tr> <tr> <td>Physical (new motion patterns, new environments)</td> <td><strong>Poor</strong></td> <td>Trained only on demonstrated behaviors; never had to <em>predict consequences</em></td> </tr> </tbody> </table> <p>The structural complaint DreamZero makes: <strong>a policy that has only ever seen <code class="language-plaintext highlighter-rouge">(obs, lang, action)</code> triples has no internal model of <em>how the world responds to actions</em>.</strong> It can imitate trained behaviors, but it can’t reason about novel ones because it lacks the world-dynamics prior. Throwing more demonstration data at the problem helps with the demonstrated tail; it doesn’t fix the structure.</p> <hr/> <h2 id="2-the-reframing--world-action-models">2. The Reframing — World Action Models</h2> <p>DreamZero’s answer is to <strong>flip the modeling target</strong>. Instead of modeling</p> \[p(a_t \mid o_{\le t}, \ell)\] <p>(an action policy), model</p> \[p(o_{t+1:t+H}, a_{t:t+H-1} \mid o_{\le t}, \ell)\] <p>— a <strong>joint distribution over future video frames <em>and</em> future actions</strong>, given current observations and language. The video prediction half forces the model to internalize physical dynamics; the action prediction half is the policy.</p> <p>The bet is that <strong>video is a dense, scalable representation of how the world evolves</strong>, and a model that can predict <em>next frames</em> under different intended actions has, implicitly, a model of the world. The actions are then “decoded” from that internal dynamics model — making the policy a downstream consequence of the world model, not a separately learned imitation.</p> <p>This is structurally close to several recent threads:</p> <ul> <li><strong>ECHO</strong> (<a href="/log/blog/2026/echo-terminal-world-models/">trend note</a>) — train on environment-response tokens, not just action tokens. The same instinct: predicting consequences builds an implicit world model.</li> <li><strong>Parcae</strong> (<a href="/log/blog/2026/parcae/">review</a>) — diagnose architectural instability and fix by parameterization. DreamZero does similar work for autoregressive video diffusion at closed-loop control speed.</li> </ul> <hr/> <h2 id="3-architecture">3. Architecture</h2> <h3 id="31-the-video-diffusion-backbone">3.1 The video-diffusion backbone</h3> <p>DreamZero is built on a pretrained <strong>image-to-video diffusion model</strong> — specifically Wan2.1-I2V-14B (40 DiT layers, hidden dimension 5120). The backbone has already been trained on internet- scale video to predict next frames given a starting image; this is the world-dynamics prior the policy will inherit.</p> <h3 id="32-autoregressive-dit-with-flow-matching">3.2 Autoregressive DiT with flow matching</h3> <p>Standard video diffusion is <strong>non-autoregressive</strong> — it denoises a whole clip at once. That’s the wrong shape for closed-loop control, where you need to roll out a few frames, see what the world actually did, and condition the next prediction on the real observation.</p> <p>DreamZero makes the backbone <strong>autoregressive</strong> by training with flow matching instead of standard denoising and structuring the DiT to produce frames sequentially. This lets the model emit an <strong>action chunk</strong> (a few actions for the next ~150ms), execute them, observe the real next frame, and feed that real observation back as the next prefix.</p> <h3 id="33-separate-decoders-for-video-and-action">3.3 Separate decoders for video and action</h3> <p>The DiT outputs to <strong>two separate decoder heads</strong> that share the backbone:</p> <ul> <li>A <strong>video decoder</strong> producing the predicted next frames.</li> <li>An <strong>action decoder</strong> producing the predicted next action chunk.</li> </ul> <p>Both decoders are trained jointly under flow matching. The shared backbone is forced to carry information sufficient for <em>both</em> — which is what makes the action predictions “world-aware.”</p> <h3 id="34-the-closed-loop-kv-cache-swap">3.4 The closed-loop KV-cache swap</h3> <p>The single most important inference-time trick:</p> <blockquote> <p>After every action chunk executes in the real world, the resulting <strong>ground-truth observation overwrites the generated frame in the KV cache</strong>.</p> </blockquote> <p>Why this matters: autoregressive video models accumulate prediction error over time (compounding drift). In open-loop generation you just live with the drift. In <em>closed-loop control</em>, you don’t have to — every few frames you get an actual observation back from the robot. DreamZero feeds that observation into the KV cache as if it were the model’s own prediction, immediately resetting drift while retaining all the prior computation.</p> <p>This is what makes a 14B autoregressive video model viable as a real-time controller, not just an open-loop world simulator.</p> <h3 id="35-dreamzero-flash--decoupled-noise-schedules">3.5 DreamZero-Flash — decoupled noise schedules</h3> <p>A faster variant for tighter latency. The trick: during training, <strong>bias video latents toward higher noise states</strong> while keeping <strong>action noise uniform</strong>. This trains the model to infer actions from <em>noisy</em> visual context — meaning at inference time you can run fewer denoising steps on the video side without hurting action quality.</p> <p>Net effect: fewer sampling steps, same control performance.</p> <hr/> <h2 id="4-training-data-and-recipe">4. Training Data and Recipe</h2> <h3 id="41-droid--heterogeneous-robot-data">4.1 DROID — heterogeneous robot data</h3> <p>DreamZero is trained on <strong>DROID</strong>, one of the most heterogeneous open-source robot manipulation datasets — many tasks, many embodiments, many camera setups. The standard wisdom is that heterogeneity hurts training (mode interference, inconsistent control conventions). DreamZero’s bet is the opposite: a <em>world- modeling</em> objective benefits from heterogeneity because it has more ways the world can evolve to learn from.</p> <h3 id="42-pretrain-then-finetune-from-a-video-prior">4.2 Pretrain-then-finetune from a video prior</h3> <p>Two-stage recipe:</p> <ol> <li><strong>Pretrained video diffusion backbone</strong> (Wan2.1-I2V-14B) supplies the visual / physical prior.</li> <li><strong>Joint video + action training</strong> on DROID adapts the backbone to embodied dynamics + action prediction.</li> </ol> <p>No explicit reinforcement learning. The “policy” emerges from the generative objective.</p> <hr/> <h2 id="5-headline-results">5. Headline Results</h2> <h3 id="51-droid-benchmark-20-seen--20-unseen-verb-tasks">5.1 DROID benchmark (20 seen + 20 unseen-verb tasks)</h3> <table> <thead> <tr> <th>Method</th> <th>Avg task progress</th> <th>Unseen-verb task progress</th> </tr> </thead> <tbody> <tr> <td>Best pretrained VLA baseline (state-of-the-art)</td> <td>27.4%</td> <td>25–32%</td> </tr> <tr> <td><strong>DreamZero (from scratch)</strong></td> <td><strong>62.2%</strong></td> <td><strong>49%</strong></td> </tr> </tbody> </table> <ul> <li><strong>Average task progress: 2.27× the best VLA</strong>, despite the VLA baselines being pretrained on thousands of hours of cross- embodiment robot data.</li> <li><strong>Unseen verb generalization: 49% vs. 25–32%</strong> — this is the cleanest test of the “world model → better generalization” claim, and DreamZero wins decisively.</li> </ul> <h3 id="52-real-time-closed-loop-control">5.2 Real-time closed-loop control</h3> <ul> <li><strong>150ms per action chunk</strong> → <strong>~7Hz closed-loop control</strong> with the 14B model.</li> <li>Real-time achievable on <strong>GB200 (Blackwell)</strong>; <strong>H100 lacks the throughput</strong> for smooth execution.</li> </ul> <p>The hardware caveat is important: DreamZero is a real-time controller <em>on current-generation Blackwell-class GPUs</em>. The same recipe is too slow on the previous generation. Inference-side hardware is now a load-bearing assumption for this class of policy.</p> <h3 id="53-real-robot-experiments">5.3 Real robot experiments</h3> <p>Across real-world manipulation tasks: <strong>&gt;2× improvement in generalization to new tasks and environments</strong> vs. SOTA VLAs. The project page hosts video evidence of the unseen-verb and unseen-environment evaluations.</p> <hr/> <h2 id="6-what-the-method-actually-says">6. What the Method Actually Says</h2> <p>Stripped of acronyms, the structural claim:</p> <blockquote> <p>A policy trained on <code class="language-plaintext highlighter-rouge">(obs, action)</code> pairs has no way to generalize to motions it has never seen, because nothing in its training objective forces it to model <em>how the world responds to actions</em>. A policy whose backbone is a video model — trained to predict <em>next frames given proposed actions</em> — inherits an implicit world model, and that world model is what enables physical generalization. Real-time closed-loop control is achievable if you autoregress the video model and swap generated frames for ground-truth observations as they arrive.</p> </blockquote> <p>This recasts the question “how do we train a better VLA?” into “what’s the <em>backbone</em> of a robot policy?” — and the answer DreamZero ships is: a video world model.</p> <hr/> <h2 id="7-why-this-matters">7. Why This Matters</h2> <p>Three reasons, in increasing order of generality:</p> <ol> <li><strong>A new structural baseline for robot policies.</strong> For two years, the default has been “VLA pretrained on demonstration data.” If the WAM result holds up at scale, the default becomes “video- diffusion backbone, jointly trained for video + action.” This is not a small change — it’s a different <em>kind</em> of training data (internet video) and a different <em>kind</em> of objective (joint generative).</li> <li><strong>It validates the video-as-world-model thesis on actual embodied control.</strong> Video world-modeling has been a research thread (Genie, Sora-as-simulator, OASIS) for a while. DreamZero is the first time I’ve seen the thesis cash out into a <em>measurable policy improvement</em> on a standardized robot benchmark.</li> <li><strong>Closed-loop KV swap as a primitive.</strong> The trick of overwriting generated frames with ground-truth observations in the KV cache is reusable. Any autoregressive generative controller — code agents, text agents, browser agents — has an analog: “when the environment responds, replace your prediction with the truth and keep the rest of the computation.” The implementation is different in each case; the <em>principle</em> is the same. This is one of those small mechanical tricks that I expect to show up in a dozen unrelated systems over the next year.</li> </ol> <p>The connection to the trend landscape on this site is direct: the “agent rollouts contain more supervision than the final reward” argument from <a href="/log/blog/2026/echo-terminal-world-models/">ECHO</a> is the same instinct DreamZero acts on at the <em>world-modeling</em> layer — environment responses are free supervision, and the system should be designed to consume them as first-class signal, not discard them.</p> <hr/> <h2 id="8-limitations-worth-knowing">8. Limitations Worth Knowing</h2> <ul> <li><strong>Hardware-pinned real-time.</strong> ~7Hz at 14B requires Blackwell- class GPUs. On H100, smooth real-time execution isn’t there. Deployments on previous-generation hardware will need DreamZero-Flash-style optimization or model-size reductions.</li> <li><strong>DROID-centric evaluation.</strong> The headline numbers are on DROID with 20+20 tasks. Whether the generalization pattern extends to more diverse embodiments (humanoids, dexterous hands) and longer-horizon tasks is the obvious next experiment.</li> <li><strong>No reasoning over plans.</strong> DreamZero is reactive — it predicts the next few frames and the next action chunk. It does not do multi-step planning over imagined futures. The world model is there, but the <em>planning loop</em> over it isn’t.</li> <li><strong>Backbone dependence.</strong> Results assume a strong pretrained video diffusion backbone (Wan2.1-I2V-14B). Whether comparable WAMs can be built on smaller / less-trained backbones determines how reproducible the recipe is outside large labs.</li> <li><strong>No language grounding stress test.</strong> The unseen-verb benchmark is a controlled test of physical generalization. How DreamZero handles fully out-of-distribution language (long compositional instructions, negations, conditionals) is not the paper’s focus.</li> </ul> <hr/> <h2 id="9-the-takeaway-for-a-first-reader">9. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>VLAs fail at <em>new physical motions</em> because they’re trained as policies on <code class="language-plaintext highlighter-rouge">(obs, action)</code> pairs without ever modeling how the world responds.</strong> DreamZero replaces the policy backbone with a <strong>video-diffusion world model</strong> and trains it to jointly predict next frames + next actions.</li> <li><strong>A pretrained image-to-video diffusion model (Wan2.1-I2V-14B) is the backbone</strong>; an <strong>autoregressive DiT with flow matching</strong> makes it usable in closed-loop, and a <strong>KV-cache swap of ground-truth observations after each action chunk</strong> eliminates compounding drift. <strong>DreamZero-Flash</strong> decouples video/action noise schedules for faster sampling.</li> <li><strong>The result is 2×+ generalization improvement on DROID</strong> vs. SOTA VLAs (62.2% vs. 27.4% average task progress; 49% vs. 25–32% on unseen verbs) with <strong>real-time 7Hz closed-loop control on Blackwell-class GPUs</strong>.</li> </ol> <p>That’s the arc: identify the structural gap in VLAs → reframe as joint video+action generation → engineer for real-time closed-loop control → win the physical generalization test.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Ye, S., Ge, Y., <em>et al.</em> (2026). <em>World Action Models are Zero-shot Policies.</em> <a href="https://arxiv.org/abs/2602.15922">arXiv:2602.15922</a>.</li> <li>Project page: <a href="https://dreamzero0.github.io/">https://dreamzero0.github.io/</a></li> <li>Code: <a href="https://github.com/dreamzero0/dreamzero">https://github.com/dreamzero0/dreamzero</a></li> <li>Checkpoint: <a href="https://huggingface.co/GEAR-Dreams/DreamZero-DROID">https://huggingface.co/GEAR-Dreams/DreamZero-DROID</a></li> <li>Backbone: Wan2.1-I2V-14B (Alibaba video diffusion model).</li> <li>Related on this site: <a href="/log/blog/2026/echo-terminal-world-models/">ECHO trend note</a>, <a href="/log/blog/2026/parcae/">Parcae paper review</a>.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="robotics"/><category term="world-models"/><category term="video-diffusion"/><category term="vla"/><category term="zero-shot"/><summary type="html"><![CDATA[Paper review of DreamZero (Ye, Ge et al., NVIDIA GEAR, 2026) — a 14B World Action Model built on a pretrained video diffusion backbone that jointly predicts future video frames and robot actions, delivering 2× generalization to unseen tasks vs. state-of- the-art VLAs and running real-time closed-loop control at 7Hz.]]></summary></entry><entry><title type="html">Polar — Agentic RL on Any Harness at Scale</title><link href="https://yjkim-stat.github.io/log/blog/2026/polar-agentic-rl/" rel="alternate" type="text/html" title="Polar — Agentic RL on Any Harness at Scale"/><published>2026-05-25T07:00:00+00:00</published><updated>2026-05-25T07:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/polar-agentic-rl</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/polar-agentic-rl/"><![CDATA[<p><strong>Paper.</strong> Binfeng Xu, Hao Zhang, Shaokun Zhang, Songyang Han, Mingjie Liu, Jian Hu, Shizhe Diao, Zhenghui Jin, Yunheng Zou, Michael Demoret, Jan Kautz, Yi Dong. <em>Polar: Agentic RL on Any Harness at Scale.</em> arXiv:2605.24220, May 2026. NVIDIA. <a href="https://arxiv.org/abs/2605.24220">[arXiv]</a> · <a href="https://github.com/NVIDIA-NeMo/ProRL-Agent-Server">[code]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>Training a coding agent with RL sounds simple in theory: run the agent in an environment, collect the trajectory, compute the reward, update the weights. In practice the <strong>agent harness</strong> — the scaffolding that manages multi-turn tool calls, context windows, error recovery, file edits — is a complex piece of software (Codex CLI, Claude Code, Qwen Code, Pi, Aider, …), and porting it into an RL environment interface is an engineering project that <strong>loses training signal along the way</strong> (re-tokenization drift, dropped metadata, misaligned turn boundaries). Polar sidesteps the porting problem entirely: it treats the harness as a <strong>black box</strong>, interposes a proxy between the harness and the inference server, records every LLM API call at token level, and reconstructs <strong>token-faithful trajectories</strong> that any RL trainer can consume. With simple GRPO, this is enough to improve Qwen3.5-4B by <strong>+22.6</strong> points on SWE-Bench Verified (Codex harness), <strong>+6.2</strong> (Pi), <strong>+4.8</strong> (Claude Code), and <strong>+0.6</strong> (Qwen Code).</p> <hr/> <h2 id="1-the-problem--why-just-run-rl-doesnt-work-for-agents">1. The Problem — Why “Just Run RL” Doesn’t Work for Agents</h2> <p>A reasoning RL setup (the DeepSeek R1 / GRPO recipe) has a tight loop: the model generates a response, a verifier scores it, GRPO updates. The “environment” is trivial — it’s the verifier.</p> <p>An <strong>agentic</strong> RL setup is different in three structural ways:</p> <ol> <li><strong>Multi-turn, tool-using trajectories.</strong> A single episode is not one model call but a <em>sequence</em> of calls — read file, edit, run tests, read error, edit again — interspersed with tool outputs, harness-injected system messages, and context management decisions.</li> <li><strong>The harness is load-bearing.</strong> Whether the agent uses Codex CLI, Claude Code, or Pi changes the trajectory format, the tool interface, the context window management, and the error-recovery logic. These differences are not cosmetic — the same model with different harnesses produces dramatically different SWE-Bench scores.</li> <li><strong>Long-running, variable-length episodes.</strong> A single SWE-Bench task can take minutes of wall-clock time. Synchronous RL (wait for the slowest episode in the batch) wastes GPU time.</li> </ol> <p>The standard approach has been to <strong>rewrite</strong> the harness into an RL-compatible environment interface. This is expensive, error-prone, and — critically — introduces <strong>re-tokenization drift</strong>: the tokens the trainer sees are not exactly the tokens the model produced during the rollout, because the trajectory was serialized to text and re-tokenized. Polar’s argument is that this entire approach is backwards.</p> <hr/> <h2 id="2-the-polar-architecture">2. The Polar Architecture</h2> <p>Polar’s design has one key insight and three components that implement it.</p> <h3 id="the-insight">The insight</h3> <blockquote> <p>Don’t adapt the harness to the trainer. <strong>Proxy the model calls</strong> and record the trajectory at the token level. The harness runs unmodified; the proxy captures everything the trainer needs.</p> </blockquote> <h3 id="21-the-api-proxy">2.1 The API Proxy</h3> <p>Polar interposes a lightweight proxy between the harness process and the inference server (e.g. SGLang, vLLM). Every LLM API call the harness makes — chat completions, tool calls, system-message injections — passes through the proxy, which:</p> <ul> <li><strong>Records the full request and response</strong> at token level (token IDs, not just text).</li> <li><strong>Timestamps and orders</strong> every interaction for trajectory reconstruction.</li> <li><strong>Passes through transparently</strong> — the harness sees a standard OpenAI-compatible API endpoint. No code changes needed.</li> </ul> <p>This is what makes Polar harness-agnostic. The harness doesn’t know Polar exists; it just talks to what looks like a normal inference server.</p> <h3 id="22-rollout-nodes-gateway">2.2 Rollout Nodes (Gateway)</h3> <p>Each rollout node manages four phases in parallel:</p> <table> <thead> <tr> <th>Phase</th> <th>What it does</th> </tr> </thead> <tbody> <tr> <td><strong>Prewarming</strong></td> <td>Spins up the runtime environment (Docker container, repo checkout, dependency install) <em>before</em> the model starts generating. Overlaps environment setup with prior episodes’ training.</td> </tr> <tr> <td><strong>Agent execution</strong></td> <td>Runs the harness inside the primed environment. The harness calls the proxy; the proxy calls the inference server.</td> </tr> <tr> <td><strong>Trajectory reconstruction</strong></td> <td>Converts the recorded API logs into a token-faithful training trajectory — token IDs, turn boundaries, reward assignment points.</td> </tr> <tr> <td><strong>Evaluation</strong></td> <td>Runs the task’s test suite to produce a reward signal (pass/fail on SWE-Bench, or any other verifier).</td> </tr> </tbody> </table> <p>The four phases are <strong>pipelined across episodes</strong>: while episode $N$ is being evaluated, episode $N+1$ is executing, and episode $N+2$’s environment is prewarming. This is where the compute utilization gain comes from.</p> <h3 id="23-asynchronous-service-endpoints">2.3 Asynchronous Service Endpoints</h3> <p>Rollout nodes expose <strong>asynchronous service endpoints</strong> that independent trainers consume. The trainer doesn’t wait for a synchronous batch of episodes — it pulls completed trajectories as they arrive. This decouples rollout speed from training speed, which is essential when episode lengths vary by 10× (a simple bug fix vs. a complex refactor).</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌──────────────┐     ┌───────────────────────┐     ┌───────────┐
│ Trainer      │     │ Rollout Node (×N)      │     │ Inference │
│ (GRPO, any)  │◄────│ prewarm → exec → recon │────►│ Server    │
│              │     │           ↕ proxy       │     │ (SGLang)  │
└──────────────┘     └───────────────────────┘     └───────────┘
   pulls async           harness runs here            serves model
</code></pre></div></div> <p>The three-way decoupling — trainer, rollout, inference — means each can scale independently and can run different software stacks.</p> <hr/> <h2 id="3-token-faithful-trajectory-reconstruction">3. Token-Faithful Trajectory Reconstruction</h2> <p>This is the technical contribution that makes the proxy approach work for RL training, not just logging.</p> <p>The problem: RL trainers need <strong>token IDs</strong> aligned exactly with the model’s vocabulary, with correct <strong>loss masks</strong> (which tokens to update on) and <strong>turn boundaries</strong> (where the model’s contribution starts and ends). A naive approach — serialize the trajectory to text, re-tokenize at training time — introduces <strong>re-tokenization drift</strong>: different tokenization of the same text can shift token boundaries, change sequence lengths, and corrupt the training signal.</p> <p>Polar avoids this by recording token IDs directly from the proxy. The reconstruction step assembles these recorded IDs into the format the trainer expects, preserving:</p> <ul> <li><strong>Exact token identity</strong> — no re-tokenization.</li> <li><strong>Turn boundaries</strong> — which tokens are model output (trainable) vs. harness injection / tool output (masked).</li> <li><strong>Reward assignment</strong> — which turn(s) receive the episode reward.</li> </ul> <p>The paper ablates different trajectory reconstruction strategies, showing that token-faithful reconstruction outperforms text-based re-tokenization.</p> <hr/> <h2 id="4-headline-results">4. Headline Results</h2> <p>All numbers are Qwen3.5-4B trained with simple GRPO, evaluated on <strong>SWE-Bench Verified</strong>:</p> <table> <thead> <tr> <th>Harness</th> <th>Δ SWE-Bench Verified (pp)</th> </tr> </thead> <tbody> <tr> <td><strong>Codex</strong></td> <td><strong>+22.6</strong></td> </tr> <tr> <td><strong>Pi</strong></td> <td><strong>+6.2</strong></td> </tr> <tr> <td><strong>Claude Code</strong></td> <td><strong>+4.8</strong></td> </tr> <tr> <td><strong>Qwen Code</strong></td> <td><strong>+0.6</strong></td> </tr> </tbody> </table> <p>The variance across harnesses is itself a finding. The same model, the same RL algorithm, the same reward signal — but four very different improvement magnitudes depending on which harness wraps the model. This validates what the <a href="/log/blog/2026/meta-harness/">Meta-Harness review</a> on this site argued: <strong>the harness is not plumbing; it’s a first-order variable in agent performance.</strong></p> <p>The Codex harness result (+22.6) is the most dramatic, suggesting that Codex’s scaffolding provides a particularly learnable trajectory structure for GRPO to optimize over. The Qwen Code result (+0.6) is near-flat, suggesting that harness may already be near-optimal for this model size, or that its trajectory structure gives GRPO less signal to work with.</p> <hr/> <h2 id="5-why-this-matters">5. Why This Matters</h2> <p>Three reasons:</p> <ol> <li><strong>It eliminates the porting tax.</strong> Every previous agentic RL system required rewriting the harness into a custom RL environment. Polar’s proxy-based approach means any OpenAI-API-compatible harness works out of the box. This moves the cost of agentic RL from “months of engineering per harness” to “point Polar at the harness and run.”</li> <li><strong>It makes the harness itself a variable.</strong> Because Polar is harness-agnostic, you can now <strong>compare</strong> the RL-training effectiveness of different harnesses on the same model with the same algorithm. The SWE-Bench numbers across Codex / Claude Code / Qwen Code / Pi are the first controlled experiment of this kind I’ve seen. This opens a new axis of optimization: <em>which harness produces the most learnable trajectories?</em></li> <li><strong>The async architecture solves the utilization problem.</strong> Agent RL episodes are long and variable. Synchronous batching wastes GPU time waiting for the slowest episode. Polar’s decoupled rollout → trainer pipeline keeps GPUs busy because the trainer pulls trajectories as they arrive, not in synchronized batches.</li> </ol> <hr/> <h2 id="6-where-this-sits-in-the-trend-landscape">6. Where This Sits in the Trend Landscape</h2> <p>Polar is the <strong>infrastructure</strong> answer to a question several recent papers on this site have been asking from the <em>algorithm</em> side:</p> <ul> <li><a href="/log/blog/2026/echo-terminal-world-models/"><strong>ECHO</strong></a> — “stop throwing away information from the rollout.” Polar makes sure none of it is lost in the first place — token-faithful reconstruction preserves every signal the rollout produced.</li> <li><a href="/log/blog/2026/meta-harness/"><strong>Meta-Harness</strong></a> — “optimize the harness, not the model.” Polar operationalizes this: by making the harness a swappable black box, it turns harness comparison into a tractable experiment.</li> <li><a href="/log/blog/2026/rlhf-to-ruler/"><strong>RLHF → RULER trend note</strong></a> — “the bottleneck is the reward signal.” Polar is orthogonal: it assumes you <em>have</em> a reward signal (SWE-Bench’s test suite) and solves the <em>infrastructure</em> problem of getting the trajectory from the harness to the trainer without corruption.</li> <li><a href="/log/blog/2026/frontiersmith/"><strong>FrontierSmith</strong></a> — “manufacture training environments at scale.” Polar is the <em>execution</em> layer for those environments: once you have the tasks, you need a framework that can run arbitrary harnesses on them at RL scale.</li> </ul> <p>The structural bet: <strong>agentic RL will standardize on proxy-based rollout infrastructure</strong> (Polar, ProRL-Agent, SkyRL-Agent) rather than bespoke per-harness RL environments. The proxy is the right abstraction because the harness is evolving faster than any RL environment spec can track.</p> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>Single model scale shown.</strong> Qwen3.5-4B is small. Whether the per-harness variance pattern (Codex +22.6, Qwen Code +0.6) holds at 14B / 32B / 72B is the obvious next experiment.</li> <li><strong>SWE-Bench only.</strong> Agent tasks beyond code (web browsing, multi-tool workflows, research) aren’t evaluated. The architecture is task-agnostic, but the results aren’t yet.</li> <li><strong>Reward is still binary pass/fail.</strong> SWE-Bench’s test suite provides a clean signal, but the framework doesn’t address what happens when you need continuous or subjective rewards (the RULER problem from the earlier trend note).</li> <li><strong>Proxy overhead.</strong> Interposing a proxy adds latency per API call. The paper argues this is negligible relative to episode length, but for short-episode tasks the cost-benefit may flip.</li> <li><strong>Harness as black box has limits.</strong> Polar can’t optimize <em>inside</em> the harness — it can only optimize the model’s responses <em>given</em> the harness. If the harness itself makes bad decisions (e.g. truncates context poorly), Polar can’t fix that.</li> </ul> <hr/> <h2 id="8-the-takeaway-for-a-first-reader">8. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Porting agent harnesses into RL environments is the bottleneck for agentic RL</strong>, and it introduces re-tokenization drift that corrupts the training signal.</li> <li><strong>Polar treats the harness as a black box</strong>, interposes a proxy that records token-level API calls, and reconstructs <strong>token-faithful trajectories</strong> — no harness modifications, no drift.</li> <li>With simple GRPO on Qwen3.5-4B, Polar produces <strong>+22.6 pp on SWE-Bench Verified</strong> (Codex harness), demonstrating that the infrastructure problem was real and that solving it unlocks substantial agent improvement.</li> </ol> <p>That’s the arc: black-box proxy → token-faithful reconstruction → async rollout pipeline → harness-agnostic RL at scale.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Xu, B., Zhang, H., Zhang, S., Han, S., Liu, M., Hu, J., Diao, S., Jin, Z., Zou, Y., Demoret, M., Kautz, J., &amp; Dong, Y. (2026). <em>Polar: Agentic RL on Any Harness at Scale.</em> <a href="https://arxiv.org/abs/2605.24220">arXiv:2605.24220</a>.</li> <li>Code: <a href="https://github.com/NVIDIA-NeMo/ProRL-Agent-Server">https://github.com/NVIDIA-NeMo/ProRL-Agent-Server</a></li> <li>Related: ProRL Agent (<a href="https://arxiv.org/abs/2603.18815">arXiv:2603.18815</a>), SkyRL-Agent (<a href="https://arxiv.org/abs/2511.16108">arXiv:2511.16108</a>), NeMo Gym (<a href="https://github.com/NVIDIA-NeMo/Gym">https://github.com/NVIDIA-NeMo/Gym</a>).</li> <li>Related on this site: <a href="/log/blog/2026/meta-harness/">Meta-Harness review</a>, <a href="/log/blog/2026/echo-terminal-world-models/">ECHO trend note</a>, <a href="/log/blog/2026/rlhf-to-ruler/">RLHF → RULER trend note</a>, <a href="/log/blog/2026/frontiersmith/">FrontierSmith review</a>.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="llm"/><category term="rl"/><category term="agents"/><category term="infrastructure"/><category term="llm-serving"/><summary type="html"><![CDATA[Paper review of Polar (Xu et al., NVIDIA, 2026) — a rollout framework that treats any agent harness as a black box, proxies LLM API calls to record token-level interactions, reconstructs token-faithful trajectories for RL training, and improves Qwen3.5-4B by up to +22.6 points on SWE-Bench Verified across four different harnesses.]]></summary></entry><entry><title type="html">From RLHF to RULER: How the Reward Signal for RL Agents Evolved</title><link href="https://yjkim-stat.github.io/log/blog/2026/rlhf-to-ruler/" rel="alternate" type="text/html" title="From RLHF to RULER: How the Reward Signal for RL Agents Evolved"/><published>2026-05-25T05:00:00+00:00</published><updated>2026-05-25T05:00:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/rlhf-to-ruler</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/rlhf-to-ruler/"><![CDATA[<p><strong>Source.</strong> Avi Chawla, <em>“How Top AI Labs Are Building RL Agents in 2026,”</em> <a href="https://blog.dailydoseofds.com/p/how-top-ai-labs-are-building-rl-agents">Daily Dose of Data Science</a>, Apr 28, 2026. Framework: <a href="https://github.com/OpenPipe/ART">OpenPipe ART</a> (9K+ stars).</p> <hr/> <h2 id="why-this-matters">Why This Matters</h2> <p>The optimization algorithm for RL on LLMs has been settled for over a year — <strong>GRPO</strong> handles it. What hasn’t been settled is <strong>where the reward comes from.</strong> The article traces a clean three-act story:</p> <ol> <li><strong>RLHF (2022):</strong> humans rate outputs → reward model → PPO. Works, but expensive and requires four full-size models in memory.</li> <li><strong>RLVR + GRPO (2025):</strong> environment provides a deterministic signal (math checker, code compiler) → no reward model, no critic. Two-model setup. Dominant recipe for reasoning.</li> <li><strong>RULER (2026):</strong> LLM-as-judge scores <em>relative</em> rankings across a trajectory group → plugs into GRPO as-is. Extends RL to non-verifiable agent tasks (RAG, support, summarization) without writing custom reward functions.</li> </ol> <p>Each transition solves the same problem — <em>where does the reward number come from?</em> — at one higher level of generality.</p> <hr/> <h2 id="act-1-rlhf-and-the-four-model-stack">Act 1: RLHF and the Four-Model Stack</h2> <p>OpenAI’s InstructGPT (2022) established the pipeline ChatGPT was built on:</p> <ol> <li>Humans rank model outputs (expensive, one-time).</li> <li>A <strong>reward model</strong> is trained on those rankings.</li> <li><strong>PPO</strong> uses the reward model to fine-tune the LLM.</li> </ol> <p>PPO requires <strong>four</strong> full-size models in memory simultaneously:</p> <table> <thead> <tr> <th>Model</th> <th>Role</th> </tr> </thead> <tbody> <tr> <td><strong>Policy</strong></td> <td>The LLM being trained</td> </tr> <tr> <td><strong>Reference</strong></td> <td>Frozen copy for KL regularization</td> </tr> <tr> <td><strong>Reward model</strong></td> <td>Human-preference approximator</td> </tr> <tr> <td><strong>Critic (value model)</strong></td> <td>Predicts the expected reward per prompt to produce the <em>advantage</em> signal</td> </tr> </tbody> </table> <p>The critic answers: “was this reward good or bad <em>relative to what we’d normally expect for this prompt?</em>” A raw reward of 0.7 means nothing in isolation — on a factual question where most outputs score 0.9 it’s below average; on a hard open-ended question where most score 0.4 it’s excellent. The critic learns that baseline.</p> <p>For a 7B model, that’s ~28B parameters in memory at once.</p> <hr/> <h2 id="act-2-rlvr--grpo--the-deepseek-breakthrough">Act 2: RLVR + GRPO — the DeepSeek Breakthrough</h2> <p>In January 2025, DeepSeek R1 replaced both the reward model and the critic with two simpler ideas:</p> <p><strong>RLVR</strong> (Reinforcement Learning with Verifiable Rewards): for math, check if the answer matches the known solution; for code, run the compiler. Binary 1/0. No humans, no learned reward model.</p> <p><strong>GRPO</strong> (Group Relative Policy Optimization): generate $N$ responses per prompt (typically 16), normalize rewards <em>within each group</em>. If 4/16 got the answer right, those 4 get positive advantage; the other 12 get negative. The group itself is the baseline — <strong>no critic model needed.</strong></p> <p>The four-model PPO stack collapses to <strong>two</strong> (policy + reference), or in practice close to one (reference folded into checkpoint).</p> <p>The result: <strong>DeepSeek R1-Zero</strong> went from 15.6% to 77.9% on AIME 2024 with just GRPO + verifiable rewards and zero supervised fine-tuning. With majority voting, 86.7% — matching OpenAI’s o1. The model developed self-verification, reflection, and chain-of-thought <em>on its own</em>, purely from the binary correct/incorrect signal.</p> <p>RLVR + GRPO became the dominant recipe for reasoning models through</p> <ol> <li>Every major lab released a reasoning variant using it.</li> </ol> <hr/> <h2 id="act-3-the-agent-reward-gap">Act 3: The Agent Reward Gap</h2> <p>GRPO is general-purpose — it doesn’t care whether the reward comes from a math verifier, a compiler, a human, or a Python script. It just needs a number per response and normalizes within each group.</p> <p>The bottleneck is <strong>where that number comes from</strong> once you leave the verifiable-task regime:</p> <table> <thead> <tr> <th>Task type</th> <th>Reward source</th> <th>Problem</th> </tr> </thead> <tbody> <tr> <td>Math, code, logic</td> <td>Environment verifier (RLVR)</td> <td>Solved</td> </tr> <tr> <td>RAG agent</td> <td>?</td> <td>No single correct answer to string-match</td> </tr> <tr> <td>Customer support</td> <td>?</td> <td>No compiler to run it through</td> </tr> <tr> <td>Summarization</td> <td>?</td> <td>Many valid summaries; no deterministic check</td> </tr> </tbody> </table> <p>The obvious workaround: <strong>write custom reward functions in Python.</strong> A RAG reward function might check faithfulness (did it use the context?), penalize hallucination, reward completeness, and weight them. This works, but:</p> <ul> <li>Writing a good one takes <strong>days of iteration</strong>.</li> <li>It’s <strong>brittle</strong> — change the retrieval pipeline and the reward function needs rewriting.</li> <li><strong>Debugging is opaque</strong> — when the agent learns bad behavior, is it the reward weights, the training hyperparams, or the data?</li> <li>A function that over-weights format and under-weights faithfulness produces <strong>beautifully formatted hallucinations</strong>.</li> </ul> <p>This is the primary reason RL has been widely adopted for verifiable tasks but not for agent workflows.</p> <hr/> <h2 id="the-lab-responses">The Lab Responses</h2> <p>The major labs have been converging on the same gap from different directions:</p> <ul> <li><strong>Anthropic — Constitutional AI.</strong> Write down principles (a “constitution”); an AI evaluates its own outputs against those principles and generates preference data for RL. A document of rules replaced an army of human evaluators.</li> <li><strong>OpenAI — Universal Verifiers.</strong> Extending RL beyond math and code into biology, medicine, and general knowledge where answers can’t be string-matched. Details not public, but the direction is general-purpose reward signals across any domain.</li> </ul> <hr/> <h2 id="ruler--llm-as-judge-that-maps-to-grpo">RULER — LLM-as-Judge That Maps to GRPO</h2> <p><a href="https://github.com/OpenPipe/ART">RULER</a>, built into OpenPipe’s ART framework, replaces custom scoring code with a single function call. It uses an LLM-as-judge to rank multiple trajectories, exploiting the same property that makes GRPO powerful: <strong>only relative rankings matter.</strong></p> <h3 id="how-it-works">How it works</h3> <ol> <li>Generate $N$ trajectories for the same scenario (typically 4–8).</li> <li>Send all $N$ to a judge LLM (o3, o4-mini, or local Qwen3-32B).</li> <li>The judge reads the agent’s <strong>system prompt</strong> to understand the task, then scores each trajectory 0–1 <em>relative to the others</em>.</li> </ol> <p>Two properties make this work:</p> <p><strong>Relative scoring is easier than absolute scoring.</strong> LLMs struggle with calibration on absolute scales but consistently handle “which of these 4 responses best follows the instructions?” RULER presents all trajectories together and asks for a ranking.</p> <p><strong>GRPO normalizes within each group anyway.</strong> Whether the best trajectory scored 0.9 or 0.3 in absolute terms doesn’t matter — GRPO computes the mean and standard deviation within the group and normalizes. RULER’s relative rankings map directly onto what GRPO expects.</p> <h3 id="the-system-prompt-is-the-reward-function">The system prompt <em>is</em> the reward function</h3> <p>The key insight: you don’t write a reward function — the system prompt already defines what “good” means. A RAG system prompt that says <em>“Answer using ONLY the retrieved context; do not add information not in the context”</em> implicitly defines faithfulness, hallucination, and completeness. The judge applies those criteria without anyone implementing them in Python.</p> <p>The article demonstrates this concretely with four RAG trajectories (faithful, hallucinated, context-ignoring, verbose-but-accurate). RULER produces scores of 0.98, 0.20, 0.05, and 0.96 — a nuanced gradient that would take significant engineering to encode in a rule-based function. And when the system prompt is tightened (“Do not add information” vs. “Answer accurately”), the judge automatically tightens its scoring to match.</p> <h3 id="custom-rubrics">Custom rubrics</h3> <p>When the system prompt isn’t specific enough:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">custom_rubric</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">
- Prioritize concise and clear responses
- Penalize emojis or informal language
- Reward responses that cite sources
</span><span class="sh">"""</span>
<span class="k">await</span> <span class="nf">ruler_score_group</span><span class="p">(</span><span class="n">group</span><span class="p">,</span> <span class="sh">"</span><span class="s">openai/o3</span><span class="sh">"</span><span class="p">,</span> <span class="n">rubric</span><span class="o">=</span><span class="n">custom_rubric</span><span class="p">)</span>
</code></pre></div></div> <p>Natural language, not Python. Iterating is fast — change a sentence, rerun, check the scores — versus editing a reward function where a misplaced weight silently teaches bad behavior.</p> <h3 id="combining-with-deterministic-verifiers">Combining with deterministic verifiers</h3> <p>For tasks that are partially verifiable:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">judged_group</span> <span class="o">=</span> <span class="k">await</span> <span class="nf">ruler_score_group</span><span class="p">(</span><span class="n">group</span><span class="p">,</span> <span class="sh">"</span><span class="s">openai/o3</span><span class="sh">"</span><span class="p">)</span>
<span class="k">for</span> <span class="n">traj</span> <span class="ow">in</span> <span class="n">judged_group</span><span class="p">.</span><span class="n">trajectories</span><span class="p">:</span>
    <span class="n">traj</span><span class="p">.</span><span class="n">reward</span> <span class="o">+=</span> <span class="nf">verify_correctness</span><span class="p">(</span><span class="n">traj</span><span class="p">)</span>  <span class="c1"># binary 0/1
</span></code></pre></div></div> <p>RULER preserves any rewards assigned during rollout, so you can layer LLM-judge scoring on top of deterministic verification.</p> <hr/> <h2 id="practical-notes-from-the-article">Practical Notes from the Article</h2> <ul> <li><strong>Judge model choice is a cost-quality tradeoff</strong>, not a hard requirement. Qwen3-32B often works; doesn’t need o3.</li> <li><strong>4–8 trajectories per group</strong> is the sweet spot. Fewer gives the judge too little to compare; more can confuse it.</li> <li><strong>Common-prefix deduplication.</strong> When all trajectories share the same system/user messages (usual case), RULER deduplicates automatically — significant token savings on long system prompts.</li> <li><strong>Disk caching.</strong> RULER caches judge responses; reruns on the same trajectories skip the API. Matters when iterating on the system prompt or rubric.</li> </ul> <hr/> <h2 id="where-this-sits-in-the-trend-landscape">Where This Sits in the Trend Landscape</h2> <p>The article crystallizes a progression that several recent posts on this site have been touching from different angles:</p> <ul> <li><a href="/log/blog/2026/soar/"><strong>SOAR</strong></a> — RL stalls when the model never solves the problem (zero reward). SOAR’s fix is to change the <em>problem distribution</em>. RULER’s fix is to change the <em>reward source</em> — from binary verifier to relative LLM judge. Different angles on the same scarcity.</li> <li><a href="/log/blog/2026/echo-terminal-world-models/"><strong>ECHO</strong></a> — “agent rollouts contain more supervision than the final reward.” ECHO uses env-token prediction as free auxiliary signal; RULER uses an LLM judge as a general-purpose signal. Both argue the same thing: <strong>stop throwing away information from the rollout.</strong></li> <li><a href="/log/blog/2026/meta-harness/"><strong>Meta-Harness</strong></a> — optimize the <em>harness</em>, not the model. RULER’s framing is the same: the system prompt is the harness, and RULER’s judge scores against it. Iterating on the system prompt iterates on the reward function.</li> <li><a href="/log/blog/2026/learning-fast-and-slow/"><strong>Fast-Slow Training</strong></a> — the “fast” surface is the prompt; the “slow” surface is the weights. RULER makes the fast surface (the system prompt) the <em>evaluation criterion</em>, not just the conditioning context.</li> </ul> <p>The structural prediction implicit in all of these: <strong>the era of hand-written reward functions for agent RL is ending.</strong> The system prompt, a rubric, or a constitution — all natural-language descriptions of what “good” means — will replace Python scoring code as the default reward interface.</p> <hr/> <h2 id="tldr">TL;DR</h2> <p>The bottleneck in applying RL to agents was never the optimization algorithm (GRPO handles it). It was always the <strong>reward signal</strong>. Three acts:</p> <ol> <li><strong>RLHF</strong> (2022) — humans → reward model → PPO. Four models in memory. Expensive.</li> <li><strong>RLVR + GRPO</strong> (2025) — environment verifier → binary reward. Two models. Dominant for reasoning. But only works when the answer is checkable.</li> <li><strong>RULER</strong> (2026) — LLM judge scores trajectories <em>relatively</em> within a GRPO group. The system prompt <em>is</em> the reward function. Works on non-verifiable agent tasks (RAG, support, summarization) with <strong>no custom scoring code</strong>. Built into OpenPipe’s open-source <a href="https://github.com/OpenPipe/ART">ART framework</a>.</li> </ol> <hr/> <h2 id="references">References</h2> <ul> <li>Chawla, A. (2026). <em>How Top AI Labs Are Building RL Agents in 2026.</em> <a href="https://blog.dailydoseofds.com/p/how-top-ai-labs-are-building-rl-agents">Daily Dose of Data Science</a>.</li> <li>OpenPipe ART framework: <a href="https://github.com/OpenPipe/ART">https://github.com/OpenPipe/ART</a></li> <li>Background: InstructGPT / RLHF (Ouyang et al., 2022), DeepSeek R1 / GRPO (DeepSeek, 2025), Constitutional AI (Anthropic, 2022).</li> <li>Related on this site: <a href="/log/blog/2026/soar/">SOAR review</a>, <a href="/log/blog/2026/echo-terminal-world-models/">ECHO trend note</a>, <a href="/log/blog/2026/meta-harness/">Meta-Harness review</a>, <a href="/log/blog/2026/learning-fast-and-slow/">Fast-Slow Training trend note</a>.</li> </ul>]]></content><author><name></name></author><category term="trends"/><category term="llm"/><category term="rl"/><category term="grpo"/><category term="agents"/><category term="reward-signal"/><summary type="html"><![CDATA[Trend note tracing the RL reward-signal problem from RLHF's four-model PPO stack (2022) through RLVR+GRPO's two-model breakthrough (2025) to RULER's LLM-as-judge scoring for non-verifiable agent tasks (2026), with a walkthrough of OpenPipe's ART framework.]]></summary></entry><entry><title type="html">AlpaServe — Statistical Multiplexing with Model Parallelism for DL Serving</title><link href="https://yjkim-stat.github.io/log/blog/2026/alpaserve/" rel="alternate" type="text/html" title="AlpaServe — Statistical Multiplexing with Model Parallelism for DL Serving"/><published>2026-05-21T02:30:00+00:00</published><updated>2026-05-21T02:30:00+00:00</updated><id>https://yjkim-stat.github.io/log/blog/2026/alpaserve</id><content type="html" xml:base="https://yjkim-stat.github.io/log/blog/2026/alpaserve/"><![CDATA[<p><strong>Paper.</strong> Zhuohan Li, Lianmin Zheng, Yinmin Zhong, Vincent Liu, Ying Sheng, Xin Jin, Yanping Huang, Zhifeng Chen, Hao Zhang, Joseph E. Gonzalez, Ion Stoica. <em>AlpaServe: Statistical Multiplexing with Model Parallelism for Deep Learning Serving.</em> OSDI 2023. UC Berkeley · Google · Peking University · Stanford · UCSD. <a href="https://arxiv.org/abs/2302.11665">[arXiv:2302.11665]</a> · <a href="https://www.usenix.org/conference/osdi23/presentation/li-zhouhan">[OSDI paper]</a> · <a href="https://github.com/alpa-projects/mms">[code]</a></p> <hr/> <h2 id="0-the-picture-in-one-paragraph">0. The Picture in One Paragraph</h2> <p>The textbook reason for <strong>model parallelism</strong> is “the model is too big to fit on one GPU; split it.” AlpaServe argues for a different reason that became, in retrospect, the foundation under most modern multi-model LLM serving stacks: <strong>even when each model fits on one GPU, splitting it across multiple GPUs is the right move whenever your workload is bursty and multi-model.</strong> The reason is <em>statistical multiplexing</em> — pooling many bursty traffic streams onto a shared fleet smooths the aggregate, the same way trunk-line provisioning works in telephony. Replication-and-bin-packing does the opposite: each model gets its own GPU and a burst on one model starves while GPUs holding <em>other</em> models sit idle. AlpaServe formalizes the tradeoff between model-parallel overhead and multiplexing gain, proposes a placement algorithm that searches the joint space of (GPU-group partition, parallelism strategy, model assignment), and shows on production traces that the resulting system handles <strong>~10× higher request rates</strong> or <strong>~6× more burstiness</strong> at <strong>&gt;99% SLO attainment</strong>.</p> <hr/> <h2 id="1-the-setup--why-replication-and-bin-pack-fails-under-bursts">1. The Setup — Why Replication-and-Bin-Pack Fails Under Bursts</h2> <p>The natural multi-model serving design is the simple one:</p> <blockquote> <p>For each model, allocate enough GPU copies to handle its expected load. Pack as many copies as fit on each GPU. Route each request to a free copy of the right model.</p> </blockquote> <p>This is essentially what early systems like <strong>Clipper</strong> did, and it works well when arrival rates are smooth and predictable. Two things break it.</p> <p><strong>Burstiness.</strong> Real production traffic is not Poisson; it’s <em>bursty</em>. The Microsoft Azure traces used in the paper have peaks that are 10×+ above the long-run average for individual models. Under a burst, the model’s dedicated GPUs queue; SLOs miss; the “spare” GPUs hosting <em>other</em> models can’t help, because they don’t have the right weights loaded.</p> <p><strong>Head-of-line blocking.</strong> A handful of long requests in a queue hold up everything behind them. A serving system that can only process one model per GPU is forced to choose between waiting and dropping.</p> <p>The deep structural complaint: <strong>per-model GPU pools are statistically independent</strong>, which is exactly the wrong thing if you want bursty streams to cancel each other out.</p> <hr/> <h2 id="2-the-idea--model-parallelism-as-multiplexing">2. The Idea — Model Parallelism <em>as Multiplexing</em></h2> <p>Statistical multiplexing is the classical answer: pool many bursty sources onto a shared channel and the aggregate is much smoother than any individual source. For DL serving, that means putting <em>multiple models on the same set of GPUs</em> and letting any request to any model use any GPU.</p> <p>The catch: each forward pass needs <em>all the model’s weights</em>. So the question becomes — how do you make the GPUs of a pool capable of serving <em>any</em> of the colocated models?</p> <p>AlpaServe’s answer is <strong>model parallelism — specifically pipeline parallelism</strong>. Split each model into $k$ stages across $k$ GPUs. Multiple models can share the same $k$-GPU group; a burst on model A and a quieter moment on model B interleave naturally because each request only occupies a stage at a time, not the whole GPU.</p> <p>This is counter-intuitive because the conventional wisdom says model parallelism is <em>expensive</em> — extra communication, extra synchronization, lower per-request throughput than a single-GPU copy. AlpaServe’s contribution is to make this trade <em>quantitative</em>:</p> <blockquote> <p>Model parallelism has a per-request overhead $O$ (communication + pipeline bubble + smaller per-stage compute) but gives a multiplexing gain $M$ that grows with the number of colocated bursty streams and the burst factor. For bursty multi-model workloads, $M$ dominates $O$ over a wide regime.</p> </blockquote> <p>The whole paper is the formalization, the algorithm to find a good operating point, and the empirical demonstration that the regime exists.</p> <hr/> <h2 id="3-the-tradeoff-in-pictures">3. The Tradeoff in Pictures</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>   Replication + bin-pack                Model-parallel colocation
   (Clipper-style)                       (AlpaServe-style)

   GPU 1: [Model A copy]                 GPU 1: [A stage 1 | B stage 1 | C stage 1]
   GPU 2: [Model A copy]                 GPU 2: [A stage 2 | B stage 2 | C stage 2]
   GPU 3: [Model B copy]                 GPU 3: [A stage 3 | B stage 3 | C stage 3]
   GPU 4: [Model C copy]                 GPU 4: [A stage 4 | B stage 4 | C stage 4]

   - A burst on A blocks A's queue       - A burst on A streams through all 4 GPUs
   - GPUs 3, 4 are idle                  - B and C still progress in pipeline bubbles
   - per-request latency: low            - per-request latency: slightly higher
   - tail latency under burst: bad       - tail latency under burst: dramatically better
</code></pre></div></div> <p>The picture on the right is what AlpaServe is selling. The cost is a constant overhead per request; the benefit is that the <em>whole cluster</em> now backs every model.</p> <hr/> <h2 id="4-the-placement-problem">4. The Placement Problem</h2> <p>Once you accept “use model parallelism to multiplex,” the system question becomes: <em>which</em> parallelism strategy and <em>which</em> model groupings?</p> <p>AlpaServe formulates this as a joint search over three things:</p> <ol> <li><strong>GPU-group partition</strong> — split the cluster’s $N$ GPUs into disjoint groups, each of size $k_g$.</li> <li><strong>Parallelism strategy per group</strong> — for each group, pick a tensor / pipeline / hybrid parallelism configuration.</li> <li><strong>Model-to-group assignment</strong> — decide which models live in which group (a model can be replicated across groups).</li> </ol> <p>The objective is <strong>SLO attainment</strong> under a target arrival distribution: maximize the fraction of requests that complete within their latency bound.</p> <p>This is a hard combinatorial problem. AlpaServe’s approach:</p> <ul> <li><strong>Simulator-driven evaluation.</strong> A discrete-event simulator estimates SLO attainment for any candidate placement. This is fast enough to score thousands of placements.</li> <li><strong>Enumeration + greedy search.</strong> Enumerate plausible group partitions; for each, enumerate parallelism configurations using Alpa’s existing auto-parallelization machinery; greedily assign models to groups by load.</li> <li><strong>Online refinement.</strong> At runtime, a scheduler does <strong>continuous batching</strong> within each group and <strong>request routing</strong> across groups, with awareness of pipeline-stage occupancy.</li> </ul> <p>The simulator is doing real work here: the search space is too big for closed-form analysis, and too coupled for naive heuristics.</p> <hr/> <h2 id="5-evaluation">5. Evaluation</h2> <p>The experimental setup matches the paper’s argument carefully.</p> <h3 id="51-workloads">5.1 Workloads</h3> <ul> <li><strong>Microsoft Azure Function trace (MAF1, MAF2)</strong> — production bursty arrival patterns.</li> <li><strong>Synthetic Gamma-process arrivals</strong> — to sweep burstiness parameters cleanly.</li> <li><strong>A heterogeneous “model zoo”</strong> with a mix of BERT-class, GPT-class, and larger models.</li> </ul> <h3 id="52-baselines">5.2 Baselines</h3> <ul> <li><strong>Clipper-style replication + bin-pack.</strong> Dedicated GPU pools per model.</li> <li><strong>AlpaSPMD.</strong> SPMD model parallelism <em>without</em> statistical multiplexing — i.e., model parallelism for capacity reasons but one model per group.</li> <li><strong>Various oracle baselines</strong> for sanity.</li> </ul> <h3 id="53-headline-numbers">5.3 Headline numbers</h3> <table> <thead> <tr> <th>Metric</th> <th>AlpaServe vs. best baseline</th> </tr> </thead> <tbody> <tr> <td>Throughput at fixed SLO (99% attainment)</td> <td><strong>up to 10× higher request rate</strong></td> </tr> <tr> <td>Tolerable burstiness at fixed SLO</td> <td><strong>up to 6× more burst-factor</strong></td> </tr> <tr> <td>SLO attainment under matched load</td> <td><strong>&gt;99%</strong> where baselines drop to single-digit percentages</td> </tr> </tbody> </table> <p>The reading of these numbers: under <em>steady</em> load, AlpaServe is comparable to a well-tuned replication system (the model-parallel overhead is real). Under <em>bursty</em> load, AlpaServe pulls dramatically ahead, because the baselines have no way to absorb bursts that exceed the dedicated per-model capacity.</p> <h3 id="54-the-right-ablations">5.4 The right ablations</h3> <ul> <li><strong>Vary burstiness</strong> — the AlpaServe gain <em>increases</em> with burst factor. Confirms the mechanism.</li> <li><strong>Vary the number of colocated models</strong> — multiplexing gain grows sublinearly with colocation count, consistent with the law-of-large-numbers intuition.</li> <li><strong>Vary parallelism configuration</strong> — pipeline parallelism gives the bulk of the multiplexing benefit; tensor parallelism is complementary, not a substitute.</li> </ul> <hr/> <h2 id="6-why-it-matters">6. Why It Matters</h2> <p>Three reasons, in increasing order of generality.</p> <ol> <li><strong>It changed the default for multi-model LLM serving.</strong> Before AlpaServe, “split a model across GPUs only if you can’t fit it” was conventional. After AlpaServe, model parallelism became a <em>load-balancing</em> tool. Modern serving systems — <a href="/log/explorations/2026-05-21-why-inference-is-hard-notes/">vLLM, SGLang, TensorRT-LLM, TGI</a> — all assume this regime now.</li> <li><strong>It formalized a tradeoff people were arguing about without numbers.</strong> Pre-AlpaServe debates about “is model parallelism worth it for serving” were qualitative; the paper turned them into a measurable curve over burstiness and colocation count.</li> <li><strong>It re-introduced a 100-year-old systems idea (statistical multiplexing) into ML serving.</strong> The framing — “ML serving is trunk-line provisioning, with model parallelism as the trunking mechanism” — is the kind of cross-disciplinary handle that keeps generating downstream work (continuous batching, prefix-cache sharing, paged KV, RadixAttention) because it gives a unifying objective.</li> </ol> <p>For where to read next on this site: the <a href="/log/reports/inference-architecture/">Inference Architecture report</a> is the broader survey; the <a href="/log/explorations/2026-05-21-why-inference-is-hard-notes/"><em>Why Inference is Hard</em> notes</a> walk through the modern engine landscape that builds on top of AlpaServe’s argument.</p> <hr/> <h2 id="7-limitations-worth-knowing">7. Limitations Worth Knowing</h2> <ul> <li><strong>Pre-LLM era assumptions.</strong> The paper predates the dominance of <em>autoregressive decoding</em> as the serving bottleneck. Modern LLM serving’s main pain point is the <strong>decode</strong> phase’s memory-bandwidth limit on KV cache, which AlpaServe’s framing doesn’t directly model. The <em>statistical-multiplexing</em> argument still applies, but the <em>parallelism choice</em> in modern systems weighs KV-cache placement very differently.</li> <li><strong>Homogeneous GPU assumption.</strong> Real clusters are mixed (A100, H100, B200, different memory tiers). AlpaServe’s placement algorithm assumes a homogeneous device pool; extending it cleanly is non-trivial.</li> <li><strong>Static workload profile.</strong> Placement is computed against an estimated arrival distribution. Diurnal shifts, traffic regime changes, and product launches require re-planning; AlpaServe doesn’t address online re-placement as a first-class problem.</li> <li><strong>Model-mix assumed known.</strong> The set of served models and their approximate load mix is treated as input. Auto-discovery of which models to colocate based on observed traffic is left for later work.</li> </ul> <hr/> <h2 id="8-the-takeaway-for-a-first-reader">8. The Takeaway for a First Reader</h2> <p>If you remember three things:</p> <ol> <li><strong>Replication-and-bin-pack fails under bursts</strong> because per-model GPU pools are statistically independent and idle GPUs on quiet models can’t help busy models.</li> <li><strong>Model parallelism is multiplexing</strong>, not just capacity provisioning. Splitting each model across a shared GPU group lets all GPUs back all colocated models, and the law-of-large-numbers takes care of bursts.</li> <li><strong>There is a measurable tradeoff</strong> between model-parallel overhead and multiplexing gain. AlpaServe formalizes the tradeoff, searches the joint placement / parallelism space with a simulator-driven algorithm, and demonstrates <strong>~10× higher request rate</strong> or <strong>~6× more burst tolerance</strong> at &gt;99% SLO on production traces.</li> </ol> <p>That’s the arc: rethink why we use model parallelism → formalize the tradeoff → find a good operating point → show it on real traffic.</p> <hr/> <h2 id="references">References</h2> <ul> <li>Li, Z., Zheng, L., Zhong, Y., Liu, V., Sheng, Y., Jin, X., Huang, Y., Chen, Z., Zhang, H., Gonzalez, J. E., &amp; Stoica, I. (2023). <em>AlpaServe: Statistical Multiplexing with Model Parallelism for Deep Learning Serving.</em> OSDI 2023. <a href="https://arxiv.org/abs/2302.11665">arXiv:2302.11665</a> · <a href="https://www.usenix.org/conference/osdi23/presentation/li-zhouhan">USENIX</a>.</li> <li>Code: <a href="https://github.com/alpa-projects/mms">https://github.com/alpa-projects/mms</a></li> <li>OSDI ‘23 talk: <a href="https://www.youtube.com/watch?v=k0fuwdkN4LA">https://www.youtube.com/watch?v=k0fuwdkN4LA</a></li> <li>Related on this site: <a href="/log/reports/inference-architecture/">Inference Architecture report</a>, <a href="/log/explorations/2026-05-21-why-inference-is-hard-notes/"><em>Why Inference is Hard</em> notes</a>.</li> </ul>]]></content><author><name></name></author><category term="paper-review"/><category term="inference"/><category term="llm-serving"/><category term="systems"/><category term="model-parallelism"/><category term="scheduling"/><summary type="html"><![CDATA[Paper review of AlpaServe (Li et al., OSDI 2023) — the systems paper that argued model parallelism is not just for fitting big models on big clusters but for *statistically multiplexing* bursty multi-model workloads, and showed it can serve requests at up to 10× higher rates or absorb 6× more burstiness at>99% SLO attainment.]]></summary></entry></feed>